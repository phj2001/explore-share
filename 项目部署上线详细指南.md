# 项目部署上线详细指南

本文档用于完整记录本项目从本机打包到服务器上线的全过程，适用于当前这套项目结构：

- 前端：Vue 3 + Vite
- 后端：Spring Boot
- 数据库：PostgreSQL
- 反向代理：Nginx
- 域名解析：Cloudflare
- 服务器连接工具：FinalShell

本文档按实际部署顺序编写，并区分：

- 本机 PowerShell 操作
- 服务器终端操作
- Cloudflare 控制台操作

所有命令都尽量先检查，再安装、再配置、再验证。

---

## 1. 部署总览

### 1.1 最终架构

部署完成后的访问链路如下：

1. 用户访问 `https://map.jieai.shop`
2. Cloudflare 负责 DNS、代理、防护、HTTPS 接入
3. Nginx 提供前端静态资源，并反向代理 `/api` 和 `/uploads`
4. Spring Boot 提供业务接口
5. PostgreSQL 存储业务数据
6. 本地磁盘目录保存上传文件

### 1.2 推荐部署顺序

1. 本机整理项目生产配置
2. 前端打包与服务器静态部署
3. 服务器安装并配置 PostgreSQL
4. 本机打包后端 JAR 并上传服务器
5. 配置 Spring Boot 生产环境与 systemd
6. 配置 Nginx 静态站点与反向代理
7. 通过服务器公网 IP 验证前后端联通
8. 配置 Cloudflare 二级域名解析
9. 绑定域名并申请 HTTPS
10. 安全加固与密码轮换

### 1.3 服务器目录规划

本文档采用如下目录布局：

```text
/var/www/place-explore/releases/           前端历史发布目录
/var/www/place-explore/current/            前端当前生效版本（软链接）
/opt/place-explore/backend/                后端运行目录
/opt/place-explore/backend/config/         后端生产配置目录
/opt/place-explore/backend/logs/           后端日志目录（可选）
/opt/place-explore/shared/uploads/         上传文件根目录
```

这样做的好处：

- 前端可以按版本发布和回滚
- 后端与上传文件分离，避免覆盖
- 配置文件放在项目产物之外，更安全

---

## 2. 部署前准备

### 2.1 本机需要准备

- Windows + PowerShell
- Node.js / npm
- Java 21
- Maven 或可用的 `mvnw.cmd`
- FinalShell
- 项目完整代码

### 2.2 服务器需要准备

- Linux 服务器，本文以 Ubuntu / Debian 为例
- root 权限或具备 sudo 权限的用户
- 公网 IP

### 2.3 域名与第三方服务需要准备

- 主域名已接入 Cloudflare
- 准备用于本项目的二级域名，例如 `map.jieai.shop`
- 高德前端 JS Key
- 高德 Web 服务 Key

### 2.4 需要预先准备的敏感信息

上线前建议先准备以下信息，后续填配置时统一使用：

- PostgreSQL 数据库用户名和密码
- `JWT_SECRET`
- 超级管理员账号和密码
- 高德 JS Key
- 高德 Web Key

注意：

- 不要把真实密码写进 Git 仓库
- 不要把真实密码提交到源码目录
- 建议所有真实生产配置都放在服务器外部配置文件或 systemd 环境变量中

---

## 3. 环境检查与安装

本节所有命令均在服务器终端执行。

### 3.1 检查服务器系统信息

```bash
cat /etc/os-release
uname -m
whoami
pwd
```

作用说明：

- `cat /etc/os-release`：查看服务器发行版信息
- `uname -m`：查看 CPU 架构
- `whoami`：确认当前登录用户
- `pwd`：确认当前所在目录

### 3.2 检查并安装 Nginx

先检查是否已安装：

```bash
nginx -v
```

如果提示命令不存在，再安装：

```bash
apt update
apt install -y nginx
```

安装后启动并设置开机自启：

```bash
systemctl enable nginx
systemctl start nginx
systemctl status nginx --no-pager
```

作用说明：

- `nginx -v`：检查 Nginx 是否已安装
- `apt update`：刷新软件包索引
- `apt install -y nginx`：安装 Nginx
- `systemctl enable`：设置开机自启
- `systemctl start`：立即启动服务
- `systemctl status`：查看当前运行状态

### 3.3 检查并安装 PostgreSQL

先检查是否已安装：

```bash
psql --version
```

如果未安装：

```bash
apt update
apt install -y postgresql postgresql-contrib
```

安装后启动并设置开机自启：

```bash
systemctl enable postgresql
systemctl start postgresql
systemctl status postgresql --no-pager
```

作用说明：

- `psql --version`：检查 PostgreSQL 客户端是否存在
- `postgresql-contrib`：安装 PostgreSQL 扩展组件
- `systemctl ... postgresql`：管理数据库服务

### 3.4 检查并安装 Java 21

先检查是否已安装：

```bash
java -version
```

如果未安装或版本不是 21：

```bash
apt update
apt install -y openjdk-21-jdk
java -version
```

作用说明：

- `java -version`：检查 Java 版本
- `openjdk-21-jdk`：安装 Java 21 开发运行环境

### 3.5 检查并安装 Certbot

先检查是否已安装：

```bash
certbot --version
```

如果未安装：

```bash
apt update
apt install -y certbot python3-certbot-nginx
```

作用说明：

- `certbot --version`：检查证书工具是否存在
- `python3-certbot-nginx`：安装 Nginx 插件，便于自动修改 Nginx 配置

### 3.6 检查防火墙

```bash
ufw status
```

如果系统未安装 `ufw`，可以先忽略。后续再做安全加固。

---

## 4. 项目上线目录初始化

本节所有命令均在服务器终端执行。

### 4.1 创建前端目录

```bash
mkdir -p /var/www/place-explore/releases
mkdir -p /var/www/place-explore/current
```

作用说明：

- `releases`：保存每次前端发布版本
- `current`：当前线上生效版本，通常做成软链接

### 4.2 创建后端与上传目录

```bash
mkdir -p /opt/place-explore/backend
mkdir -p /opt/place-explore/backend/config
mkdir -p /opt/place-explore/backend/logs
mkdir -p /opt/place-explore/shared/uploads/avatars
mkdir -p /opt/place-explore/shared/uploads/poi-shares
mkdir -p /opt/place-explore/shared/uploads/announcements
mkdir -p /opt/place-explore/shared/uploads/activities
mkdir -p /opt/place-explore/shared/uploads/routes
```

作用说明：

- `/opt/place-explore/backend`：放后端 JAR
- `/opt/place-explore/backend/config`：放生产配置文件
- `/opt/place-explore/shared/uploads/...`：放用户上传的文件

### 4.3 创建后端运行用户

```bash
id placeapp || useradd --system --home /opt/place-explore --shell /usr/sbin/nologin placeapp
```

作用说明：

- `id placeapp`：检查运行用户是否已存在
- `useradd --system`：创建系统用户，供后端服务使用

### 4.4 设置目录权限

```bash
chown -R placeapp:placeapp /opt/place-explore/backend
chown -R placeapp:placeapp /opt/place-explore/shared/uploads
```

作用说明：

- 让后端进程用户有权读取 JAR、写入上传目录

---

## 5. 前端部署

本节分为本机操作和服务器操作两部分。

### 5.1 本机准备生产环境变量

在本机 PowerShell 执行：

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-frontend
Copy-Item .env.production.example .env.production
notepad .env.production
```

作用说明：

- 进入前端目录
- 从示例文件复制出正式生产环境配置文件
- 用记事本编辑生产配置

推荐配置内容：

```env
VITE_API_BASE_URL=/api
VITE_AMAP_JS_KEY=你的高德JS_KEY
VITE_AMAP_SECURITY_JS_CODE=你的高德安全密钥
VITE_COORDINATE_SOURCE=wgs84
```

说明：

- `VITE_API_BASE_URL=/api`：前端通过同域名下的 `/api` 请求后端
- 生产环境不要再写 `localhost:8080`

### 5.2 本机构建前端

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-frontend
npm install
npm run build
```

作用说明：

- `npm install`：安装依赖
- `npm run build`：打包生产环境静态文件

打包成功后会生成：

```text
smart-campus-frontend\dist
```

### 5.3 本机压缩前端产物

```powershell
Compress-Archive -Path .\dist\* -DestinationPath .\frontend-dist.zip -Force
```

作用说明：

- 将前端打包结果压缩，便于通过 FinalShell 上传

### 5.4 服务器创建发布目录

```bash
mkdir -p /var/www/place-explore/releases/frontend-20260402
```

作用说明：

- 创建本次前端发布目录
- 日期可根据实际发布时间修改

### 5.5 使用 FinalShell 上传前端压缩包

操作位置：FinalShell 文件传输面板。

上传路径：

```text
/var/www/place-explore/releases/frontend-20260402
```

上传文件：

```text
frontend-dist.zip
```

### 5.6 服务器解压前端文件

```bash
cd /var/www/place-explore/releases/frontend-20260402
unzip frontend-dist.zip
rm -f frontend-dist.zip
```

作用说明：

- 解压前端静态文件
- 删除压缩包，保持目录整洁

### 5.7 切换当前生效版本

```bash
rm -rf /var/www/place-explore/current
ln -s /var/www/place-explore/releases/frontend-20260402 /var/www/place-explore/current
ls -la /var/www/place-explore/current
```

作用说明：

- 删除旧的 `current`
- 将 `current` 软链接到本次发布版本
- 检查软链接是否正确

### 5.8 前端部署验证

验证方式：

- 服务器浏览器或本机浏览器访问公网 IP
- 访问首页
- 访问 `/login`
- 刷新 `/login` 不应 404

---

## 6. 数据库部署

本节所有命令均在服务器终端执行。

### 6.1 创建数据库用户和数据库

进入 PostgreSQL：

```bash
sudo -u postgres psql
```

执行 SQL：

```sql
CREATE USER place_app WITH PASSWORD '你的强密码';
CREATE DATABASE place_explore OWNER place_app;
GRANT ALL PRIVILEGES ON DATABASE place_explore TO place_app;
\q
```

作用说明：

- `CREATE USER`：创建专用数据库用户
- `CREATE DATABASE`：创建业务数据库
- `GRANT ALL PRIVILEGES`：授权该用户管理该数据库

### 6.2 验证数据库连接

```bash
PGPASSWORD='你的强密码' psql -h 127.0.0.1 -U place_app -d place_explore -c '\conninfo'
```

作用说明：

- 用新用户从本机连接数据库
- 验证用户名、密码、数据库名是否全部正确

成功表现：

- 显示已连接到 `place_explore`

### 6.3 查看库和用户

```bash
sudo -u postgres psql -c "\l"
sudo -u postgres psql -c "\du"
```

作用说明：

- `\l`：查看数据库列表
- `\du`：查看数据库用户列表

### 6.4 数据库安全建议

- 不要开放 5432 到公网
- 后端通过 `127.0.0.1` 本机连接数据库
- 数据库密码不要写进 Git

### 6.5 修复 `pois.id` 自增序列（重要）

本项目的 `POI` 实体使用了 PostgreSQL sequence，自增主键理论上应由数据库自动生成。

但如果你的数据库表是早期手工创建、结构迁移不完整，可能会出现下面这种导入错误：

```text
null value in column "id" of relation "pois" violates not-null constraint
```

这表示：

- `pois` 表存在
- `id` 列存在
- 但 `id` 列没有绑定默认的 `nextval('pois_id_seq')`

先检查表结构：

```bash
PGPASSWORD='你的数据库密码' psql -h 127.0.0.1 -U place_app -d place_explore -c "\d pois"
```

如果 `id` 列没有类似下面的默认值：

```text
default nextval('pois_id_seq'::regclass)
```

则执行修复：

```bash
PGPASSWORD='你的数据库密码' psql -h 127.0.0.1 -U place_app -d place_explore -c "CREATE SEQUENCE IF NOT EXISTS pois_id_seq START 1;"
PGPASSWORD='你的数据库密码' psql -h 127.0.0.1 -U place_app -d place_explore -c "ALTER TABLE pois ALTER COLUMN id SET DEFAULT nextval('pois_id_seq');"
PGPASSWORD='你的数据库密码' psql -h 127.0.0.1 -U place_app -d place_explore -c "SELECT setval('pois_id_seq', COALESCE((SELECT MAX(id) FROM pois), 0) + 1, false);"
```

作用说明：

- `CREATE SEQUENCE IF NOT EXISTS`：创建缺失的序列
- `ALTER TABLE ... SET DEFAULT nextval(...)`：让 `id` 列恢复自动递增
- `setval(...)`：让序列值与现有数据对齐，避免后续插入主键冲突

---

## 7. 后端部署

本节分本机打包和服务器部署两部分。

### 7.1 本机打包后端

在本机 PowerShell 执行：

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-backend
mvn -v
```

如果 Maven 可用：

```powershell
mvn clean package -DskipTests
```

如果本机 Maven 不可用，可尝试：

```powershell
.\mvnw.cmd clean package -DskipTests
```

作用说明：

- `mvn -v`：检查 Maven 是否可用
- `package -DskipTests`：打包后端 JAR，跳过测试

打包成功后，到 `target` 目录找到 JAR 文件。

### 7.2 上传后端 JAR

建议将本机打包好的 JAR 上传到服务器：

```text
/opt/place-explore/backend/
```

上传后统一重命名为：

```text
app.jar
```

如果上传后文件名不是 `app.jar`，可在服务器执行：

```bash
mv /opt/place-explore/backend/smart-campus-backend-0.0.1-SNAPSHOT.jar /opt/place-explore/backend/app.jar
```

再设置权限：

```bash
chown placeapp:placeapp /opt/place-explore/backend/app.jar
chmod 644 /opt/place-explore/backend/app.jar
ls -lh /opt/place-explore/backend/app.jar
```

作用说明：

- `mv`：统一后端产物名称，便于 systemd 管理
- `chown`：设置归属用户
- `chmod 644`：设置可读权限

### 7.3 编写生产配置文件

创建生产配置文件：

```bash
nano /opt/place-explore/backend/config/application-prod.properties
```

推荐内容：

```properties
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/place_explore
spring.datasource.username=place_app
spring.datasource.password=你的数据库密码

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

server.forward-headers-strategy=framework
```

作用说明：

- 定义生产环境数据库连接
- 关闭 SQL 打印
- 设置上传大小
- 支持反向代理头处理

### 7.4 编写 systemd 服务文件

创建服务文件：

```bash
nano /etc/systemd/system/place-explore-backend.service
```

推荐内容：

```ini
[Unit]
Description=Place Explore Backend
After=network.target postgresql.service

[Service]
User=placeapp
Group=placeapp
WorkingDirectory=/opt/place-explore/backend

Environment=SPRING_PROFILES_ACTIVE=prod
Environment="SPRING_DATASOURCE_URL=jdbc:postgresql://127.0.0.1:5432/place_explore"
Environment="SPRING_DATASOURCE_USERNAME=place_app"
Environment="SPRING_DATASOURCE_PASSWORD=你的数据库密码"
Environment=JWT_SECRET=你的JWT_SECRET
Environment=SUPER_ADMIN_USERNAME=你的管理员账号
Environment=SUPER_ADMIN_PASSWORD=你的管理员密码
Environment=AMAP_WEB_KEY=你的高德Web服务Key

Environment=APP_UPLOAD_AVATAR_DIR=/opt/place-explore/shared/uploads/avatars
Environment=APP_UPLOAD_POI_SHARE_DIR=/opt/place-explore/shared/uploads/poi-shares
Environment=APP_UPLOAD_ANNOUNCEMENT_DIR=/opt/place-explore/shared/uploads/announcements
Environment=APP_UPLOAD_ACTIVITY_DIR=/opt/place-explore/shared/uploads/activities
Environment=APP_UPLOAD_ROUTE_DIR=/opt/place-explore/shared/uploads/routes

ExecStart=/usr/bin/java -jar /opt/place-explore/backend/app.jar --spring.config.additional-location=file:/opt/place-explore/backend/config/
SuccessExitStatus=143
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

作用说明：

- 使用 systemd 守护后端服务
- 环境变量写入生产敏感配置
- 上传目录指定到独立磁盘路径
- 后端异常退出后自动重启

### 7.4.1 `systemd` 服务文件格式注意事项

`systemd` 服务文件对格式要求非常严格，下面这种写法是错误的：

```ini
[Unit] Description=Place Explore Backend
```

正确写法必须是：

```ini
[Unit]
Description=Place Explore Backend
```

也就是：

- `[Unit]` 必须单独一行
- `[Service]` 必须单独一行
- `[Install]` 必须单独一行
- 每一个 `Environment=...` 单独占一行

如果写错，常见报错是：

```text
Invalid section header '[Unit] Description=...'
Bad message
```

修复后一定要执行：

```bash
systemctl daemon-reload
systemctl status place-explore-backend --no-pager -l
```

### 7.4.2 为什么数据库连接信息最终建议写进 `systemd`

虽然 `application-prod.properties` 可以写数据库连接信息，但在实际部署中，如果配置文件路径、读取顺序、权限或转义处理出现问题，最容易导致：

```text
The server requested SCRAM-based authentication, but no password was provided
```

为了减少这类问题，最终更推荐把下面三个值显式写进 `systemd`：

```ini
Environment="SPRING_DATASOURCE_URL=jdbc:postgresql://127.0.0.1:5432/place_explore"
Environment="SPRING_DATASOURCE_USERNAME=place_app"
Environment="SPRING_DATASOURCE_PASSWORD=你的数据库密码"
```

这样做的好处：

- 启动链路更直接
- 更方便排查
- 避免配置文件未被正确加载时后端启动失败

### 7.5 启动后端服务

```bash
systemctl daemon-reload
systemctl enable place-explore-backend
systemctl start place-explore-backend
systemctl status place-explore-backend --no-pager -l
```

作用说明：

- `daemon-reload`：重新加载 systemd 配置
- `enable`：设置开机启动
- `start`：启动服务
- `status`：检查是否成功运行

### 7.6 查看后端日志

```bash
journalctl -u place-explore-backend -n 100 --no-pager -l
```

作用说明：

- 查看最近 100 行后端日志
- 用于排查启动失败、数据库认证失败、配置文件问题等

### 7.7 验证后端接口

```bash
curl http://127.0.0.1:8080/api/pois
```

作用说明：

- 直接请求本机 Spring Boot 接口
- 验证后端是否已在 8080 监听并正常返回 JSON

成功表现：

- 返回 JSON
- 即使是空列表也表示服务正常

注意：

- 刚执行 `systemctl start place-explore-backend` 后，Spring Boot 通常需要 10 到 25 秒完成启动
- 如果你过早执行 `curl http://127.0.0.1:8080/api/pois`，可能会得到：

```text
curl: (7) Failed to connect to 127.0.0.1 port 8080: Connection refused
```

这不一定表示部署失败，可能只是服务还没完全启动好。应先确认：

```bash
systemctl status place-explore-backend --no-pager -l
ss -ltnp | grep 8080
```

---

## 8. Nginx 配置

本节所有命令均在服务器终端执行。

### 8.1 删除默认站点

```bash
rm -f /etc/nginx/sites-enabled/default
```

作用说明：

- 移除默认欢迎页配置，避免与项目站点冲突

### 8.2 创建站点配置

首次可用服务器公网 IP 调试，正式绑定域名后再改 `server_name`。

创建站点配置：

```bash
nano /etc/nginx/sites-available/place-explore
```

正式可用的参考配置：

```nginx
server {
    listen 80;
    listen [::]:80;

    server_name map.jieai.shop;

    root /var/www/place-explore/current;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass http://127.0.0.1:8080/uploads/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

关键说明：

- `/`：提供前端静态资源
- `try_files ... /index.html`：保证前端路由刷新不 404
- `/api/`：反代到 Spring Boot
- `/uploads/`：反代到后端暴露的上传文件路径

### 8.3 启用站点

```bash
ln -s /etc/nginx/sites-available/place-explore /etc/nginx/sites-enabled/place-explore
```

作用说明：

- 创建启用站点的软链接

### 8.4 检查并重载 Nginx

```bash
nginx -t
systemctl reload nginx
```

作用说明：

- `nginx -t`：检查配置语法
- `systemctl reload nginx`：平滑重载配置

### 8.5 验证 API 与 uploads

```bash
curl http://127.0.0.1/api/pois
```

作用说明：

- 通过 Nginx 访问 `/api`，验证反向代理是否成功

如果已经有真实上传文件，可进一步测试：

```bash
curl -I https://map.jieai.shop/uploads/avatars/某个真实文件名.png
```

作用说明：

- 检查 `/uploads` 文件访问是否正常

---

## 9. 域名与 Cloudflare 配置

### 9.1 为什么使用二级域名

由于主域名 `jieai.shop` 可能用于多个项目，推荐为本项目分配单独二级域名，例如：

- `map.jieai.shop`

### 9.2 在 Cloudflare 添加 DNS 记录

在 Cloudflare 控制台：

1. 进入 `jieai.shop`
2. 打开 `DNS`
3. 点击 `Add record`
4. 添加：
   - `Type`: `A`
   - `Name`: `map`
   - `IPv4 address`: 服务器公网 IP
   - `Proxy status`: 初期建议 `DNS only`
   - `TTL`: `Auto`

### 9.3 为什么先使用 DNS only

调试阶段先使用灰云 `DNS only` 的原因：

- 先确认源站 Nginx 是否配置正确
- 排除 Cloudflare 代理层对排障的影响
- 更容易申请和验证源站 HTTPS

### 9.3.1 本项目实际推荐的 Cloudflare 切换顺序

结合本项目的真实部署过程，建议按下面顺序切换：

1. `DNS only`：先确认 Nginx、前端、后端、HTTPS 都正常
2. 源站证书配置完成后，再切换为 `Proxied`（橙云）
3. 切为橙云后，再去 Cloudflare 中开启额外防护

不建议一开始就直接开橙云，原因是：

- HTTPS 签发和源站调试更复杂
- 排查到底是 Cloudflare 问题还是源站问题会更困难

### 9.3.2 如何判断当前是不是橙云

如果你在本机执行：

```powershell
nslookup map.jieai.shop 1.1.1.1
```

返回的是类似下面的地址：

```text
104.21.x.x
172.67.x.x
2606:4700:....
```

通常表示当前流量正在经过 Cloudflare 代理，也就是橙云状态。

如果返回的是你服务器自己的公网 IP，则通常表示当前是灰云 `DNS only`。

### 9.4 本机验证 DNS 解析

在本机 PowerShell 执行：

```powershell
nslookup map.jieai.shop 1.1.1.1
```

作用说明：

- 使用 Cloudflare 公共 DNS 查询域名解析结果
- 确认子域名是否已指向你的服务器

### 9.5 绑定域名后的 Nginx 验证

访问：

```text
http://map.jieai.shop
http://map.jieai.shop/login
```

验证项：

- 首页正常
- `/login` 正常
- 刷新页面不 404
- API 请求正常

---

## 10. HTTPS 配置

### 10.1 申请证书前检查

确保：

- `map.jieai.shop` 已解析到服务器
- Nginx 已能通过 HTTP 正常访问该域名
- `nginx -t` 检查通过

### 10.2 使用 Certbot 申请证书

```bash
certbot --nginx -d map.jieai.shop
```

作用说明：

- 自动申请 Let’s Encrypt 证书
- 自动修改 Nginx 配置
- 可选择自动跳转 HTTP 到 HTTPS

推荐在提示中选择：

- 邮箱：填写你自己的邮箱
- 同意协议：同意
- 跳转 HTTPS：选择自动跳转

### 10.3 检查并重载 Nginx

```bash
nginx -t
systemctl reload nginx
```

### 10.4 测试证书自动续期

```bash
systemctl status certbot.timer --no-pager
certbot renew --dry-run
```

作用说明：

- 查看自动续期定时器是否启用
- 通过模拟续期确认未来续期是否可用

### 10.5 HTTPS 验证

浏览器验证：

- `https://map.jieai.shop`
- `https://map.jieai.shop/login`
- `http://map.jieai.shop` 是否自动跳转到 HTTPS

### 10.6 Cloudflare SSL 模式建议

在源站证书和 Nginx HTTPS 都正常后，Cloudflare 中推荐设置：

- `SSL/TLS` -> `Overview` -> `Full (strict)`

不建议使用：

- `Flexible`

原因：

- `Full (strict)` 会同时校验 Cloudflare 与源站之间的 HTTPS
- 安全性更高
- 更符合正式生产环境使用方式

### 10.7 切到橙云后的验证项

当你把 `map.jieai.shop` 从灰云切成橙云后，建议立即验证：

- `https://map.jieai.shop`
- `https://map.jieai.shop/login`
- `https://map.jieai.shop/api/pois`
- 页面静态资源是否正常加载
- 登录/注册请求是否正常

如果切成橙云后访问异常，应优先检查：

- Cloudflare SSL 模式是否正确
- 源站证书是否仍然有效
- Nginx 是否按域名正常工作

---

## 11. 上线后的安全加固

### 11.1 轮换敏感信息

建议在项目全部跑通后立即轮换：

- PostgreSQL 密码
- `JWT_SECRET`
- 超级管理员密码

### 11.2 重置 PostgreSQL 密码

```bash
sudo -u postgres psql
```

```sql
ALTER USER place_app WITH PASSWORD '你的新密码';
\q
```

然后验证：

```bash
PGPASSWORD='你的新密码' psql -h 127.0.0.1 -U place_app -d place_explore -c '\conninfo'
```

### 11.3 生成新的 JWT_SECRET

```bash
openssl rand -hex 48
```

作用说明：

- 生成足够长的随机 JWT 密钥

### 11.4 修改后端服务中的敏感环境变量

修改文件：

```text
/etc/systemd/system/place-explore-backend.service
```

修改后重载：

```bash
systemctl daemon-reload
systemctl restart place-explore-backend
```

### 11.5 Cloudflare 橙云

在站点稳定后，建议将子域名从 `DNS only` 切换为 `Proxied`（橙云）。

橙云的价值：

- 隐藏源站真实 IP 的 DNS 暴露
- 获得 Cloudflare 基础防护和代理能力
- 可进一步启用 WAF、限速、挑战页

### 11.6 Cloudflare SSL 模式

如果源站证书已正常配置，建议将 Cloudflare `SSL/TLS` 模式设置为：

- `Full (strict)`

不建议使用：

- `Flexible`

### 11.7 服务器防火墙建议

后续建议进一步收紧：

- 22 端口仅允许自己的固定 IP
- 80/443 仅允许 Cloudflare 回源 IP 段
- 5432 不对公网开放

---

## 12. 上线后的验证清单

以下项目全部通过，才算真正上线完成：

- 前端首页可访问
- `/login` 页面可访问且刷新不 404
- `https://map.jieai.shop` 可访问
- HTTP 会自动跳转到 HTTPS
- `/api/pois` 返回 JSON
- `/uploads` 文件可访问
- 高德地图正常显示
- 超级管理员可登录后台
- 后端服务为 `active (running)`
- PostgreSQL 可正常连接
- Nginx 配置检查通过
- 证书续期模拟成功

---

## 13. 日常维护与更新发布

### 13.1 更新前端

本机重新构建：

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-frontend
npm run build
Compress-Archive -Path .\dist\* -DestinationPath .\frontend-dist.zip -Force
```

上传新版本到新的发布目录，例如：

```bash
mkdir -p /var/www/place-explore/releases/frontend-20260403
```

上传并解压后切换软链接：

```bash
rm -rf /var/www/place-explore/current
ln -s /var/www/place-explore/releases/frontend-20260403 /var/www/place-explore/current
systemctl reload nginx
```

### 13.2 更新后端

本机重新打包：

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-backend
mvn clean package -DskipTests
```

上传新的 JAR 并覆盖：

```bash
systemctl stop place-explore-backend
```

使用 FinalShell 上传新的 `app.jar` 到：

```text
/opt/place-explore/backend/app.jar
```

再重启：

```bash
chown placeapp:placeapp /opt/place-explore/backend/app.jar
chmod 644 /opt/place-explore/backend/app.jar
systemctl start place-explore-backend
systemctl status place-explore-backend --no-pager -l
```

### 13.3 查看后端日志

```bash
journalctl -u place-explore-backend -n 100 --no-pager -l
journalctl -u place-explore-backend -f
```

作用说明：

- 第一条查看最近日志
- 第二条实时跟踪日志

### 13.4 查看 Nginx 状态

```bash
systemctl status nginx --no-pager
nginx -t
```

### 13.5 备份数据库

```bash
pg_dump -U place_app -h 127.0.0.1 place_explore > place_explore_backup.sql
```

作用说明：

- 导出数据库为 SQL 文件

### 13.6 备份上传文件

```bash
tar -czf uploads_backup.tar.gz /opt/place-explore/shared/uploads
```

作用说明：

- 将上传目录打包备份

### 13.7 导入全国 4A/5A 景区数据

本项目实际已经完成过一批全国景区数据导入，导入源文件位于项目目录：

```text
data/poi-imports/national_4a5a_pois_postgresql_import.sql
```

导入前建议先确认当前 `pois` 表记录数：

```bash
PGPASSWORD='你的数据库密码' psql -h 127.0.0.1 -U place_app -d place_explore -c "select count(*) from pois;"
```

如果 `pois` 表为空，可直接导入：

```bash
PGPASSWORD='你的数据库密码' psql -v ON_ERROR_STOP=1 -h 127.0.0.1 -U place_app -d place_explore -f /opt/place-explore/data-imports/national_4a5a_pois_postgresql_import.sql
```

导入前建议先上传该 SQL 文件到服务器目录：

```text
/opt/place-explore/data-imports/
```

并先做一个表级备份：

```bash
PGPASSWORD='你的数据库密码' pg_dump -h 127.0.0.1 -U place_app -d place_explore -t pois > /opt/place-explore/data-imports/pois_backup_before_import.sql
```

导入后验证：

```bash
PGPASSWORD='你的数据库密码' psql -h 127.0.0.1 -U place_app -d place_explore -c "select count(*) from pois;"
PGPASSWORD='你的数据库密码' psql -h 127.0.0.1 -U place_app -d place_explore -c "select id, name, category from pois order by id limit 5;"
curl http://127.0.0.1:8080/api/pois
```

说明：

- 这批数据已经按当前项目结构整理成 `name/category/description/latitude/longitude`
- 当前分类统一为 `景点景区`
- 当前可导入记录约 4800 条，适合作为全国景区第一批基础数据

---

## 14. 常用命令速查

### 14.1 Nginx

```bash
nginx -t
```

作用：检查 Nginx 配置语法。

```bash
systemctl reload nginx
```

作用：平滑重载 Nginx 配置。

```bash
systemctl restart nginx
```

作用：重启 Nginx 服务。

### 14.2 PostgreSQL

```bash
sudo -u postgres psql
```

作用：进入 PostgreSQL 管理终端。

```bash
PGPASSWORD='密码' psql -h 127.0.0.1 -U place_app -d place_explore -c '\conninfo'
```

作用：测试数据库账号密码。

### 14.3 后端服务

```bash
systemctl status place-explore-backend --no-pager -l
```

作用：查看后端服务完整状态。

```bash
systemctl restart place-explore-backend
```

作用：重启后端服务。

```bash
journalctl -u place-explore-backend -n 100 --no-pager -l
```

作用：查看后端最近日志。

### 14.4 网络排查

```bash
ss -ltnp | grep 8080
```

作用：检查 8080 是否已被后端监听。

```bash
curl http://127.0.0.1:8080/api/pois
```

作用：直接测试后端接口。

```bash
curl http://127.0.0.1/api/pois
```

作用：测试 Nginx 是否正确反代到后端。

```powershell
nslookup map.jieai.shop 1.1.1.1
```

作用：检查域名解析是否已生效。

---

## 15. 常见问题排查

### 15.1 `Unable to access jarfile`

原因：

- JAR 没上传
- JAR 文件名不对
- systemd 配置指向了错误路径

处理：

- 确认 `/opt/place-explore/backend/app.jar` 存在

### 15.2 数据库认证失败

典型错误：

- `The server requested SCRAM-based authentication, but no password was provided`

原因：

- 数据库密码没写进后端配置或 systemd 环境变量

处理：

- 检查 `application-prod.properties`
- 检查 `SPRING_DATASOURCE_PASSWORD`

### 15.2.1 导入 POI 数据时报 `id` 非空约束错误

典型错误：

```text
null value in column "id" of relation "pois" violates not-null constraint
```

原因：

- `pois.id` 没有默认 sequence

处理：

- 按本文档 `6.5` 小节修复 `pois_id_seq`
- 修复后重新导入

### 15.2.2 导入 SQL 最后只看到 `ROLLBACK`

如果看到：

```text
ERROR: current transaction is aborted, commands ignored until end of transaction block
ROLLBACK
```

这不是第一条错误，而是说明前面某一条 SQL 已经先失败。

应使用下面命令重新执行并定位第一条错误：

```bash
PGPASSWORD='你的数据库密码' psql -v ON_ERROR_STOP=1 -h 127.0.0.1 -U place_app -d place_explore -f /opt/place-explore/data-imports/national_4a5a_pois_postgresql_import.sql 2>&1 | tee /opt/place-explore/data-imports/poi_import_error.log
```

然后查看日志：

```bash
tail -n 40 /opt/place-explore/data-imports/poi_import_error.log
```

### 15.3 前端刷新路由 404

原因：

- Nginx 未配置 `try_files ... /index.html`

处理：

- 检查前端站点 `location /`

### 15.4 域名访问不到站点

原因：

- Cloudflare DNS 未生效
- 解析到了错误 IP
- Nginx `server_name` 未配置对应域名

处理：

- `nslookup`
- 检查 Cloudflare DNS 记录
- 检查 Nginx 配置

### 15.5 HTTPS 申请失败

原因：

- 域名未正确解析到服务器
- 80 端口无法访问
- Cloudflare 代理状态影响回源验证

处理：

- 先切 `DNS only`
- 确认 HTTP 可访问后再执行 Certbot

---

## 16. 结语

如果按本文档顺序完成全部步骤，则本项目的完整上线状态应包括：

- 前端静态资源正常
- 后端接口正常
- 上传资源正常
- PostgreSQL 正常
- Nginx 正常
- 域名正常
- HTTPS 正常
- Cloudflare 可用
- 敏感信息已轮换

建议后续再补充两类内容：

- 业务初始化数据导入指南
- 数据库与上传文件定时备份方案
