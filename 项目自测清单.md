# 项目自测清单

这份清单用于你后续继续迭代时做手工回归，不偏答辩，偏实际开发自测。

## 1. 登录与权限

- 未登录访问 `/admin` 是否会跳转登录页
- 普通用户登录后是否无法进入运营后台
- 超级管理员登录后是否可以访问全部后台页面
- token 失效后是否提示“登录状态已失效，请重新登录”

## 2. 地图与地点

- 首页地图是否能正常加载
- 点击地点后是否弹出地点详情
- 地点详情中的经纬度、分类、描述是否正常显示
- 地点详情中的打卡面板是否正常打开
- 点击地点后是否不再出现闪烁的输入光标

## 3. 地点打卡

- 登录用户是否可以发布分享
- 上传图片是否正常
- 分享发布后是否立即显示在地点打卡列表中
- 回复、点赞、删除、举报是否都能正常使用
- 空状态和未登录提示文案是否正常

## 4. 公告

- 后台新建草稿公告是否成功
- 发布公告后首页左侧公告栏是否可见
- 置顶公告是否排在前面
- 公告详情弹窗是否可正常关闭
- 左侧公告栏折叠后是否只保留展开按钮

## 5. 推荐分享

- 后台添加推荐分享后首页是否出现卡片
- 推荐排序调整后前台顺序是否正确
- 推荐语是否正确显示
- 点击推荐卡片后是否联动到地图并打开对应地点

## 6. 活动

- 后台新增活动并发布后首页是否出现
- 活动时间、地点和封面图是否显示正确
- 活动详情弹窗是否能正常打开
- “在地图中查看地点”是否能联动成功

## 7. 路线

- 后台创建路线并配置途经点是否成功
- 发布路线后首页是否出现精选路线卡片
- 点击路线是否能在地图中生成整条路线
- 起点、终点、途经点标记是否正常
- 路线详情里点击某个途经点是否能跳转到对应地点

## 8. 后台内容管理

- 分享管理列表、筛选、详情、删除是否正常
- 回复管理列表、筛选、删除是否正常
- 举报审核列表、详情、处理、驳回是否正常
- 用户管理中的角色切换、状态切换是否正常

## 9. 后台运营模块

- 平台公告的新增、编辑、发布、置顶、删除是否正常
- 活动管理的新增、编辑、发布、删除是否正常
- 推荐内容的新增、编辑、取消推荐是否正常
- 路线管理的新增、编辑、发布、删除是否正常

## 10. 后台支撑模块

- 系统日志是否能记录关键管理动作
- 文件资源页是否能识别正常资源、残留文件、文件缺失
- 删除文件资源后业务引用是否同步更新
- 系统配置修改后前台对应展示是否生效

## 11. 文案与展示

- 页面标题是否统一为“地点探索”
- 首页是否还残留“智慧校园”“校园地图”“校园公告”等旧文案
- 后台页面是否还残留乱码
- 中文提示、空状态、按钮文案是否统一

## 12. 构建与运行

- 前端 `npm run build` 是否通过
- 后端 `mvnw.cmd -q -DskipTests package` 是否通过
- 前后端同时启动后关键页面是否可访问

## 建议使用方式

每次你完成一轮模块开发后，至少回归：

- 登录与权限
- 地图与地点
- 地点打卡
- 你本次改动相关模块
- 前端构建
- 后端打包
