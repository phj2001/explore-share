# POI打卡功能实施方案

## 1. 目标

在现有 POI 详情能力中，新增一个独立的“打卡”模块，用于表达“我到过这里”。

第一版目标只包含：

- 用户可对某个 POI 执行打卡
- 用户可取消自己对某个 POI 的打卡
- POI 详情可显示当前用户是否已打卡
- POI 详情可显示该 POI 的总打卡人数

这套能力必须满足：

- 不影响现有 POI 分享、评论、点赞、回复功能
- 不改变现有 POI 查询、地图渲染、后台管理主链路
- 前后端都采用独立模块接入，避免和现有分享系统耦合

---

## 2. 设计原则

### 2.1 打卡是独立功能，不并入评论系统

打卡与“发布分享/评论”不是同一类行为：

- 打卡：轻互动，门槛低
- 分享/评论：内容创作，门槛高

因此不建议：

- 通过“发一条评论”替代打卡
- 把“打卡”作为分享类型的一种
- 要求用户必须填写内容才能打卡

### 2.2 一人一地一条打卡记录

同一用户对同一 POI 只允许存在一条有效打卡记录。

这样可以：

- 简化交互
- 避免重复刷数据
- 便于统计总打卡人数
- 便于后续做热度排行

### 2.3 第一版只做最小闭环

第一版不做：

- 打卡排行榜
- 连续打卡天数
- 勋章系统
- 地理位置校验
- 打卡照片强绑定
- 打卡动态流

这些都可以在第一版稳定后再扩展。

---

## 3. 功能范围

### 3.1 用户端

POI 详情弹窗中新增：

- `打卡 / 已打卡` 按钮
- `xxx 人打卡` 统计展示

交互逻辑：

- 未登录用户点击打卡：提示先登录
- 已登录且未打卡：点击后创建打卡记录
- 已登录且已打卡：点击后可取消打卡

### 3.2 后端接口

至少提供以下接口：

1. `POST /api/pois/{poiId}/check-in`
   - 作用：当前登录用户对指定 POI 打卡

2. `DELETE /api/pois/{poiId}/check-in`
   - 作用：当前登录用户取消打卡

3. `GET /api/pois/{poiId}/check-in/status`
   - 作用：获取当前用户对该 POI 的打卡状态，以及总打卡人数

第一版不强制要求：

4. `GET /api/pois/{poiId}/check-ins`
   - 可选，后续用于最近打卡用户展示

---

## 4. 数据库设计

建议新增一张独立表：

`poi_check_ins`

### 4.1 建议字段

- `id`
- `poi_id`
- `user_id`
- `created_at`

### 4.2 约束要求

- `poi_id` 外键关联 `pois`
- `user_id` 外键关联 `users`
- 唯一约束：`(poi_id, user_id)`

### 4.3 索引建议

至少增加：

- `unique(poi_id, user_id)`
- `index(user_id)`
- `index(poi_id)`
- 可选：`index(created_at desc)`

### 4.4 实体建议

新增实体：

- `POICheckIn`

字段建议：

- `Long id`
- `POI poi`
- `User user`
- `LocalDateTime createdAt`

---

## 5. 后端实现方案

### 5.1 新增层次

建议新增这些文件：

- `entity/POICheckIn.java`
- `repository/POICheckInRepository.java`
- `service/POICheckInService.java`
- `service/impl/POICheckInServiceImpl.java`
- `controller/POICheckInController.java`
- `dto/response/POICheckInStatusResponse.java`

### 5.2 Repository 能力

建议至少支持：

- `existsByPoiIdAndUserId(...)`
- `countByPoiId(...)`
- `findByPoiIdAndUserId(...)`
- `deleteByPoiIdAndUserId(...)`

### 5.3 Service 行为

建议提供方法：

- `checkIn(Long poiId, Long userId)`
- `cancelCheckIn(Long poiId, Long userId)`
- `getStatus(Long poiId, Long userId)`

### 5.4 返回结构

建议统一状态响应：

```json
{
  "checkedIn": true,
  "checkInCount": 123
}
```

这样前端在点击后可以直接刷新按钮状态和人数。

### 5.5 鉴权要求

- 打卡和取消打卡接口必须登录
- 状态接口可以分两种：
  - 已登录：返回真实 `checkedIn`
  - 未登录：返回 `checkedIn=false`，但仍返回 `checkInCount`

如果当前鉴权实现对“可选登录态”支持不方便，第一版也可以拆成：

- 公开接口返回总人数
- 登录接口返回我的打卡状态

但更推荐合并为一个状态接口。

---

## 6. 前端实现方案

### 6.1 接入位置

建议接在现有 POI 详情弹窗中，也就是：

- `MapContainer.vue` 中的 POI 详情区域

这样不会影响地图点位渲染逻辑，只是在详情面板增加一个轻交互模块。

### 6.2 UI 组成

建议新增一个独立的小区块：

- 主按钮：`打卡` / `已打卡`
- 辅助文案：`123 人打卡`

推荐交互：

- 未打卡时按钮为主色
- 已打卡时按钮为淡色或带勾状态
- 点击时显示 loading

### 6.3 前端 API

建议新增：

- `getPOICheckInStatus(poiId)`
- `checkInPOI(poiId)`
- `cancelCheckInPOI(poiId)`

放到：

- `src/api/poiCheckIn.js`

### 6.4 前端状态管理

第一版不建议进入全局 store。

原因：

- 打卡状态是“当前 POI 详情级别”的局部状态
- 没必要为这个功能新增复杂全局 store

建议在 POI 详情组件内维护：

- `checkInStatus`
- `checkInLoading`

切换不同 POI 时重新加载该 POI 的状态即可。

---

## 7. 与现有功能的隔离策略

为了不影响现有模块，必须遵守以下隔离要求：

### 7.1 不修改现有 POI 分享表结构

不要把打卡字段塞进：

- `poi_shares`
- `poi_share_replies`
- 点赞表

### 7.2 不改变现有评论/分享接口返回结构

打卡是独立功能，不要把现有分享链路一起重构，否则风险太高。

### 7.3 不把打卡状态混入地图点位接口

地图上的 POI 点位数据已经做了性能优化，不应为了打卡而扩大 `/api/pois/bounds` 返回体。

建议：

- 地图仍只负责点位展示
- 打卡状态仅在 POI 详情弹窗打开后再单独请求

### 7.4 不影响后台管理链路

第一版不必给后台加“打卡管理页”。

理由：

- 先验证用户端价值
- 后台管理不是当前最紧急场景
- 减少改动面

---

## 8. 推荐实施步骤

### 第一步：后端数据层

1. 新建 `poi_check_ins` 实体和表
2. 加唯一约束和索引
3. 新建 repository

### 第二步：后端服务层

1. 实现打卡
2. 实现取消打卡
3. 实现状态查询
4. 补接口单元逻辑校验

### 第三步：前端接入

1. 新增 `src/api/poiCheckIn.js`
2. 在 POI 详情弹窗增加打卡区域
3. 详情打开时加载状态
4. 点击按钮时更新状态

### 第四步：联调验证

验证以下场景：

- 未登录点打卡
- 已登录首次打卡
- 已打卡后取消打卡
- 多个用户打卡后人数统计是否正确
- 切换 POI 时状态是否正确刷新

---

## 9. 验收标准

第一版完成后，应满足：

- 用户可成功打卡
- 用户可成功取消打卡
- 同一用户不能重复打卡同一 POI
- POI 详情能正确显示打卡人数
- 不影响现有 POI 分享、评论、点赞、回复功能
- 不影响地图加载性能
- 不影响后台已有模块

---

## 10. 后续可扩展方向

第一版稳定后，可以继续扩展：

- 最近打卡用户列表
- 打卡时间展示
- 热门打卡地点排行
- 用户个人打卡记录页
- 用户足迹地图
- 打卡勋章/成就系统
- 地理位置校验打卡

但这些都应建立在第一版独立打卡模型稳定的前提下，而不是一开始就做复杂。

---

## 11. 风险与注意事项

### 11.1 不要让详情弹窗多发无用请求

打卡状态接口只在：

- 打开新的 POI 详情时请求
- 打卡/取消打卡后刷新

不要在地图拖动、点位刷新时同步拉取。

### 11.2 不要把打卡统计塞进边界查询

这会直接拉大地图主链路返回体，影响性能。

### 11.3 唯一约束必须存在

否则后端并发下可能出现重复打卡记录。

### 11.4 第一版不建议加复杂缓存

打卡人数和状态属于实时互动数据，第一版保持简单直接即可。

---

## 12. 最终建议

当前项目最适合的实现方式是：

- 打卡做独立表
- 打卡做独立接口
- 打卡只挂在 POI 详情弹窗中
- 地图主链路不感知打卡状态
- 第一版只做“打卡 / 取消打卡 / 人数展示 / 当前状态”

这是改动面最小、风险最低、收益最直接的方案。
