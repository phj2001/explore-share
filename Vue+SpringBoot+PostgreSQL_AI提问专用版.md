# Vue + Spring Boot + PostgreSQL AI 提问专用版

## 1. 最稳的总提示词
```text
我要做一个【XX系统】。
技术栈：前端 Vue3 + Vite + Element Plus，后端 Spring Boot + Spring Data JPA，数据库 PostgreSQL。
请按下面顺序输出，不要跳步：
1. 先梳理业务对象
2. 再设计数据库表
3. 再给接口清单
4. 再生成后端代码结构
5. 再生成前端代码结构
6. 最后给联调步骤和测试清单

要求：
- 目录清晰
- 分层明确
- 统一返回格式
- 包含 JWT 登录鉴权
- 适合中小型项目复用
```

## 2. 第一阶段：只做分析，不写代码
```text
我要做一个【XX系统】。
请先不要写代码。
先帮我完成：
1. 业务对象梳理
2. 数据库表设计
3. 表关系图
4. 接口清单
```

## 3. 第二阶段：只生成后端
```text
现在根据上面的设计，生成后端代码结构。
技术栈：Spring Boot + JPA + PostgreSQL。
请按 entity -> repository -> service -> controller 的顺序输出。
要求：
- 统一返回 Result
- 包含异常处理
- 包含 JWT 登录鉴权
- 命名统一
```

## 4. 第三阶段：只生成前端
```text
现在根据接口清单，生成前端代码结构。
技术栈：Vue3 + Vite + Element Plus。
请按 api -> store -> router -> views -> components 的顺序输出。
要求：
- 统一 axios 封装
- 页面职责清晰
- 列表页包含搜索、分页、新增、编辑、删除
- 登录态使用 token
```

## 5. 第四阶段：做联调与检查
```text
请帮我列出这个项目的联调步骤、测试点、常见报错和排查顺序。
重点检查：
- 请求地址
- 请求参数
- token
- 权限控制
- 返回结构
- 数据库落库
- 页面渲染
```

## 6. 新建一个业务模块时的 AI 提问模板
```text
现在我要在现有项目里新增【学生管理】模块。
请按下面顺序帮助我，不要跳步：
1. 先设计 student 表
2. 再给 student 接口清单
3. 再写后端 entity/repository/service/controller
4. 再写前端 api/store/view/component
5. 最后给联调步骤
要求：
- 不破坏现有结构
- 命名统一
- 保持和已有模块风格一致
```

## 7. 让 AI 帮你审查代码时的模板
```text
请用代码审查的方式检查我这个模块。
重点看：
1. 前后端接口是否对齐
2. 鉴权是否真正生效
3. 数据库字段映射是否正确
4. 是否存在重复逻辑
5. 是否有明显的维护性问题
请优先列出真实问题，不要只给泛泛建议。
```

## 8. 让 AI 帮你排查报错时的模板
```text
我这个项目现在报错了。
技术栈：Vue3 + Spring Boot + PostgreSQL。
请按下面顺序帮我排查：
1. 先判断错误发生在前端、后端还是数据库
2. 再列出最可能的 3 个原因
3. 再告诉我应该检查哪些文件
4. 再给出最小修复方案

报错信息如下：
【粘贴完整报错】
```

## 9. 让 AI 帮你重构时的模板
```text
请帮我重构这个模块，但不要改变业务行为。
目标：
- 结构更清晰
- 重复逻辑更少
- 前后端职责更清楚
- 保持现有接口不变
请先说明重构思路，再分步修改。
```

## 10. 你和 AI 协作时最重要的原则
- 不要一上来就让 AI 直接写整个项目
- 先让 AI 帮你梳理对象、表、接口
- 再分后端、前端两段生成
- 每一轮都让 AI 保持和现有结构一致
- 每次都要求“不要跳步”
- 每次都要求“先说明顺序，再输出内容”

## 11. 一句话记忆版
```text
先让 AI 帮我定业务、定表、定接口，再让它按后端和前端分层生成代码，最后让它帮我联调和审查。
```
