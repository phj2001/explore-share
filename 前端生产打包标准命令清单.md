# 前端生产打包标准命令清单

本文档用于避免在前端生产打包时，误把局域网地址、`localhost` 或临时测试环境变量打进正式生产包。

---

## 一、本次问题的根因总结

本次线上异常的根因是：

1. 之前做局域网真机测试时，在 PowerShell 会话里临时设置过：

   ```powershell
   $env:VITE_API_BASE_URL="http://192.168.1.215:8080/api"
   ```

2. 后续在同一个 PowerShell 窗口里直接执行了生产打包。
3. Vite 构建时，Shell 环境变量优先级高于 `.env.production`。
4. 虽然 `.env.production` 中写的是：

   ```env
   VITE_API_BASE_URL=/api
   ```

   但最终仍被临时环境变量覆盖。

5. 打出来的生产包里，API 地址被写成了：

   ```text
   http://192.168.1.215:8080/api
   ```

6. 页面部署到公网后，浏览器去请求局域网地址，自然无法连接，最终表现为：
   - 页面提示无法连接后端
   - 首页部分接口加载失败
   - 登录注册按钮行为异常

一句话总结：

**不是服务器后端挂了，而是前端生产包打错了 API 地址。**

---

## 二、生产打包前的基本原则

每次正式打包前端生产包时，遵守下面几条：

1. 使用一个新的 PowerShell 窗口，不要复用之前做过真机测试的终端。
2. 打包前先清理当前 Shell 中所有可能污染生产构建的 `VITE_*` 环境变量。
3. 打包前确认 `.env.production` 中的 `VITE_API_BASE_URL=/api`。
4. 打包后必须检查构建产物中是否还残留 `192.168.x.x` 或 `localhost`。
5. 检查通过后再压缩上传服务器。

---

## 三、标准生产打包步骤

### 1. 打开新的 PowerShell 窗口

不要使用之前执行过真机测试命令的 PowerShell 窗口。

### 2. 进入前端目录

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-frontend
```

### 3. 清理可能污染生产构建的环境变量

```powershell
Remove-Item Env:VITE_API_BASE_URL -ErrorAction SilentlyContinue
Remove-Item Env:VITE_AMAP_JS_KEY -ErrorAction SilentlyContinue
Remove-Item Env:VITE_AMAP_SECURITY_JS_CODE -ErrorAction SilentlyContinue
Remove-Item Env:VITE_COORDINATE_SOURCE -ErrorAction SilentlyContinue
```

说明：

- 这一步用于清理当前 Shell 会话中的临时环境变量
- 尤其是防止 `VITE_API_BASE_URL` 覆盖 `.env.production`

### 4. 检查当前 Shell 中这些变量是否已经为空

```powershell
echo $env:VITE_API_BASE_URL
echo $env:VITE_AMAP_JS_KEY
echo $env:VITE_AMAP_SECURITY_JS_CODE
echo $env:VITE_COORDINATE_SOURCE
```

正确预期：

- 输出应为空，或至少不能出现生产不该使用的值
- 绝不能再看到：

  ```text
  http://192.168.1.215:8080/api
  ```

### 5. 检查生产环境文件

```powershell
Get-Content .env.production
```

你至少应确认以下内容正确：

```env
VITE_API_BASE_URL=/api
VITE_AMAP_JS_KEY=你的高德JS_KEY
VITE_AMAP_SECURITY_JS_CODE=你的高德安全密钥
VITE_COORDINATE_SOURCE=wgs84
```

关键要求：

- `VITE_API_BASE_URL` 必须是 `/api`
- 不能写 `localhost`
- 不能写 `192.168.x.x`
- 不能写本机或内网地址

### 6. 正式打包

如果依赖没有变化：

```powershell
npm run build
```

如果本次改动了依赖：

```powershell
npm install
npm run build
```

### 7. 打包后检查产物中是否混入局域网地址

```powershell
Get-ChildItem .\dist\assets\index-*.js | Select-String -Pattern "192\.168\.|localhost:8080|VITE_API_BASE_URL|/api"
```

检查重点：

- 不应出现 `192.168.1.215`
- 不应出现 `localhost:8080`
- 出现 `/api` 是正常的

如果仍然查到局域网地址或 localhost，说明这次构建不能上线，必须先排查环境变量污染。

### 8. 压缩生产包

```powershell
Compress-Archive -Path .\dist\* -DestinationPath .\frontend-dist-20260405-02.zip -Force
Get-Item .\frontend-dist-20260405-02.zip
```

---

## 四、最短可执行版命令清单

以后每次正式打包前端生产包，可以直接执行下面这组命令：

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-frontend
Remove-Item Env:VITE_API_BASE_URL -ErrorAction SilentlyContinue
Remove-Item Env:VITE_AMAP_JS_KEY -ErrorAction SilentlyContinue
Remove-Item Env:VITE_AMAP_SECURITY_JS_CODE -ErrorAction SilentlyContinue
Remove-Item Env:VITE_COORDINATE_SOURCE -ErrorAction SilentlyContinue
Get-Content .env.production
npm run build
Get-ChildItem .\dist\assets\index-*.js | Select-String -Pattern "192\.168\.|localhost:8080"
Compress-Archive -Path .\dist\* -DestinationPath .\frontend-dist-20260405-02.zip -Force
```

判断标准：

- 如果 `Select-String` 那条命令没有任何输出，通常说明没有把局域网地址或 localhost 打进生产包。

---

## 五、上传服务器后的标准动作

服务器创建新的发布目录：

```bash
mkdir -p /var/www/place-explore/releases/frontend-20260405-02
```

将压缩包上传到：

```text
/var/www/place-explore/releases/frontend-20260405-02/
```

然后执行：

```bash
cd /var/www/place-explore/releases/frontend-20260405-02
unzip frontend-dist-20260405-02.zip
rm -f frontend-dist-20260405-02.zip
ln -sfn /var/www/place-explore/releases/frontend-20260405-02 /var/www/place-explore/current
readlink -f /var/www/place-explore/current
nginx -t
systemctl reload nginx
```

---

## 六、发布后验证建议

发布完成后建议立即做下面几件事：

1. Cloudflare 后台执行 `Purge Everything`
2. 用无痕窗口访问站点
3. 打开浏览器开发者工具，确认：
   - 没有请求 `192.168.x.x`
   - 没有请求 `localhost`
   - API 请求正常走 `/api`

---

## 七、以后如何避免再次踩坑

建议你把局域网真机测试和正式生产打包彻底分开：

1. 真机测试只在专门的临时 PowerShell 窗口中做
2. 测试结束后直接关闭该窗口
3. 正式生产打包永远重新开一个新窗口
4. 每次生产打包前，都先执行环境变量清理命令

这样基本就不会再把局域网地址误打进生产包。
