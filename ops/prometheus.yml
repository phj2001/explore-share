global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # 后端 Spring Boot Actuator + Micrometer Prometheus 端点（SecurityConfig 已放行匿名抓取）
  - job_name: smart-campus-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ['backend:8080']
