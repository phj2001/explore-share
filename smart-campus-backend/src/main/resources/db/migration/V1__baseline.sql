-- ============================================================
-- V1 · Flyway 基线标记（无实际 DDL）
-- ------------------------------------------------------------
-- 为什么是空迁移：
--   1. 既有库：全量表结构由 Hibernate（ddl-auto=update 时期）+ 手工脚本演进而来，
--      无法安全重放，故以 baseline-on-migrate（version=0）直接认领现状，V1 仅做标记。
--   2. 全新空库：首次启动用 DDL_AUTO=update 让 Hibernate 建全量表（见 application-prod.properties 注释），
--      扩展与 assistant_cache 由 db/init/01~03 脚本负责（docker 初始化卷）。
-- 自此之后的所有结构变更（加表/加列/加索引/数据订正）一律新增 V{n}__描述.sql，
-- 不再依赖 ddl-auto=update。
-- ============================================================
SELECT 1;
