# 局域网手机真机测试启动清单

## 1. 适用场景

本清单适用于以下开发测试环境：

- 前端项目：本地通过 `Vite` 启动
- 后端项目：通过 `IDEA` 直接运行
- 电脑未安装全局 Maven
- 手机和电脑处于同一局域网

当前电脑局域网 IP：

```text
192.168.1.215
```

本清单目标：

- 不修改项目源码
- 只通过临时启动方式，让手机浏览器可以访问本地项目
- 用于移动端真机验收和交互测试

---

## 2. 启动前检查

开始前请确认：

1. 手机和电脑连接的是同一个 Wi‑Fi
2. IDEA 可以正常启动本地后端
3. 前端目录可以正常执行 `npm run dev`
4. 后端本地端口为：

```text
8080
```

5. 前端本地端口为：

```text
5173
```

---

## 3. 后端启动方式（IDEA）

后端通过 IDEA 启动时，需要给运行配置增加环境变量，否则手机访问前端时会被后端跨域拦截。

### 3.1 在 IDEA 中打开运行配置

打开你的 Spring Boot 启动配置，找到：

```text
Environment variables
```

### 3.2 填入以下环境变量

```text
APP_SECURITY_ALLOWED_ORIGIN_PATTERNS=http://localhost:5173,http://127.0.0.1:5173,http://192.168.1.215:5173
```

### 3.3 作用说明

这项配置表示允许以下前端来源访问本地后端：

- `http://localhost:5173`
- `http://127.0.0.1:5173`
- `http://192.168.1.215:5173`

如果不加这一项：

- 本机浏览器可能仍可访问
- 但手机通过局域网访问前端后，请求后端时会被 CORS 拦截

### 3.4 启动后端

正常点击 IDEA 的运行按钮启动即可。

---

## 4. 前端启动方式（PowerShell）

打开 PowerShell，执行以下命令：

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-frontend
$env:VITE_API_BASE_URL="http://192.168.1.215:8080/api"
npm run dev -- --host 0.0.0.0
```

### 4.1 每条命令作用

#### 进入前端目录

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-frontend
```

作用：

- 进入前端项目目录

#### 设置本次启动的 API 地址

```powershell
$env:VITE_API_BASE_URL="http://192.168.1.215:8080/api"
```

作用：

- 让前端请求你的电脑后端
- 避免手机把 `localhost:8080` 误解析成手机自己

#### 用局域网方式启动 Vite

```powershell
npm run dev -- --host 0.0.0.0
```

作用：

- 让前端开发服务对局域网开放
- 不仅本机可访问，手机也可访问

---

## 5. 前端启动成功后的正确访问地址

当前端启动成功后，终端通常会看到类似：

```text
Local:   http://localhost:5173/
Network: http://192.168.1.215:5173/
```

手机浏览器要访问的是：

```text
http://192.168.1.215:5173
```

不是：

```text
http://localhost:5173
```

因为手机上的 `localhost` 指向的是手机自己，不是你的电脑。

---

## 6. 手机端访问步骤

1. 启动 IDEA 后端
2. 启动 PowerShell 前端
3. 确认两者都已成功运行
4. 在手机浏览器打开：

```text
http://192.168.1.215:5173
```

---

## 7. 推荐的真机测试页面

建议在手机浏览器重点测试这些页面：

- 首页
- 地图区域
- POI 详情
- 登录注册页
- 个人中心页
- 推荐分享
- 活动区块
- 推荐路线
- 公告抽屉/公告详情

---

## 8. 真机测试时重点检查什么

不要只看页面能不能打开，要按真实用户交互测试。

### 首页与地图

- 地图能否顺畅拖动
- 双指缩放是否正常
- 点击 POI 是否容易命中
- 详情弹层是否容易关闭
- 页面滚动与地图拖动是否冲突

### 登录注册

- 输入框是否会被软键盘遮挡
- 按钮是否容易点击
- 错误提示是否清楚

### 个人中心

- 表单是否单列自然
- 保存资料/更新密码按钮是否好点
- 是否存在横向滚动

### 内容区块

- 卡片是否单列显示
- 图片与标题是否清晰
- 按钮是否容易点击

---

## 9. 常见问题排查

## 9.1 手机打不开前端页面

先在电脑浏览器打开：

```text
http://192.168.1.215:5173
```

如果电脑自己都打不开，通常是：

- 前端没有用 `--host 0.0.0.0` 启动
- 启动命令没执行成功

---

## 9.2 手机能打开前端，但没有数据

通常是后端跨域没有放开，检查 IDEA 启动配置里是否已设置：

```text
APP_SECURITY_ALLOWED_ORIGIN_PATTERNS=http://localhost:5173,http://127.0.0.1:5173,http://192.168.1.215:5173
```

---

## 9.3 手机打不开后端接口

在电脑浏览器里测试：

```text
http://192.168.1.215:8080/api/pois/count
```

如果电脑自己都打不开，说明：

- 后端未启动成功
- 或后端未监听到本机网络访问

---

## 9.4 手机和电脑不在同一网络

表现通常是：

- 手机打不开 `192.168.1.215:5173`
- 或能打开很慢/不稳定

请确认：

- 电脑不是有线公司网
- 手机不是单独连了热点
- 两者都在同一台路由器下

---

## 10. 本次测试结束后如何恢复

本清单中的改动都是**临时启动参数**，不会修改项目源码。

测试结束后：

- 关闭 IDEA 运行中的后端
- 关闭 PowerShell 中运行的前端

下次重新测试时，再按本清单重新启动即可。

---

## 11. 一页版快速步骤

### IDEA 后端

运行配置环境变量：

```text
APP_SECURITY_ALLOWED_ORIGIN_PATTERNS=http://localhost:5173,http://127.0.0.1:5173,http://192.168.1.215:5173
```

然后正常启动后端。

### PowerShell 前端

```powershell
cd C:\Users\Administrator\Desktop\地点探索与分享\smart-campus-frontend
$env:VITE_API_BASE_URL="http://192.168.1.215:8080/api"
npm run dev -- --host 0.0.0.0
```

### 手机浏览器

打开：

```text
http://192.168.1.215:5173
```

