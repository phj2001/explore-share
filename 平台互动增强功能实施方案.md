# 平台互动增强功能实施方案

> 编制时间：2026-04-27
> 项目现状：地图浏览 + POI 打卡分享 + 运营后台已完整运行
> 核心问题：用户间无社交连接、用户对 POI 参与维度薄、缺乏游戏化驱动

---

## 方案总览

三个梯队，共 10 个功能模块。按"投入产出比"降序排列，建议逐梯队推进，每完成一个功能就打磨上线。

| 梯队 | 功能 | 核心价值 | 预计工作量 |
|------|------|----------|-----------|
| 一 | POI 收藏 | 给用户"回头路"，增加留存 | 中 |
| 一 | POI 评分与评价 | 补齐轻互动层，辅助决策 | 中 |
| 一 | 用户公开主页 | 建立社交身份，刺激互动 | 中 |
| 一 | 探索排行 | 游戏化驱动持续回访 | 中 |
| 二 | 关注 + 动态流 | 社交闭环，内容分发 | 大 |
| 二 | 用户自创路线 | UGC 扩展，差异化亮点 | 大 |
| 二 | 成就/徽章体系 | 长线游戏化，成就感 | 中 |
| 三 | POI 图片墙 | 视觉丰富度，降低分享门槛 | 小 |
| 三 | 标签/话题 | 内容聚合与发现 | 中 |
| 三 | 通知系统 | 闭环互动感知 | 大 |

---

## 第一梯队

### 1.1 POI 收藏

#### 1.1.1 功能描述

- 用户可在 POI 详情弹窗、地图标注等位置点击"收藏"
- 个人中心展示"我的收藏"列表（支持分页）
- POI 详情中显示该 POI 被收藏的总数
- 支持取消收藏

#### 1.1.2 数据库设计

新增 `poi_favorites` 表：

```sql
CREATE TABLE poi_favorites (
    id          BIGSERIAL PRIMARY KEY,
    poi_id      BIGINT NOT NULL REFERENCES pois(id) ON DELETE CASCADE,
    user_id     BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at  TIMESTAMP NOT NULL DEFAULT NOW(),
    CONSTRAINT uk_poi_favorites_poi_user UNIQUE (poi_id, user_id)
);

CREATE INDEX idx_poi_favorites_user_id ON poi_favorites(user_id);
CREATE INDEX idx_poi_favorites_poi_id ON poi_favorites(poi_id);
```

#### 1.1.3 后端实现

**新增 Entity**

`com.smartcampus.entity.POIFavorite`
- 字段：id, poi(ManyToOne), user(ManyToOne), createdAt
- 唯一约束：(poi_id, user_id)

**新增 Repository**

`com.smartcampus.repository.POIFavoriteRepository`
- `existsByPoiIdAndUserId(Long poiId, Long userId)`
- `deleteByPoiIdAndUserId(Long poiId, Long userId)`
- `countByPoiId(Long poiId)`
- `findByUserIdOrderByCreatedAtDesc(Long userId, Pageable)`
- `findPoiIdByUserId(Long userId)`

**新增 Service**

`com.smartcampus.service.POIFavoriteService` / `impl.POIFavoriteServiceImpl`
- `getFavoriteStatus(Long poiId, Long userId)` → { favorited, favoriteCount }
- `addFavorite(Long poiId, Long userId)` → 收藏
- `removeFavorite(Long poiId, Long userId)` → 取消收藏
- `getUserFavorites(Long userId, Pageable)` → 分页收藏列表（含 POI 摘要）

**新增 Controller**

`com.smartcampus.controller.POIFavoriteController`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/pois/{poiId}/favorite` | Optional Auth | 获取收藏状态和总数 |
| POST | `/api/pois/{poiId}/favorite` | Authenticated | 收藏 POI |
| DELETE | `/api/pois/{poiId}/favorite` | Authenticated | 取消收藏 |
| GET | `/api/users/me/favorites` | Authenticated | 我的收藏列表（分页） |

**修改 POI 相关响应**

`POIResponse` 新增字段：
- `favoriteCount` (Long) — 该 POI 被收藏的总人数
- `favoritedByCurrentUser` (Boolean) — 当前用户是否已收藏（登录时返回）

#### 1.1.4 前端实现

**新增 API 文件**

`src/api/poiFavorite.js`
- `getFavoriteStatus(poiId)`
- `addFavorite(poiId)`
- `removeFavorite(poiId)`
- `getUserFavorites(params)`

**修改 MapContainer.vue**

POI 详情弹窗中，打卡卡片下方增加"收藏"按钮：
- 未收藏：显示心形轮廓 + "收藏" + 收藏人数
- 已收藏：显示实心心形 + "已收藏" + 收藏人数
- 点击切换收藏/取消收藏

**修改 Settings.vue**

新增"我的收藏"标签页：
- 展示收藏的 POI 列表（卡片形式，含名称、分类、坐标）
- 支持分页加载
- 点击可跳转到地图定位到该 POI

#### 1.1.5 任务清单

- [x] 后端：新增 `POIFavorite` Entity
- [x] 后端：新增 `POIFavoriteRepository`
- [x] 后端：新增 `POIFavoriteService` + `POIFavoriteServiceImpl`
- [x] 后端：新增 `POIFavoriteController`
- [ ] 后端：修改 `POIResponse` 增加 favoriteCount / favoritedByCurrentUser
- [ ] 后端：修改 `POIServiceImpl` 在查询 POI 详情时附带收藏信息
- [x] 前端：新增 `src/api/poiFavorite.js`
- [x] 前端：修改 `MapContainer.vue` POI 详情弹窗增加收藏按钮
- [x] 前端：修改 `Settings.vue` 增加"我的收藏"区域
- [ ] 测试：收藏/取消收藏功能
- [ ] 测试：POI 详情中收藏状态和数量正确显示
- [ ] 测试：个人中心收藏列表分页加载

---

### 1.2 POI 评分与评价

#### 1.2.1 功能描述

- 用户可在 POI 详情中对 POI 打 1-5 星评分
- 可附文字评价（200 字以内）
- POI 详情中展示平均评分、评价总数、评价列表
- 地图上 POI 标注可显示评分标识
- 运营后台可管理评价（删除违规评价）

#### 1.2.2 数据库设计

新增 `poi_reviews` 表：

```sql
CREATE TABLE poi_reviews (
    id          BIGSERIAL PRIMARY KEY,
    poi_id      BIGINT NOT NULL REFERENCES pois(id) ON DELETE CASCADE,
    user_id     BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    rating      SMALLINT NOT NULL CHECK (rating BETWEEN 1 AND 5),
    content     VARCHAR(200),
    created_at  TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at  TIMESTAMP NOT NULL DEFAULT NOW(),
    CONSTRAINT uk_poi_reviews_poi_user UNIQUE (poi_id, user_id)
);

CREATE INDEX idx_poi_reviews_poi_id ON poi_reviews(poi_id);
CREATE INDEX idx_poi_reviews_user_id ON poi_reviews(user_id);
```

POI 聚合评分缓存（可选，也可实时计算）：

```sql
ALTER TABLE pois ADD COLUMN IF NOT EXISTS avg_rating DECIMAL(3,2) DEFAULT 0;
ALTER TABLE pois ADD COLUMN IF NOT EXISTS review_count INT DEFAULT 0;
```

#### 1.2.3 后端实现

**新增 Entity**

`com.smartcampus.entity.POIReview`
- 字段：id, poi(ManyToOne), user(ManyToOne), rating(Short 1-5), content(200), createdAt, updatedAt
- 唯一约束：(poi_id, user_id) — 每个 POI 每个用户只能评价一次，可修改

**新增 Repository**

`com.smartcampus.repository.POIReviewRepository`
- `findByPoiIdOrderByCreatedAtDesc(Long poiId, Pageable)`
- `findByPoiIdAndUserId(Long poiId, Long userId)`
- `countByPoiId(Long poiId)`
- `@Query AVG rating by poiId`

**新增 Service**

`com.smartcampus.service.POIReviewService` / `impl.POIReviewServiceImpl`
- `createOrUpdateReview(Long poiId, Long userId, int rating, String content)` — 创建或更新评价
- `getPoiReviews(Long poiId, Pageable)` — 分页评价列表
- `getPoiRatingSummary(Long poiId)` → { avgRating, reviewCount }
- `deleteReview(Long reviewId, Long userId)` — 删除自己的评价
- `getUserReviews(Long userId, Pageable)` — 我的评价列表

**新增 Controller**

`com.smartcampus.controller.POIReviewController`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/pois/{poiId}/reviews` | Public | 评价列表（分页） |
| GET | `/api/pois/{poiId}/rating` | Public | 评分汇总 |
| POST | `/api/pois/{poiId}/reviews` | Authenticated | 创建/更新评价 |
| DELETE | `/api/pois/reviews/{reviewId}` | Authenticated | 删除自己的评价 |
| GET | `/api/users/me/reviews` | Authenticated | 我的评价列表 |

**运营后台扩展**

`AdminContentController` 增加：
- `GET /api/admin/reviews` — 评价列表（支持按 POI/用户/评分筛选）
- `DELETE /api/admin/reviews/{reviewId}` — 删除评价

**修改 POI 相关**

`POIResponse` 新增字段：`avgRating`、`reviewCount`

#### 1.2.4 前端实现

**新增 API 文件**

`src/api/poiReview.js`
- `getPoiReviews(poiId, params)`
- `getPoiRatingSummary(poiId)`
- `createOrUpdateReview(poiId, { rating, content })`
- `deleteReview(reviewId)`
- `getUserReviews(params)`

**修改 MapContainer.vue**

POI 详情弹窗中，打卡卡片下方新增评分区域：
- 5 星评分组件（可点击选择）
- 文字输入框（可选，200 字以内）
- 提交按钮
- 已评价时显示自己的评分和内容，支持修改

**新增评价列表展示**

POI 详情弹窗底部展示评价列表：
- 每条评价显示：用户头像+名称、星级、文字、时间
- 支持分页加载更多

**修改 Settings.vue**

新增"我的评价"标签页，展示用户发表过的所有评价。

**新增运营后台页面**

`src/views/Admin/ReviewList.vue` — 评价管理页面

#### 1.2.5 任务清单

- [x] 后端：新增 `POIReview` Entity
- [x] 后端：新增 `POIReviewRepository`
- [x] 后端：新增 `POIReviewService` + `POIReviewServiceImpl`
- [x] 后端：新增 `POIReviewController`
- [ ] 后端：修改 `POIResponse` 增加 avgRating / reviewCount
- [x] 后端：AdminContentController 增加评价管理接口（集成在 POIReviewController 中）
- [x] 前端：新增 `src/api/poiReview.js`
- [x] 前端：修改 `MapContainer.vue` 增加评分组件和评价列表
- [x] 前端：修改 `Settings.vue` 增加"我的评价"区域
- [x] 前端：新增 `src/views/Admin/ReviewList.vue` 运营评价管理
- [x] 前端：路由中增加评价管理页
- [ ] 测试：创建/更新/删除评价
- [ ] 测试：评分汇总正确计算
- [ ] 测试：运营后台评价管理

---

### 1.3 用户公开主页

#### 1.3.1 功能描述

- 点击分享、回复、评价中的用户名/头像可跳转到该用户的公开主页
- 展示：头像、昵称、简介、注册时间
- 展示：打卡数、分享数、获赞数、评价数
- 展示：最近的打卡分享（分页）
- 展示：打卡过的 POI 列表（可展开地图模式）
- 第二梯队完成后：关注/粉丝数、关注按钮

#### 1.3.2 数据库设计

无需新增表。所有数据可从现有表聚合查询：
- 打卡数 → `poi_check_ins` COUNT
- 分享数 → `poi_shares` COUNT
- 获赞数 → `poi_share_likes` WHERE share.user_id = ?
- 评价数 → `poi_reviews` COUNT（待 1.2 完成后）

#### 1.3.3 后端实现

**新增 Controller**

`com.smartcampus.controller.UserProfileController` 中增加公开接口：

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/users/{userId}/profile` | Public | 获取用户公开资料 |
| GET | `/api/users/{userId}/shares` | Public | 该用户的分享列表（分页） |
| GET | `/api/users/{userId}/checkins` | Public | 该用户的打卡列表（分页，含 POI 摘要） |
| GET | `/api/users/{userId}/stats` | Public | 该用户的统计摘要 |

**新增 DTO**

`com.smartcampus.dto.response.UserPublicProfileResponse`
- userId, username, displayName, avatarUrl, bio, createdAt
- checkInCount, shareCount, receivedLikeCount, reviewCount

#### 1.3.4 前端实现

**新增页面**

`src/views/UserProfile.vue`
- 顶部：用户信息卡片（头像、昵称、简介、注册时间）
- 统计条：打卡数、分享数、获赞数、评价数
- 标签页切换：分享 | 打卡足迹
- 分享列表：复用现有卡片样式
- 打卡足迹：POI 列表或地图模式

**新增 API 文件**

`src/api/user.js` 中增加：
- `getUserPublicProfile(userId)`
- `getUserPublicShares(userId, params)`
- `getUserCheckIns(userId, params)`
- `getUserStats(userId)`

**路由注册**

```js
{
  path: '/user/:userId',
  name: 'UserProfile',
  component: () => import('@/views/UserProfile.vue'),
  meta: { title: '用户主页' }
}
```

**修改现有组件**

- `PoiSharePanel.vue`：分享/回复中的用户名/头像可点击，跳转到 `/user/{userId}`
- `MapContainer.vue`：POI 详情弹窗中的打卡用户列表（如有）可点击跳转

#### 1.3.5 任务清单

- [x] 后端：新增 `UserPublicProfileResponse` DTO
- [x] 后端：`UserProfileController` 增加公开资料、分享列表、打卡列表、统计接口
- [x] 后端：Service 层实现用户统计数据聚合查询
- [x] 前端：新增 `src/views/UserProfile.vue` 页面
- [x] 前端：`src/api/user.js` 增加公开主页相关接口
- [x] 前端：路由注册 `/user/:userId`
- [x] 前端：修改 `PoiSharePanel.vue` 用户名/头像添加跳转链接
- [x] 前端：移动端适配
- [ ] 测试：公开主页数据正确展示
- [ ] 测试：未登录用户可查看公开主页
- [ ] 测试：移动端展示正常

---

### 1.4 探索排行

#### 1.4.1 功能描述

- 首页增加"排行榜"区域
- 排行维度：打卡达人（打卡数最多）、活跃分享者（分享数最多）、获赞最多
- 时间维度：周榜 / 总榜
- 地图上标注热门打卡区域（基于 POI 打卡数聚合）

#### 1.4.2 数据库设计

无需新增表。排行数据基于现有表实时聚合查询 + Redis 缓存。

Redis 缓存策略：
- `leaderboard:checkin:week` — 周打卡排行，每周日 0 点重置，缓存 1 小时
- `leaderboard:checkin:total` — 总打卡排行，缓存 1 小时
- `leaderboard:share:week` — 周分享排行
- `leaderboard:share:total` — 总分享排行
- `leaderboard:likes:week` — 周获赞排行
- `leaderboard:likes:total` — 总获赞排行

#### 1.4.3 后端实现

**新增 Service**

`com.smartcampus.service.LeaderboardService` / `impl.LeaderboardServiceImpl`
- `getLeaderboard(String type, String period, int limit)` — 获取排行数据
- 内部使用 JPQL 聚合查询 + Redis 缓存
- 返回 `List<LeaderboardItemResponse>`

**新增 DTO**

`com.smartcampus.dto.response.LeaderboardItemResponse`
- rank, userId, displayName, avatarUrl, count（打卡数/分享数/获赞数）

`com.smartcampus.dto.response.HotPoiResponse`
- poiId, poiName, category, latitude, longitude, checkInCount

**新增 Controller**

`com.smartcampus.controller.LeaderboardController`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/leaderboard` | Public | 排行榜数据（type=checkin/share/likes, period=week/total） |
| GET | `/api/leaderboard/hot-pois` | Public | 热门打卡 POI（top N） |

#### 1.4.4 前端实现

**新增组件**

`src/components/leaderboard/LeaderboardSection.vue`
- 首页新增排行区域（在推荐路线和公告之间）
- 三个标签页：打卡达人 | 分享达人 | 获赞达人
- 每个标签页内可切换：周榜 / 总榜
- 列表展示：排名、头像、昵称、数量

**新增 API 文件**

`src/api/leaderboard.js`
- `getLeaderboard(params)` — { type, period, limit }
- `getHotPois(params)`

**修改 Home.vue**

在活动区块和路线区块之间插入排行榜区域。

#### 1.4.5 任务清单

- [x] 后端：新增 `LeaderboardItemResponse`、`HotPoiResponse` DTO
- [x] 后端：新增 `LeaderboardService` + `LeaderboardServiceImpl`
- [x] 后端：新增 `LeaderboardController`
- [x] 后端：实现 Redis 缓存策略
- [x] 前端：新增 `src/api/leaderboard.js`
- [x] 前端：新增 `src/components/leaderboard/LeaderboardSection.vue`
- [x] 前端：修改 `Home.vue` 增加排行榜区块
- [x] 前端：移动端适配
- [ ] 测试：各维度排行榜数据正确
- [ ] 测试：Redis 缓存生效
- [ ] 测试：热门 POI 接口

---

## 第二梯队

### 2.1 关注 + 动态流

#### 2.1.1 功能描述

- 用户可关注其他用户
- 首页增加"动态"标签页，展示关注对象的最新打卡和分享
- 用户主页展示关注数和粉丝数
- 支持取消关注

#### 2.1.2 数据库设计

新增 `user_follows` 表：

```sql
CREATE TABLE user_follows (
    id            BIGSERIAL PRIMARY KEY,
    follower_id   BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    following_id  BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at    TIMESTAMP NOT NULL DEFAULT NOW(),
    CONSTRAINT uk_user_follows UNIQUE (follower_id, following_id),
    CONSTRAINT ck_user_follows_self CHECK (follower_id != following_id)
);

CREATE INDEX idx_user_follows_follower ON user_follows(follower_id);
CREATE INDEX idx_user_follows_following ON user_follows(following_id);
```

#### 2.1.3 后端实现

**新增 Entity**

`com.smartcampus.entity.UserFollow`
- 字段：id, follower(ManyToOne → User), following(ManyToOne → User), createdAt
- 唯一约束：(follower_id, following_id)，自关注校验

**新增 Repository**

`com.smartcampus.repository.UserFollowRepository`
- `existsByFollowerIdAndFollowingId(Long followerId, Long followingId)`
- `deleteByFollowerIdAndFollowingId(Long followerId, Long followingId)`
- `countByFollowerId(Long followerId)` — 关注数
- `countByFollowingId(Long followingId)` — 粉丝数
- `findByFollowerIdOrderByCreatedAtDesc(Long followerId, Pageable)` — 关注列表
- `findByFollowingIdOrderByCreatedAtDesc(Long followingId, Pageable)` — 粉丝列表
- `findFollowingIdByFollowerId(Long followerId)` — 我关注的人 ID 列表

**新增 Service**

`com.smartcampus.service.UserFollowService` / `impl.UserFollowServiceImpl`
- `follow(Long followerId, Long followingId)` — 关注
- `unfollow(Long followerId, Long followingId)` — 取消关注
- `getFollowStatus(Long currentUserId, Long targetUserId)` → { following, follower }
- `getFollowingList(Long userId, Pageable)` — 关注列表
- `getFollowerList(Long userId, Pageable)` — 粉丝列表
- `getFollowingIds(Long userId)` → List<Long>

**新增 Controller**

`com.smartcampus.controller.UserFollowController`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/users/{userId}/follow` | Authenticated | 关注用户 |
| DELETE | `/api/users/{userId}/follow` | Authenticated | 取消关注 |
| GET | `/api/users/{userId}/follow-status` | Optional Auth | 关注关系状态 |
| GET | `/api/users/{userId}/following` | Public | 关注列表 |
| GET | `/api/users/{userId}/followers` | Public | 粉丝列表 |

**动态流接口**

`com.smartcampus.controller.FeedController`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/feed` | Authenticated | 关注动态流（分页） |

`com.smartcampus.service.FeedService`
- 查询关注对象的最近分享，按时间倒序
- 初期直接 JOIN 查询，数据量大后可迁移到 Redis/Timeline 方案

#### 2.1.4 前端实现

**新增 API**

`src/api/userFollow.js`
- `followUser(userId)`
- `unfollowUser(userId)`
- `getFollowStatus(userId)`
- `getFollowingList(userId, params)`
- `getFollowerList(userId, params)`

`src/api/feed.js`
- `getFeed(params)`

**修改 Home.vue**

首页增加"动态"标签页（与地图并列或作为切换标签）。

**新增组件**

`src/components/feed/FeedSection.vue` — 动态流列表
`src/components/feed/FeedItem.vue` — 单条动态卡片

**修改 UserProfile.vue**

- 新增关注数/粉丝数统计
- 新增"关注"按钮（非自己的主页时显示）
- 新增"关注"/"粉丝"列表标签页

**修改 Settings.vue**

- 新增"我的关注"/"我的粉丝"区域

#### 2.1.5 任务清单

- [x] 后端：新增 `UserFollow` Entity
- [x] 后端：新增 `UserFollowRepository`
- [x] 后端：新增 `UserFollowService` + `UserFollowServiceImpl`
- [x] 后端：新增 `UserFollowController`
- [x] 后端：新增 `FeedService` + `FeedController`
- [x] 前端：新增 `src/api/userFollow.js`、`src/api/feed.js`
- [x] 前端：新增动态流组件
- [x] 前端：修改 `Home.vue` 增加动态标签页
- [x] 前端：修改 `UserProfile.vue` 增加关注/粉丝展示和操作
- [x] 前端：修改 `Settings.vue` 增加关注/粉丝管理
- [ ] 测试：关注/取关流程
- [ ] 测试：动态流数据正确且按时间排序
- [ ] 测试：不能自己关注自己

---

### 2.2 用户自创路线

#### 2.2.1 功能描述

- 登录用户可在地图上创建自己的游玩路线
- 选择多个 POI 作为途经点，设置出行方式
- 可添加路线标题、描述
- 路线发布后进入公开路线池，其他用户可浏览、收藏、复用
- 个人中心展示"我创建的路线"

#### 2.2.2 数据库设计

复用现有 `recommended_routes` 和 `recommended_route_waypoints` 的表结构，新增用户路线：

```sql
CREATE TABLE user_routes (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    title           VARCHAR(100) NOT NULL,
    summary         VARCHAR(200),
    description     TEXT,
    default_mode    VARCHAR(20) NOT NULL DEFAULT 'walking',
    cover_image_url VARCHAR(255),
    status          SMALLINT NOT NULL DEFAULT 1,
    like_count      INT NOT NULL DEFAULT 0,
    favorite_count  INT NOT NULL DEFAULT 0,
    created_at      TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE user_route_waypoints (
    id              BIGSERIAL PRIMARY KEY,
    route_id        BIGINT NOT NULL REFERENCES user_routes(id) ON DELETE CASCADE,
    poi_id          BIGINT REFERENCES pois(id) ON DELETE SET NULL,
    latitude        DECIMAL(10,7) NOT NULL,
    longitude       DECIMAL(10,7) NOT NULL,
    waypoint_name   VARCHAR(100),
    sort_order      INT NOT NULL DEFAULT 0,
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_user_routes_user_id ON user_routes(user_id);
CREATE INDEX idx_user_routes_status ON user_routes(status);

CREATE TABLE user_route_likes (
    id          BIGSERIAL PRIMARY KEY,
    route_id    BIGINT NOT NULL REFERENCES user_routes(id) ON DELETE CASCADE,
    user_id     BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at  TIMESTAMP NOT NULL DEFAULT NOW(),
    CONSTRAINT uk_user_route_likes UNIQUE (route_id, user_id)
);

CREATE TABLE user_route_favorites (
    id          BIGSERIAL PRIMARY KEY,
    route_id    BIGINT NOT NULL REFERENCES user_routes(id) ON DELETE CASCADE,
    user_id     BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    created_at  TIMESTAMP NOT NULL DEFAULT NOW(),
    CONSTRAINT uk_user_route_favorites UNIQUE (route_id, user_id)
);
```

#### 2.2.3 后端实现

**新增 Entity**

`UserRoute`、`UserRouteWaypoint`、`UserRouteLike`、`UserRouteFavorite`

**新增 Controller**

`com.smartcampus.controller.UserRouteController`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/user-routes` | Authenticated | 创建路线 |
| PUT | `/api/user-routes/{id}` | Authenticated | 更新自己的路线 |
| DELETE | `/api/user-routes/{id}` | Authenticated | 删除自己的路线 |
| GET | `/api/user-routes` | Public | 公开路线列表（分页） |
| GET | `/api/user-routes/{id}` | Public | 路线详情 |
| POST | `/api/user-routes/{id}/like` | Authenticated | 点赞 |
| DELETE | `/api/user-routes/{id}/like` | Authenticated | 取消点赞 |
| POST | `/api/user-routes/{id}/favorite` | Authenticated | 收藏 |
| DELETE | `/api/user-routes/{id}/favorite` | Authenticated | 取消收藏 |
| GET | `/api/users/me/routes` | Authenticated | 我创建的路线 |
| GET | `/api/users/me/favorite-routes` | Authenticated | 我收藏的路线 |

#### 2.2.4 前端实现

**新增页面**

`src/views/RouteCreate.vue` — 路线创建/编辑页面
- 地图上选择途经点 POI
- 设置标题、描述、出行方式
- 预览路线

**新增组件**

`src/components/route/UserRouteCard.vue` — 用户路线卡片
`src/components/route/UserRouteSection.vue` — 首页用户路线展示区

**路由注册**

```js
{ path: '/route/create', name: 'RouteCreate', component: ..., meta: { requiresAuth: true } }
{ path: '/route/:id', name: 'UserRouteDetail', component: ... }
```

#### 2.2.5 任务清单

- [x] 后端：新增 4 个 Entity + Repository
- [x] 后端：新增 `UserRouteService` + `UserRouteServiceImpl`
- [x] 后端：新增 `UserRouteController`
- [x] 前端：新增路线创建/编辑页面
- [x] 前端：新增路线详情页面
- [x] 前端：首页增加用户路线展示区
- [x] 前端：Settings.vue 增加我的路线/收藏路线
- [x] 前端：路由注册
- [ ] 测试：路线创建/编辑/删除
- [ ] 测试：路线点赞/收藏
- [ ] 测试：路线列表分页

---

### 2.3 成就/徽章体系

#### 2.3.1 功能描述

- 系统定义一组成就规则（如"打卡 10 个地点"、"发布 5 条分享"、"获得 50 个赞"）
- 用户达成条件后自动解锁徽章
- 用户主页展示已获得和未获得的徽章
- 解锁时有视觉反馈

#### 2.3.2 数据库设计

```sql
CREATE TABLE achievement_definitions (
    id          VARCHAR(50) PRIMARY KEY,
    name        VARCHAR(100) NOT NULL,
    description VARCHAR(200) NOT NULL,
    icon_url    VARCHAR(255),
    category    VARCHAR(50) NOT NULL,
    sort_order  INT NOT NULL DEFAULT 0,
    created_at  TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE user_achievements (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    achievement_id  VARCHAR(50) NOT NULL REFERENCES achievement_definitions(id),
    unlocked_at     TIMESTAMP NOT NULL DEFAULT NOW(),
    CONSTRAINT uk_user_achievements UNIQUE (user_id, achievement_id)
);

CREATE INDEX idx_user_achievements_user_id ON user_achievements(user_id);
```

#### 2.3.3 预设成就定义

| ID | 名称 | 描述 | 类别 | 条件 |
|----|------|------|------|------|
| check_in_1 | 初次打卡 | 完成第一次地点打卡 | 探索 | 打卡数 >= 1 |
| check_in_10 | 探索者 | 打卡 10 个不同地点 | 探索 | 打卡数 >= 10 |
| check_in_50 | 资深探索者 | 打卡 50 个不同地点 | 探索 | 打卡数 >= 50 |
| check_in_100 | 百地达人 | 打卡 100 个不同地点 | 探索 | 打卡数 >= 100 |
| share_1 | 第一次分享 | 发布第一条地点打卡内容 | 社交 | 分享数 >= 1 |
| share_10 | 内容创作者 | 发布 10 条打卡内容 | 社交 | 分享数 >= 10 |
| likes_10 | 受欢迎 | 获得累计 10 个赞 | 社交 | 获赞数 >= 10 |
| likes_100 | 人气之星 | 获得累计 100 个赞 | 社交 | 获赞数 >= 100 |
| category_5 | 多元探索 | 打卡涵盖 5 个不同分类 | 探索 | 打卡分类数 >= 5 |
| route_1 | 路线规划师 | 完成第一次路线规划 | 路线 | 路线规划次数 >= 1 |

#### 2.3.4 后端实现

**新增 Service**

`com.smartcampus.service.AchievementService`
- `checkAndUnlock(Long userId)` — 检查并解锁新成就（在打卡/分享/获赞等操作后调用）
- `getUserAchievements(Long userId)` — 获取用户所有成就（含未解锁的）
- `getAllDefinitions()` — 获取所有成就定义

**新增 Controller**

`com.smartcampus.controller.AchievementController`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/achievements` | Public | 所有成就定义 |
| GET | `/api/users/me/achievements` | Authenticated | 我的成就 |
| GET | `/api/users/{userId}/achievements` | Public | 某用户的成就 |

**集成点**

在以下 Service 方法中调用 `achievementService.checkAndUnlock(userId)`：
- `POICheckInServiceImpl.checkIn()` — 打卡后
- `POIShareServiceImpl.createShare()` — 分享后
- `POIShareServiceImpl.likeShare()` — 被赞用户检查（异步）
- `RouteServiceImpl.planRoute()` — 路线规划后

#### 2.3.5 前端实现

**修改 UserProfile.vue**

新增"成就"标签页，展示徽章网格：
- 已解锁：彩色图标 + 名称 + 解锁时间
- 未解锁：灰色图标 + 名称 + "???"

**修改 Settings.vue**

展示自己的成就进度概览。

#### 2.3.6 任务清单

- [x] 后端：新增 `AchievementDefinition`、`UserAchievement` Entity
- [x] 后端：初始化成就定义数据（启动时检查并插入）
- [x] 后端：新增 `AchievementService` + `AchievementServiceImpl`
- [x] 后端：新增 `AchievementController`
- [x] 后端：在各触发点集成成就检查
- [x] 前端：修改 `UserProfile.vue` 增加成就展示
- [x] 前端：修改 `Settings.vue` 增加成就概览
- [x] 前端：新增 `src/api/achievement.js`
- [ ] 测试：各成就触发条件正确
- [ ] 测试：成就不会重复解锁

---

## 第三梯队

### 3.1 POI 图片墙

#### 3.1.1 功能描述

- POI 详情中新增"图片墙"标签页
- 聚合展示所有用户对该 POI 分享中上传的图片
- 瀑布流或网格展示，点击可查看大图

#### 3.1.2 实现思路

无需新增表。复用 `poi_share_images` 表，通过 `poi_shares.poi_id` 关联查询：

```sql
SELECT si.* FROM poi_share_images si
JOIN poi_shares ps ON si.share_id = ps.id
WHERE ps.poi_id = ?
ORDER BY si.created_at DESC
```

#### 3.1.3 任务清单

- [x] 后端：新增 `GET /api/pois/{poiId}/gallery` 接口（分页）
- [x] 前端：修改 POI 详情弹窗增加"图片墙"标签页
- [x] 前端：瀑布流/网格图片展示组件
- [ ] 测试：图片墙加载和分页

---

### 3.2 标签/话题

#### 3.2.1 功能描述

- 分享支持添加 #话题标签
- 可按话题浏览内容
- 热门话题展示在首页

#### 3.2.2 数据库设计

```sql
CREATE TABLE tags (
    id         BIGSERIAL PRIMARY KEY,
    name       VARCHAR(50) NOT NULL UNIQUE,
    usage_count INT NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE poi_share_tags (
    share_id BIGINT NOT NULL REFERENCES poi_shares(id) ON DELETE CASCADE,
    tag_id   BIGINT NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
    PRIMARY KEY (share_id, tag_id)
);
```

#### 3.2.3 任务清单

- [x] 后端：新增 `Tag`、`POIShareTag` Entity
- [x] 后端：修改 `POIShare` 支持标签关联
- [x] 后端：新增标签查询接口（热门标签、按标签查分享）
- [x] 前端：分享发布时支持输入标签
- [x] 前端：标签展示和点击跳转
- [ ] 测试：标签创建、关联、查询

---

### 3.3 通知系统

#### 3.3.1 功能描述

- 用户收到通知场景：被关注、分享被点赞、分享被回复、评价被回复、成就解锁
- 顶部导航栏显示未读通知数角标
- 通知列表页（已读/未读标记、全部已读）
- 可选：邮件通知

#### 3.3.2 数据库设计

```sql
CREATE TABLE notifications (
    id              BIGSERIAL PRIMARY KEY,
    recipient_id    BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    actor_id        BIGINT REFERENCES users(id) ON DELETE SET NULL,
    type            VARCHAR(30) NOT NULL,
    title           VARCHAR(100) NOT NULL,
    content         VARCHAR(300),
    target_type     VARCHAR(30),
    target_id       BIGINT,
    is_read         BOOLEAN NOT NULL DEFAULT FALSE,
    created_at      TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_notifications_recipient ON notifications(recipient_id, is_read, created_at DESC);
```

#### 3.3.3 通知类型枚举

| type | 触发场景 | title 示例 |
|------|----------|-----------|
| FOLLOW | 被关注 | "{actor} 关注了你" |
| LIKE | 分享被点赞 | "{actor} 赞了你的打卡" |
| REPLY | 分享被回复 | "{actor} 回复了你的打卡" |
| ACHIEVEMENT | 成就解锁 | "恭喜解锁成就：{name}" |

#### 3.3.4 任务清单

- [x] 后端：新增 `Notification` Entity
- [x] 后端：新增 `NotificationService` + `NotificationServiceImpl`
- [x] 后端：新增 `NotificationController`（列表、标记已读、未读数）
- [x] 后端：在各触发点（关注、点赞、回复、成就）调用通知服务
- [x] 前端：Header 增加通知铃铛图标 + 未读数角标
- [x] 前端：新增通知列表页面或弹窗
- [x] 前端：新增 `src/api/notification.js`
- [ ] 测试：各类型通知正确触发
- [ ] 测试：已读/未读状态管理

---

## 实施路线图

```
第一阶段（建议先做，稳固基础互动）
  ├── 1.1 POI 收藏         ← 最基础，用户留存利器
  ├── 1.2 POI 评分与评价    ← 丰富 POI 互动维度
  ├── 1.3 用户公开主页      ← 建立社交身份
  └── 1.4 探索排行          ← 游戏化驱动

第二阶段（社交闭环，依赖第一阶段）
  ├── 2.1 关注 + 动态流     ← 核心社交功能
  ├── 2.2 用户自创路线      ← UGC 扩展
  └── 2.3 成就/徽章体系     ← 长线激励

第三阶段（锦上添花）
  ├── 3.1 POI 图片墙        ← 视觉丰富
  ├── 3.2 标签/话题         ← 内容聚合
  └── 3.3 通知系统          ← 闭环感知
```

---

## 约束与原则

1. **不破坏现有功能**：每个新增模块独立，不修改现有核心逻辑
2. **渐进式开发**：每完成一个功能就测试上线，不堆积
3. **数据库兼容**：使用 `IF NOT EXISTS`、默认值、可空字段，确保增量迁移不阻塞
4. **移动端适配**：所有新增前端组件必须包含 `@media` 响应式样式
5. **性能意识**：排行榜等聚合查询必须加 Redis 缓存
6. **安全**：所有用户输入（评价内容、标签）需做 XSS 过滤和长度校验
