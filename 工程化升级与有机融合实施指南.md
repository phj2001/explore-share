# 地点探索与打卡分享平台 —— 工程化升级与有机融合实施指南

> 本文档是「智慧校园综合运营平台」工程化精华向「地点探索与打卡分享平台」有机迁移的施工蓝图。
> 制定日期：2026-06-22　·　目标版本：Spring Boot 4.0.3 / Java 21 / JPA + Hibernate Spatial
> 源借鉴项目：`smart_campus_management`（Spring Boot 3.2.2 / MyBatis-Plus）
> **修订：R1（2026-06-22）——经全文回溯审计修正 8 处问题，详见文末「修订记录」**

---

## 0. 文档定位

本文档**不是功能堆砌清单**，而是回答一个问题：

> 如何把智慧校园项目中验证过的工程化能力，**顺着地点探索项目自身的业务纹理**移植进来，让主线项目从「全栈 CRUD 平台」升级为「具备可观测性、可靠消息、操作审计、登录安防、容器化部署的工业级平台」——同时不破坏其「地图化地点探索社区」的产品定位。

所有迁移项都遵循一个判断标准：**它是否能在地点探索的现有业务中找到自然落点，而不是凭空多出一块「没用的先进」**。

> ⚠️ **审计铁律**：凡本文档涉及"现状"的描述，均以代码核实为准；若某能力**目标项目已具备且更优，则不迁**（见第 6 章 Token 吊销）。

---

## 1. 有机融合三原则（贯穿全文）

| 原则 | 含义 | 反例（禁止） |
|---|---|---|
| **① 业务落点优先** | 每个能力必须挂载到地点探索已有的业务场景上 | 为了写 RabbitMQ 而硬造一个"消息中心" |
| **② 复用现有体系** | 优先增强已有实体/Service/配置，而非新建平行体系 | 已有 `AdminOperationLog` 却另起一张审计表 |
| **③ 适配而非照搬** | 智慧校园用 MyBatis-Plus，本项目用 JPA——所有数据访问层必须转写；且先核查目标项目是否已有更优实现 | 直接复制 `LambdaQueryWrapper`；把更差的方案引入覆盖更优的 |

---

## 2. 地点探索项目现状基线（融合前的"地基"）

> 以下结论均来自代码核实，是后续所有改造的出发点。

### 2.1 技术栈基线
- **后端**：Spring Boot **4.0.3** / Java 21 / JPA + Hibernate Spatial + PostgreSQL(+PostGIS) / Spring Security / JWT / Redis / Mail
- **前端**：Vue 3 + Vite + Element Plus + Pinia
- **配置**：`application.properties` + `application-dev/prod.properties`，**全部环境变量化**（`*.properties` 用 `${VAR:default}`），已具备 dev/prod 分离——这一块比智慧校园更规范（智慧校园 `application.yml` 明文写了数据库密码），**配置管理不需向其学习**。
- **主启动类**：`SmartCampusBackendApplication`，已开启 `@EnableAsync`，**未开启 `@EnableScheduling`**（关键前置，见第 7 章）。
- **容器化**：⚠️ **本项目当前没有 `docker-compose.yml`**（Docker 配置在智慧校园项目里）。容器化是本次升级要**新建**的工作，不是修改。

### 2.2 业务实体规模（29 个，与简历一致）
POI 体系（POI/POIApplication/POICheckIn/POIFavorite/POIReview）、分享互动（POIShare/POIShareImage/POIShareLike/POIShareReply/POIShareTag/POIShareTagId/RecommendedShare）、路线（RecommendedRoute/RecommendedRouteWaypoint/UserRoute/UserRouteWaypoint/UserRouteFavorite/UserRouteLike）、社交（User/UserFollow/AchievementDefinition/UserAchievement）、运营治理（Activity/Announcement/Notification/ContentReport/SystemConfigEntry/**AdminOperationLog**）。

### 2.3 Controller 形态（37 个，平铺无子包）
- **包结构**：全部 37 个 Controller **平铺在 `com.smartcampus.controller` 包下，没有任何子包**（无 `controller.admin` 之类）。
- **后台 Admin Controller（13 个，类名以 `Admin` 前缀）**：`AdminActivityController / AdminAnnouncementController / AdminContentController / AdminContentReportController / AdminDashboardController / AdminFileResourceController / AdminOperationLogController / AdminPOIApplicationController / AdminPoiCategoryController / AdminRecommendedRouteController / AdminRecommendedShareController / AdminSystemConfigController / AdminUserController`
- **前台 C 端 Controller**：`POIController / POIShareController / POICheckInController / POIReviewController / UserRouteController / NotificationController / AnnouncementController / AuthController / ContentReportController / FeedController / LeaderboardController / TagController / UserFollowController ...`
- **对 AOP 的含义**：没有 `controller.Admin*` 子包可切，切面必须用**注解驱动**（`@annotation(OperationLog)`）或按类名前缀匹配，详见第 5 章。

### 2.4 五处关键现状（直接决定融合方案）

**① 操作日志：已是"业务语义级手动记录"**
- 已有完整链路：`AdminOperationLog` 实体 + `AdminOperationLogService.record(...)` + Repository + Controller。
- 实体字段：`operatorUserId / operatorDisplayName / operatorUsername / moduleName / actionName / targetType / targetId / summary / createdAt`（`@PrePersist` 填充时间）。
- `record()` 方法签名（唯一、无重载）：
  ```java
  void record(Long operatorUserId, String moduleName, String actionName,
              String targetType, Long targetId, String summary);
  ```
- 现状调用方式（散落各 Admin Service，每个 Service 注入并手动调用）：
  ```java
  // AdminContentServiceImpl.java:120
  adminOperationLogService.record(operatorUserId, "内容管理", "删除分享", "分享", shareId, summary);
  ```
- **痛点**：① 手动调用散落，易遗漏；② 前台用户关键行为（打卡、发分享、举报）**完全没有留痕**；③ 缺少请求级元数据（IP、traceId、成功/失败、耗时）。
- **融合定位**：**不是替换 record()，而是用 AOP + 声明式注解升级它**（详见第 5 章）。

**② 通知：同步直写 DB（注意事务语义）**
- `NotificationServiceImpl.sendNotification(recipientId, actorId, type, title, content, targetType, targetId)` 直接 `notificationRepository.save(notification)`。
- ⚠️ **当前事务传播是 `@Transactional(propagation = Propagation.REQUIRES_NEW)`**——这是 Outbox 改造的关键矛盾点（详见第 7 章）。
- 触发场景：点赞、回复、关注、举报处理等。
- **融合定位**：**Outbox 改造的最干净切入点**——业务调用签名不变，仅替换内部实现 + 反转事务策略。

**③ JWT 吊销：已实现且方案更优（不迁！）**
- `JwtAuthenticationFilter` 第 47–59 行已实现 **`revoke_before` 时间戳吊销方案**：
  ```
  key: jwt:revoke_before:{userId} = 当前时间戳
  校验：若 token 签发时间(issuedAt) <= revoke_before，则视为已吊销
  ```
- 配套：`AuthController` 已有 `@DeleteMapping("/logout")` 登出接口；`JwtTokenProvider` 已有 `getIssuedAtFromToken()`。
- **对比智慧校园**：智慧校园用 `TokenBlacklistService`（逐 token SHA256 入黑名单，每个 token 占一个 key）。**地点探索的 `revoke_before` 按用户维度只存一个时间戳，存储成本与复杂度更低，方案更优**。
- **结论**：Token 吊销这块**不迁、不改、保留现有**，并作为简历亮点（见第 12 章）。

**④ 登录：已有风控接入点，但无失败锁定**
- 登录链路完整：`AuthController.loginByJson/loginByForm/loginByMultipart`（3 种 content-type 都映射到 `/api/auth/login`）→ `AuthService.login(...)`（实现于 `AuthServiceImpl`）。
- **缺**：无失败计数、无锁定——这是对外开放 C 端社区的真实缺口。
- **融合定位**：仅迁移"登录风控（失败锁定）"即可，集成点清晰（`AuthServiceImpl.login`）。

**⑤ Redis 工程化已有基础**
- `JwtAuthenticationFilter` 已用 `user:info:{userId}` 缓存（`status:role` 格式，5 分钟 TTL）；有 `RedisUtils` 工具类。
- **含义**：登录风控迁移可直接复用 `RedisUtils` / `StringRedisTemplate`，无需额外搭建。

---

## 3. 升级蓝图总览

### 3.1 五项工作 + 优先级矩阵（R1 修订：新增"Docker 化前置"，登录安全项缩减）

| # | 升级项 | 业务落点 | 侵入性 | 工作量 | 优先级 |
|---|---|---|---|---|---|
| ⓪ | **Docker 化前置**（新建 docker-compose） | 全部后续项依赖 | 低 | S | ⭐⭐⭐⭐⭐ 前置 |
| ① | **可观测性体系**（traceId + Prometheus + Grafana） | 全局请求链路 / 服务监控 | 极低（零业务改动） | S | ⭐⭐⭐⭐⭐ 先做 |
| ② | **AOP 操作审计统一**（注解化 + 切面） | 后台管理 + 前台关键行为留痕 | 中（改 Admin Service） | M | ⭐⭐⭐⭐ |
| ③ | **登录风控**（失败计数 + 锁定） | 登录链路 | 低（改 AuthServiceImpl） | S | ⭐⭐⭐⭐ |
| ④ | **通知 Outbox 异步可靠投递** | 点赞/回复/关注/路线等 6 类通知 | 中高（新增 MQ + 改 sendNotification + 反转事务） | L | ⭐⭐⭐⭐⭐ 核心价值 |

> **R1 变更说明**：
> - 原"升级项③ 登录安全增强（风控 + Token 黑名单）"**缩减为"登录风控"**——Token 吊销本项目已有更优方案（`revoke_before`），不迁。
> - **新增"升级项⓪ Docker 化前置"**——本项目当前无 `docker-compose.yml`，而可观测性与 Outbox 都需要 Redis/RabbitMQ/Prometheus/Grafana 容器，必须先 Docker 化。

### 3.2 推荐实施顺序
**⓪ Docker 化前置 → ① 可观测性 → ③ 登录风控 → ② AOP 审计 → ④ Outbox**

> 理由：⓪ 是 ①④ 的基础设施依赖，必须最先；①③ 侵入最小、见效最快；② 需重构 Admin Service；④ 改动最大且需 RabbitMQ，放最后集中攻坚。
>
> 📌 **关于"章节顺序 vs 实施顺序"**：第 4–8 章按**工程领域**展开（Docker→可观测→AOP→登录→Outbox），实施顺序（上表 / M0–M4）按**侵入性与工作量**排序，二者编号一致但顺序不同（登录风控③ 在 AOP 审计② 之前实施）。施工时以本表为准，章节仅作能力说明。

---

## 4. 升级项⓪：Docker 化前置（新建 docker-compose.yml）

### 4.1 为什么需要它
- 本项目当前**没有 `docker-compose.yml`**（核实结论，见 2.1）。本地运行靠手动起 PostgreSQL/Redis。
- 后续可观测性（Prometheus/Grafana）、Outbox（RabbitMQ）都需要中间件容器，Docker 化是它们的前置。
- 智慧校园项目已有成熟的 `docker-compose.yml`（postgres + backend + frontend 三服务，环境变量化、带 volumes 和 health 依赖），可直接作为蓝本扩展。

### 4.2 目标设计
基于智慧校园 `docker-compose.yml` 扩展，补齐本项目需要的中间件：

```yaml
services:
  postgres:
    image: postgis/postgis:15-3.4          # ⚠️ 本项目用 PostGIS，必须用 postgis 镜像而非普通 postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-discover}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports: ["5432:5432"]
    volumes:
      - ./smart-campus-backend/db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - pg_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    restart: unless-stopped

  rabbitmq:                                  # Outbox 依赖（升级项④）
    image: rabbitmq:3.13-management
    ports: ["5672:5672", "15672:15672"]
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-guest}
    restart: unless-stopped

  prometheus:                                # 可观测性依赖（升级项①）
    image: prom/prometheus:latest
    volumes: ["./ops/prometheus.yml:/etc/prometheus/prometheus.yml:ro"]
    ports: ["9090:9090"]
    restart: unless-stopped

  grafana:                                   # 可观测性依赖（升级项①）
    image: grafana/grafana:latest
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
    ports: ["3000:3000"]
    depends_on: [prometheus]
    restart: unless-stopped

  backend:
    build: { context: ./smart-campus-backend }
    depends_on: [postgres, redis, rabbitmq]
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-discover}
      REDIS_HOST: redis
      RABBITMQ_HOST: rabbitmq
      # ... 其余沿用 application.properties 的 ${VAR} 注入
    ports: ["8080:8080"]
    volumes: [backend_storage:/app/uploads]
    restart: unless-stopped

  frontend:
    build: { context: ./smart-campus-frontend, args: { VITE_API_BASE_URL: /api } }
    depends_on: [backend]
    ports: ["5173:80"]
    restart: unless-stopped

volumes: { pg_data: , backend_storage: }
```

> ⚠️ **PostGIS 关键点**：智慧校园用的是普通 `postgres:15`，但本项目依赖 PostGIS（Hibernate Spatial + 空间索引），必须换成 `postgis/postgis:15-3.4` 这类带 PostGIS 扩展的镜像，否则空间查询启动即报错。这是本项目与智慧校园 Docker 化的最大差异。

### 4.3 验收标准
- [ ] `docker compose up -d` 能起齐 postgres(+PostGIS)/redis/rabbitmq/prometheus/grafana。
- [ ] 后端容器能连上 postgres 并通过 PostGIS 空间索引初始化（`POIDatabaseIndexInitializer` 不报错）。

---

## 5. 升级项①：可观测性体系（traceId 链路追踪 + Prometheus + Grafana）

### 5.1 为什么地点探索需要它
- 地图社区请求链路长（POI 查询走 PostGIS 空间索引 + 高德 API + 图片缩略图），出问题难定位。
- 当前 `application.properties` 已开 `server.compression`，说明已有工程化意识，补齐监控是自然延伸。
- **零业务侵入**，性价比最高。

### 5.2 目标设计
1. 每个请求自动生成/透传 `X-Trace-Id`，写入 MDC，日志自动带 traceId。
2. 暴露 `/actuator/prometheus` 指标端点。
3. Grafana 预设监控大盘（依赖升级项⓪ 的 prometheus/grafana 容器）。

### 5.3 改造清单（源：智慧校园 `config/RequestTraceFilter.java` + `util/RequestTraceContext.java` + `config/ObservabilityConfig.java`）

**新增文件**（直接移植，仅改包名 `com.example.smart_campus` → `com.smartcampus`）：
- `util/RequestTraceContext.java` —— traceId 生成/解析/MDC 工具类。核心方法：`resolveTraceId(request)` / `getCurrentTraceId()` / `prependTraceId(msg)`。
- `config/RequestTraceFilter.java` —— `OncePerRequestFilter`，把 traceId 放入 MDC 与响应头 `X-Trace-Id`。
- `config/ObservabilityConfig.java` —— `MeterRegistryCustomizer` 注入 `commonTags(application, module)`。

**修改文件**：
- `pom.xml`：新增依赖
  ```xml
  <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-actuator</artifactId></dependency>
  <dependency><groupId>io.micrometer</groupId><artifactId>micrometer-registry-prometheus</artifactId></dependency>
  ```
  > ⚠️ **SB4 兼容性验证点**：本项目用 Spring Boot **4.0.3**（比智慧校园的 3.2.2 新一个大版本）。SB4 下 actuator 端点暴露、micrometer 注册、jakarta 命名空间可能有变化。**务必加依赖后先单独跑一次启动 + `GET /actuator`**，以实际响应为准修正配置键，不要盲目照搬 SB3 的 `management.*` 键名。
>
> ✅ **M1 实测结论（2026-06-23，已 `./mvnw compile` exit=0 通过）**：
> - **类包路径迁移（关键，已修正）**：`MeterRegistryCustomizer` 在 SB4 从 `org.springframework.boot.actuate.autoconfigure.metrics` 迁移到 **`org.springframework.boot.micrometer.metrics.autoconfigure`**，所在模块也变为 SB4 新拆分的 `spring-boot-micrometer-metrics`（由 `spring-boot-starter-actuator` 传递引入，**无需额外声明依赖**）。`InfoContributor` 包路径**未变**（仍 `org.springframework.boot.actuate.info`）。`RequestTraceContext`/`RequestTraceFilter` 源码已是 jakarta 命名空间，直接改包名移植即可编译通过。
> - **配置键**：`management.endpoints.web.exposure.include` / `management.endpoint.prometheus.enabled` 在 SB4 仍有效；prometheus registry 由 classpath 自动激活，`management.prometheus.metrics.export.enabled` **可省略**——若运行后 `/actuator/prometheus` 无数据，再补 `management.metrics.export.prometheus.enabled=true`（micrometer 2.x 键位）。
- `application.properties`：新增
  ```properties
  management.endpoints.web.exposure.include=health,info,prometheus,metrics
  management.endpoint.prometheus.enabled=true
  management.prometheus.metrics.export.enabled=true
  spring.application.name=discover-share-places
  ```

### 5.4 验收标准
- [ ] 任意请求响应头含 `X-Trace-Id`，后端日志可按 traceId 串联。
- [ ] `GET /actuator/prometheus` 返回指标文本。
- [ ] Grafana 能查到 `http_server_requests_seconds`、`jvm_memory_used_bytes` 等指标。

### 5.5 简历价值
> 集成 Prometheus + Grafana 实现服务监控大盘，基于 traceId 实现全链路追踪，将线上问题定位从"翻日志"升级为"指标 + 链路"双维定位。

---

## 6. 升级项②：AOP 操作审计统一（注解化 + 切面）

### 6.1 为什么"有机"——关键洞察
智慧校园的 `OperationLogAspect` 记录的是**HTTP 请求级**元数据（接口、方法、IP、成功/失败）。地点探索现有 `record()` 记录的是**业务语义级**数据（谁对哪个 targetId 做了什么，带 summary）。

**两者是互补关系，不是替代关系。** 强行用 AOP 替换 record() 会丢失 targetId/summary 的精准度；保留 record() 又无法解决"散落、易遗漏、无请求级元数据"的痛点。

**有机方案**：引入声明式注解 `@OperationLog`，让 AOP 切面统一接管，复用现有 `AdminOperationLog` 实体与 `record()` 方法，并通过 SpEL 从方法参数提取 targetId/summary——既统一入口，又保留业务语义。

### 6.2 目标设计
```java
// 改造后：Admin Service 从"手动调用 record()"变为"注解声明"
// 注：实际签名是 deleteShare(shareId, operatorUserId)——Controller 把 userId 作为第二参传入（见 6.3）
@OperationLog(module = "内容管理", action = "删除分享", targetType = "分享",
              targetIdSpel = "#shareId", summarySpel = "'删除分享 #' + #shareId")
public void deleteShare(Long shareId, Long operatorUserId) { ... }
```
切面自动完成：解析注解 → 提取 SpEL → 取当前用户 → 补 IP/traceId/成功失败 → 调用现有 `adminOperationLogService.record(...)` 落库。

### 6.3 改造清单

**新增**：
- `annotation/OperationLog.java` —— 自定义注解（module / action / targetType / targetIdSpel / summarySpel）。
- `aspect/OperationLogAspect.java` —— `@Around` 切面。
  - **切点用注解驱动**：`@annotation(com.smartcampus.annotation.OperationLog)`。
    > ⚠️ R1 修正：本项目 Controller **平铺无子包**，不能像智慧校园那样按包名 + 方法名前缀匹配。注解驱动是最精准、零命名耦合的方式。
  - SpEL 解析用 `SpelExpressionParser` + `DefaultParameterNameDiscoverer`。
  - 异常分支也记录（状态=失败）。
  - IP 提取复用智慧校园的 `X-Forwarded-For` 逻辑。
  - traceId 通过升级项① 的 `RequestTraceContext.prependTraceId` 拼入 summary。

**当前用户获取（适配点）**：
- 本项目 `JwtAuthenticationFilter` 把 `userId`（`Long`）作为 principal 放入 `SecurityContextHolder`（**不是**智慧校园的 `Auth` 对象）。
- 切面取当前用户：`Long userId = (Long) SecurityContextHolder.getContext().getAuthentication().getPrincipal();` 再查 `UserRepository` 取 displayName/username。
- 现状参照：后台 Controller（如 `AdminContentController`）正是用 `getCurrentUserId(authentication)` 从 `Authentication.getPrincipal()` 取 `Long userId`，再显式作为参数传入 Service——切面统一从 `SecurityContext` 取，可省去这层手动传递。

**⚠️ 注解适用边界（R2 补充，避免实施踩坑）**：
- `@OperationLog` 优先加在 **Controller 直接调用的 Service 入口方法**（这些方法运行在请求线程内，`SecurityContext` 可用）。
- **若某 Service 方法被另一个 Service 内部调用（非 HTTP 请求链路），`SecurityContext` 为空，切面取不到用户**——此类场景应保留现有手动 `record()`，不要套注解。
- `operatorUserId` 一律由切面从 `SecurityContext` 获取，**不通过 SpEL 表达式**；SpEL 仅用于 `targetId` / `summary` 这类业务参数。

**增强 `AdminOperationLog` 实体**（可选，建议加）：
- 新增字段：`request_method`、`request_uri`、`ip_address`、`operation_status`(成功1/失败0)、`trace_id`、`duration_ms`。
- 迁移脚本：`ALTER TABLE admin_operation_logs ADD COLUMN ...`（均允许 null，向后兼容）。
- 注意：`record()` 现有 6 参数签名要保持向后兼容，建议**新增一个重载** `record(...)` 接收扩展字段，旧调用点不受影响。

**重构 Admin Service**（渐进式，优先改高频 Service）：
- `AdminContentServiceImpl` / `AdminContentReportServiceImpl` / `AdminAnnouncementServiceImpl` / `AdminActivityServiceImpl` / `AdminUserServiceImpl`：把内部手动 `record(...)` 替换为方法级 `@OperationLog`。

**扩展到前台关键行为**（智慧校园没有、但地点探索更需要的超越点）：
- 在 `POICheckInController`（打卡）、`POIShareController`（发分享）、`ContentReportController`（举报）等关键方法上加 `@OperationLog`，补齐"前台用户行为留痕"。

### 6.4 验收标准
- [ ] 后台任意写操作自动产生审计记录，含 IP/traceId/成功失败。
- [ ] 前台"打卡/发分享/举报"行为可留痕。
- [ ] 手动 `record()` 调用点逐步清零（除少数无法用注解表达的场景）。

### 6.5 简历价值
> 基于 Spring AOP + 自定义注解 + SpEL 实现声明式操作审计，复用统一 `AdminOperationLog` 模型，覆盖后台治理与前台关键行为全链路；集成 traceId 实现审计记录与请求链路串联。

---

## 7. 升级项③：登录风控（失败计数 + 锁定）

> **R1 修订**：本项原为"登录安全增强（风控 + Token 黑名单）"。经核实，Token 吊销本项目已有更优的 `revoke_before` 方案（见 2.4 ③），**删除 Token 黑名单迁移**，本项缩减为纯"登录风控"。

### 7.1 为什么地点探索需要它
- 本项目是对外开放的 C 端社区（简历原话："面向大众用户"），暴露面远大于校园内网，**暴力破解防护是刚需**。
- 当前 `AuthServiceImpl.login` 只校验密码，无失败锁定。

### 7.2 目标设计
- 登录连续失败 N 次（默认 5 次）锁定账号 X 秒（默认 900s）。
- Redis 优先、本地内存降级（Redis 不可用时不阻断业务）。

### 7.3 改造清单（源：智慧校园 `LoginRiskControlServiceImpl`）

**新增 Service**（移植，仅改包名，核心逻辑零改动）：
- `service/LoginRiskControlService.java` + `impl/LoginRiskControlServiceImpl.java`
  - 三方法：`assertLoginAllowed(username)` / `recordFailure(username)` / `clearFailures(username)`
  - Redis key：`auth:login:fail:count:{user}`（TTL 1800s）/ `auth:login:fail:lock:{user}`（TTL 900s）
  - **保留其 Redis 优先 + ConcurrentHashMap 本地降级的双保险设计**——工业级实现，不要简化。
  - 可直接用本项目的 `RedisUtils` 或注入 `StringRedisTemplate`。

**集成点**（`AuthServiceImpl.login`）：

> 核实：本方法签名是 `Optional<LoginResponse> login(String identifier, String password)`，`identifier` 支持**邮箱或用户名**；3 个 Controller 登录入口（JSON/FORM/MULTIPART）统一委托 `doLogin → authService.login`，故**只需改这一处**。

```
assertLoginAllowed(identifier)    // 前置：检查是否锁定（key 用 identifier）
... 校验密码 ...
if (密码错误) recordFailure(identifier)   // 后置：记失败
else           clearFailures(identifier)  // 成功：清零
```
> 风控 key 设计说明：用 `identifier` 作为锁定维度。若担心"同一用户用邮箱/用户名两种方式登录可绕过锁定"，可在校验通过后拿到 `userId`，再用 `userId` 作为二次锁定 key（增强项，初版可不做）。

**配置项**（追加到 `application.properties`，保持本项目环境变量风格）：
```properties
app.security.login-risk-control-enabled=${LOGIN_RISK_ENABLED:true}
app.security.login-max-failures=${LOGIN_MAX_FAILURES:5}
app.security.login-lock-duration-seconds=${LOGIN_LOCK_SECONDS:900}
app.security.login-counter-ttl-seconds=${LOGIN_COUNTER_TTL:1800}
```

> **Token 吊销说明**：登出即时失效由现有 `AuthController.logout` + `revoke_before` 机制保障，**本次不动**。简历可把 `revoke_before` 作为独立亮点（见第 12 章）。

### 7.4 验收标准
- [ ] 同一账号连续 5 次输错密码后，第 6 次登录被拒并提示剩余锁定秒数。
- [ ] 登录成功后失败计数清零。
- [ ] 关闭 Redis 时登录仍可工作（降级到本地内存）。

### 7.5 简历价值
> 实现登录风控（失败计数 + 定时锁定，Redis 优先 + 本地内存降级），防御暴力破解；配合现有 `revoke_before` 时间戳方案实现登出 Token 即时吊销。

---

## 8. 升级项④：通知 Outbox 异步可靠投递（核心业务升级）

### 8.1 为什么这是"最有机"的融合
- 地点探索**已有完整的 Notification 业务**，且经核实**所有通知场景统一走 `sendNotification` 单一入口、无人绕过**（全项目仅 `sendNotification` 内部一处 `new Notification()/save`）。触发点实际覆盖 6 类：分享点赞(`LIKE`)、分享回复(`REPLY`)、用户关注(`FOLLOW`)、POI 申请处理、成就达成(`ACHIEVEMENT`)、路线互动，分散于 `POIShareServiceImpl / UserFollowServiceImpl / POIApplicationServiceImpl / AchievementServiceImpl / UserRouteServiceImpl`。当前是**同步直写 DB**。
- 同步写入的问题：① 通知失败会拖垮主业务；② 无法重试；③ 未来扩展多通道（邮件/推送）困难。
- Outbox 模式让通知变为**异步、可重试、最终一致**。**因为通知入口是 `sendNotification` 唯一方法，只需改造它一处，全部 6 类通知自动升级**——这是比"分散改造"更彻底的有机融合。

> ⚠️ R2 事实订正：原"点赞/回复/关注/路线等 6 类通知"中**"举报"不触发通知**，举报走的是 `AdminContentReportServiceImpl` 的治理流程，不调 `sendNotification`。准确清单见上。

### 8.2 ⚠️ 两个隐蔽前置（R1 审计新增，最易踩坑）

**前置 A：开启 `@EnableScheduling`**
- 本项目主启动类 `SmartCampusBackendApplication` **只有 `@EnableAsync`，没有 `@EnableScheduling`**。
- Outbox Dispatcher 的 `@Scheduled` 定时扫描**必须在主启动类加 `@EnableScheduling` 才会生效**，否则 outbox 堆积无人投递。这是最容易遗漏的一步。

**前置 B：反转 `sendNotification` 的事务传播（核心语义坑）**
- 当前：`sendNotification` 用 `@Transactional(propagation = REQUIRES_NEW)` → 开**独立新事务**，本意是"通知不受主业务回滚影响"。
- Outbox 要求：**outbox 事件必须与业务同事务**——业务回滚则事件回滚（否则会出现"业务没成功但通知发了"的脏数据）。
- **改造必须反转**：写 outbox 时用默认 `REQUIRED`（加入调用方事务），**删掉 `REQUIRES_NEW`**。
  ```java
  // Before（同步直写，独立事务）
  @Transactional(propagation = Propagation.REQUIRES_NEW)
  public void sendNotification(...) { notificationRepository.save(n); }

  // After（写 outbox，跟随业务事务）
  @Transactional  // 默认 REQUIRED，与调用方同事务
  public void sendNotification(...) { outboxRepository.save(event); }
  ```
- 这是 Outbox 模式的精髓，搞反了会导致事件一致性破坏。

### 8.3 目标架构
```
业务层 sendNotification()  【REQUIRED，同业务事务】
        │  写 outbox（业务成功则事件必存；业务回滚则事件回滚）
        ▼
 ┌──────────────┐   @Scheduled 扫描 PENDING（需 @EnableScheduling）
 │ outbox 表    │ ───────────────────▶ ┌─────────────┐
 │ (JPA Entity) │                      │ Dispatcher  │
 └──────────────┘                      └──────┬──────┘
                                              │ publish
                                              ▼
                                      ┌─────────────┐    DLX/DLQ
                                      │  RabbitMQ   │ ─────────▶ 死信队列
                                      │ Exchange    │
                                      └──────┬──────┘
                                             ▼
                                      ┌─────────────┐
                                      │  Listener   │ ──▶ notification 表（最终落库）
                                      └─────────────┘
```

### 8.4 Outbox 实体设计（R1 修正：补全字段 + 厘清定位）

智慧校园的 `NotificationEventOutbox` 是一个**通用领域事件 outbox**（DDD 风格，带 aggregate 与 MQ 路由字段），**不只是"通知队列"**。完整字段如下：

| 字段 | 类型 | 说明 | 迁移建议 |
|---|---|---|---|
| `id` | Long(AUTO) | 主键 | 保留 |
| `eventLogId` | Long | 关联事件日志 | 可省（本项目无事件日志体系） |
| `aggregateType` | String | 聚合根类型（DDD，如 "POIShare"） | **建议保留**——支撑未来扩展到非通知事件 |
| `aggregateId` | Long | 聚合根 ID | **建议保留** |
| `notificationId` | Long | 生成的通知 ID | 保留 |
| `eventType` | String | 事件类型 | 保留 |
| `topicName` | String | MQ topic | 若只用单一 exchange 可省 |
| `routingKey` | String | MQ 路由键 | **建议保留**——支撑多事件类型路由 |
| `payloadJson` | String | 事件负载(JSON) | 保留 |
| `deliveryStatus` | Integer | 0待发/1成功/2失败 | 保留（状态机核心） |
| `attemptCount` | Integer | 尝试次数 | 保留 |
| `lastError` | String(500) | 最近错误 | 保留 |
| `nextRetryAt` | OffsetDateTime | 下次重试时间 | 保留 |
| `publishedAt` | OffsetDateTime | 成功投递时间 | 保留 |
| `createdAt` | OffsetDateTime | 创建时间 | 保留（用 `@PrePersist`） |
| `updatedAt` | OffsetDateTime | 更新时间 | 保留（用 `@PreUpdate`） |

> **迁移粒度建议**：保留 `aggregateType/aggregateId/routingKey`，让 outbox 成为"通知优先、可扩展"的通用事件表——这是把智慧校园的设计精髓真正吸收过来，而非降级为通知专用队列。

**新增 JPA 实体** `entity/NotificationEventOutbox.java`（⇄ 智慧校园同名校色，MyBatis 注解全换 JPA）：
- MyBatis `@TableName/@TableId/@TableField` → JPA `@Entity/@Table/@Id/@Column`
- MyBatis `FieldFill.INSERT/INSERT_UPDATE` → JPA `@PrePersist/@PreUpdate`（本项目 `AdminOperationLog` 已是这个风格，保持一致）

### 8.5 改造清单（源：智慧校园 Outbox 全套）

**新增 Repository** `repository/NotificationEventOutboxRepository.java`（替代智慧古城 Mapper）：
- 关键查询（⚠️ R2 修正：**不要用超长派生方法名**——`...NextRetryAtNullOrNextRetryAtBefore...` 这种 `NullOr` 组合在 Spring Data JPA 解析器里有歧义、会解析失败。改用 `@Query` JPQL + `Pageable` 控制条数）：
  ```java
  // 取待分发（PENDING 且到重试时间），null 的 nextRetryAt 优先
  @Query("select e from NotificationEventOutbox e where e.deliveryStatus = :status " +
         "and (e.nextRetryAt is null or e.nextRetryAt <= :now) " +
         "order by case when e.nextRetryAt is null then 0 else 1 end, e.nextRetryAt asc, e.id asc")
  List<NotificationEventOutbox> findPendingForDispatch(@Param("status") int status,
                                                       @Param("now") OffsetDateTime now,
                                                       Pageable pageable);
  // 取失败重试
  List<NotificationEventOutbox> findByDeliveryStatusOrderByUpdatedAtAscIdAsc(int status, Pageable pageable);
  ```
  调用时传 `PageRequest.of(0, batchSize)` 限量；`Pageable` 比 `findTopN` 派生查询更可控。

**新增 Dispatcher** `service/impl/NotificationOutboxDispatcherServiceImpl.java`：
- 逻辑完整移植智慧校园（三态 PENDING/SUCCESS/FAILED、批量 batchSize、失败 nextRetryAt=now+retry-delay、lastError 截断 500）。
- `@Scheduled(fixedDelayString="${app.notification.outbox.dispatch-interval-ms:15000}")` 扫描 PENDING。
- **关键适配**：MyBatis `mapper.updateById(event)` → JPA `repository.save(event)`。

**新增 Publisher** `service/NotificationEventPublisher.java` + impl：
- 把 outbox 事件序列化为 JSON，发到 RabbitMQ exchange。
- `@ConditionalOnProperty` 开关：RabbitMQ 未启用时走 mock（保证可降级开发）。

**新增 AmqpConfig** `config/NotificationAmqpConfig.java`（直接移植）：
- TopicExchange + 死信交换机 DLX + 主队列（绑定 DLX）+ 死信队列 DLQ。
- `@ConditionalOnProperty(prefix="app.notification.outbox.rabbitmq", name="enabled", havingValue="true")`。

**新增 Listener** `messaging/NotificationEventRabbitListener.java`：
- `@RabbitListener(queues="${app.notification.outbox.rabbitmq.queue}")` 消费事件 → 反序列化 → 写 `Notification` 表。
- 消费失败由 DLX 自动路由到死信队列，避免无限重试。

**改造 `NotificationServiceImpl.sendNotification(...)`**（核心、唯一业务改动点，配合前置 B 反转事务）：
```java
// ⚠️ 必须保留原"不给自己发通知"守卫（sendNotification 原第 36 行）
if (recipientId.equals(actorId)) return;
if (userRepository.findById(recipientId).isEmpty()) return;

// After：写 outbox（REQUIRED，与业务同事务）
NotificationEventOutbox event = NotificationEventOutbox.builder()
    .eventType(type).recipientId(recipientId).actorId(actorId)
    .aggregateType(targetType).aggregateId(targetId)
    .payloadJson(toJson(title, content, ...)).deliveryStatus(PENDING).build();
outboxRepository.save(event);
// 真正的 Notification 落库交给 Listener 异步完成
```
> 业务调用方（点赞/回复/关注等 Service）**完全无感知**——有机融合的最佳证明。

**新增管理接口** `controller/AdminNotificationOutboxController.java`：
- 查询 outbox 总览（待发/成功/失败计数）、手动重试单条/批量失败。

**pom.xml 依赖**：
```xml
<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-amqp</artifactId></dependency>
```

**配置项**（`application.properties`）：
```properties
app.notification.outbox.dispatcher-enabled=${OUTBOX_DISPATCHER_ENABLED:true}
app.notification.outbox.batch-size=20
app.notification.outbox.retry-delay-seconds=120
app.notification.outbox.dispatch-interval-ms=15000
app.notification.outbox.rabbitmq.enabled=${RABBITMQ_ENABLED:true}
app.notification.outbox.rabbitmq.exchange=discover.notification.event
app.notification.outbox.rabbitmq.queue=discover.notification.event.queue
app.notification.outbox.rabbitmq.dead-letter-exchange=discover.notification.event.dlx
app.notification.outbox.rabbitmq.dead-letter-queue=discover.notification.event.dlq
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
```

> ⚠️ **多实例前瞻风险**：`@Scheduled` 在多实例部署下会**每个实例都扫 outbox**，可能重复投递。本项目当前单实例无此问题；若未来容器多副本，需引入分布式锁（如 ShedLock）或让消费端幂等。作为前瞻性风险记录，初版可不处理。

**消费落库约定（R2 补充，源设计澄清）**：
- 智慧校园的消费链路实为 `Listener → NotificationEventConsumerService.consumeEvent(...)`，且通过 **AMQP message headers** 传递 `notificationId` 等字段，不是 Listener 直接写表。
- 迁移到本项目时，二选一：
  - **方案 A（简化，推荐初版）**：把 `payloadJson` 里塞全量字段（recipientId/actorId/type/title/content/targetType/targetId），Listener 直接反序列化并 `notificationRepository.save(...)`，不引入 ConsumerService 中间层。
  - **方案 B（贴近源设计）**：照搬 ConsumerService，事件经 MQ headers 路由再落库——适合未来多事件类型扩展。
- `NotificationEventOutbox.notificationId` 字段语义：outbox 写入时为 null，**消费成功后回填**（标记"已生成通知"），用于幂等与对账。

**数据库迁移脚本（R2 补充，易遗漏）**：
- 在本项目 db 初始化脚本里新增：
  ```sql
  CREATE TABLE notification_event_outbox (
    id BIGSERIAL PRIMARY KEY,
    aggregate_type VARCHAR(50), aggregate_id BIGINT,
    notification_id BIGINT, event_type VARCHAR(50),
    routing_key VARCHAR(100), payload_json JSONB,
    delivery_status SMALLINT NOT NULL DEFAULT 0,
    attempt_count INT NOT NULL DEFAULT 0,
    last_error VARCHAR(500), next_retry_at TIMESTAMPTZ, published_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
  );
  CREATE INDEX idx_outbox_dispatch ON notification_event_outbox(delivery_status, next_retry_at);
  ```
- 升级项② 的 `admin_operation_logs` 字段扩展也要同步 `ALTER TABLE`。
- 本项目主键风格用 `SEQUENCE`（参见 `AdminOperationLog` 的 `@SequenceGenerator`），outbox 实体保持一致，不要混用 `BIGSERIAL`/`IDENTITY`。

### 8.6 验收标准
- [ ] 点赞/回复触发通知后，`notification_event_outbox` 表立即出现 PENDING 记录。
- [ ] Dispatcher 周期内记录转为 SUCCESS，`notifications` 表出现最终记录，用户端可见。
- [ ] 人为停掉 RabbitMQ，outbox 记录累积为 FAILED；恢复后 `retryFailedEvents` 自动重投成功。
- [ ] 模拟消费异常，消息进入死信队列不丢失。
- [ ] 关闭 `rabbitmq.enabled`，系统退回 mock 模式仍可发通知（降级）。
- [ ] **业务回滚时，对应的 outbox 记录也回滚**（验证前置 B 事务反转生效）。

### 8.7 简历价值（本项目最核心的工程深度亮点）
> 引入 RabbitMQ Outbox 模式重构通知链路：业务同事务写 outbox 保证事件与业务一致，Dispatcher 定时扫描 + 指数重试 + 死信队列保障最终一致性，将点赞/回复/关注/路线等 6 类通知从同步直写升级为异步可靠投递；业务调用方零改动。

---

## 9. 持久层适配专项（JPA ⇄ MyBatis-Plus 转换表）

> 这是整个融合过程中最容易踩坑的地方，单独成章。

| 智慧校园（MyBatis-Plus） | 本项目（JPA） | 说明 |
|---|---|---|
| `@TableName / @TableId / @TableField` | `@Entity / @Table / @Id / @Column` | 实体注解全换 |
| `BaseMapper<T>` + `LambdaQueryWrapper` | `JpaRepository<T, ID>` + 派生查询 / `@Query` / Specification | 查询方式重构 |
| `mapper.insert/updateById/selectList` | `repository.save/findAll/findByXxx` | CRUD API 改写 |
| `FieldFill.INSERT / INSERT_UPDATE`（MetaObjectHandler） | `@PrePersist / @PreUpdate` | 本项目 `AdminOperationLog` 已用 `@PrePersist`，新实体沿用 |
| `mapper/*.xml`（如有） | 无 | MyBatis XML 在本项目无对应物，复杂查询统一用 `@Query` |
| `.last("LIMIT n")` | `findTopNBy...` 派生查询 | 派生查询更安全（避免 SQL 注入与方言问题） |

> ⚠️ Outbox 的 `selectList(LambdaQueryWrapper(...).last("LIMIT n"))` 必须转写为 Spring Data 的 `findTop20By...` 派生查询，不要在 `@Query` 里手拼 LIMIT。

---

## 10. 分阶段实施路线图

| 阶段 | 目标 | 交付物 | 预估工作量 |
|---|---|---|---|
| **M0 Docker 化前置** | 升级项⓪ | docker-compose.yml（含 PostGIS/Redis/RabbitMQ/Prometheus/Grafana） | 0.5～1 天 |
| **M1 可观测性打底** | 升级项① | traceId 贯通 + Prometheus 指标 + Grafana 大盘 | 0.5～1 天 |
| **M2 登录风控** | 升级项③ | 登录失败锁定上线 | 0.5 天 |
| **M3 审计统一** | 升级项② | `@OperationLog` 注解 + 切面 + Admin Service 重构 + 前台关键行为覆盖 | 1.5～2 天 |
| **M4 通知 Outbox** | 升级项④ | outbox 实体/Dispatcher/Publisher/Listener + @EnableScheduling + sendNotification 改造 + 事务反转 | 2～3 天 |
| **M5 收尾** | 简历同步 + 文档 | 更新 README、补架构图、刷新简历项目②描述 | 0.5 天 |

> 每个里程碑完成后，运行一次回归：后端 `mvnw compile` + 前端 `npm run build` + 关键接口手测。

---

## 11. 简历项目②描述（融合后建议文案）

> **地点探索与打卡分享平台** ｜ 全栈开发 ｜ 2025.10–2026.01
> 技术栈：Spring Boot 4、Vue 3、PostgreSQL/PostGIS、Hibernate Spatial、高德地图 API、RabbitMQ、Prometheus + Grafana、Docker Compose
> - 独立构建面向大众用户的地图化地点探索与内容分享社区，集成 Hibernate Spatial + PostGIS 实现空间数据存储与地理范围检索/路线规划；设计 29 个数据实体、37 个后端 Controller、25 个前端页面；Docker Compose 一键部署 7 服务（PostGIS + Redis + RabbitMQ + Prometheus + Grafana + 前后端）。
> - 引入 **RabbitMQ Outbox 模式**重构通知链路（同事务写 outbox + 定时扫描 + 失败重试 + 死信队列 DLX），将点赞/回复/关注等 6 类通知升级为异步可靠投递，保障最终一致性，业务调用方零改动；配套 **21 个单元测试**覆盖三态状态机与重试逻辑。
> - 基于 **Spring AOP + 自定义注解 + SpEL** 实现声明式操作审计，切面统一补齐用户/IP/traceId/耗时/成功失败，**REQUIRES_NEW 独立事务**保障失败记录不丢失，覆盖后台治理与前台关键行为全链路。
> - 集成 **Prometheus + Grafana + traceId 全链路追踪**实现服务可观测；实现**登录风控**（Redis 失败计数 + 定时锁定 + 本地内存降级双保险）防御暴力破解；设计 **`revoke_before` 时间戳方案**实现登出 Token 即时吊销（按用户维度，存储成本低于逐 token 黑名单）。

---

## 12. 风险与回滚策略

| 风险 | 应对 |
|---|---|
| Spring Boot 4.0.3 下 actuator/micrometer/amqp starter 行为变化 | 每加一个依赖先单独跑启动 + 冒烟测试；关注 jakarta 命名空间一致性 |
| **忘记 `@EnableScheduling`**（隐蔽） | M4 第一件事就是加，并写测试验证 Dispatcher 周期触发 |
| **Outbox 事务反转遗漏**（隐蔽） | sendNotification 改造时显式删掉 `REQUIRES_NEW`；验收增加"业务回滚→outbox 回滚"用例 |
| Outbox 引入消息延迟，用户感知通知"变慢" | Dispatcher 默认 15s 扫描，可按需调小；关键场景可"同步直达 + 异步兜底"双写过渡 |
| RabbitMQ 不可用拖垮系统 | 全链路 `@ConditionalOnProperty` 开关 + mock 降级；DLX 防止无限重试 |
| AOP 切面吞没业务异常 | 切面仅"记录"，`@Around` 必须 `throw ex`，不得吞异常 |
| `@Scheduled` 多实例重复投递（前瞻） | 当前单实例无此问题；未来多副本需 ShedLock 或消费端幂等 |
| 重构 Admin Service 引入回归 | 渐进式迁移，一个 Service 改完即测；保留 `record()` 手动入口过渡 |
| PostGIS 镜像选错 | 必须用 `postgis/postgis` 镜像，不能用普通 `postgres` |

**回滚**：每个升级项独立、开关可控（`app.notification.outbox.rabbitmq.enabled` / `app.security.login-risk-control-enabled` 等），任一项出问题可配置级关闭。

---

## 13. 明确不做清单（避免过度设计）

为防止"为了用而用"，以下能力**不迁入**本项目：

- ❌ **Token 黑名单（智慧校园 `TokenBlacklistService`）**：本项目已有更优的 `revoke_before` 时间戳方案（按用户维度吊销，存储成本更低）。**保留现有方案**。
- ❌ **MyBatis-Plus / `MetaObjectHandler`**：本项目 JPA 体系成熟，混用两套持久层得不偿失。
- ❌ **动态菜单 / 角色权限树（智慧校园 `PermissionGuard` + `MenuService`）**：本项目是"前台 C 端 + 运营后台"双形态，角色简单（SUPER_ADMIN/ADMIN/USER），套用校园 RBAC 过度设计。
- ❌ **校园业务模块（教务/学工/宿舍/通知模板）**：与地图社区定位完全无关。
- ❌ **智慧校园的明文密码 `application.yml`**：本项目已环境变量化，配置管理更优，保持现状。
- ❌ **`NotificationScheduleDispatchJob`（定时通知）**：它属于"到点发送的定时通知"功能（如活动提醒），与 Outbox 投递是两套独立机制，不在本次 Outbox 迁移范围。

---

## 附录 A：源借鉴文件索引（智慧校园项目）

| 能力 | 源文件路径 | 备注 |
|---|---|---|
| AOP 审计 | `.../aspect/OperationLogAspect.java` | 切点改为注解驱动；**SB4 实测**：AOP starter 改名 `spring-boot-starter-aspectj`（原 `spring-boot-starter-aop`，BOM 只管理新名） |
| 链路追踪 | `.../config/RequestTraceFilter.java` + `.../util/RequestTraceContext.java` | 直接移植 |
| 可观测性 | `.../config/ObservabilityConfig.java` + `application.yml` 的 `management.*` | **SB4 已实测**：`MeterRegistryCustomizer` 迁至 `o.s.b.micrometer.metrics.autoconfigure`（见 5.3 实测结论）；`InfoContributor` 路径不变 |
| Docker 蓝本 | `smart_campus_management/docker-compose.yml` | 扩展 + 改 PostGIS 镜像 |
| Outbox 投递 | `.../entity/NotificationEventOutbox.java` / `.../mapper/NotificationEventOutboxMapper.java` / `.../service/impl/NotificationOutboxDispatcherServiceImpl.java` / `.../messaging/NotificationEventRabbitListener.java` / `.../config/NotificationAmqpConfig.java` / `.../controller/NotificationOutboxDispatchController.java` | Mapper→JPA 转写 |
| 登录风控 | `.../service/impl/LoginRiskControlServiceImpl.java` | 直接移植 |

> ⚠️ R1 修正：`NotificationScheduleDispatchJob.java` **已从 Outbox 索引移除**——它调用 `dispatchDueScheduledNotifications()`，属于"定时通知"功能，与 Outbox 投递调度是两套独立机制。

## 附录 B：本项目改造落点索引（地点探索项目）

| 能力 | 落点（新增/修改） |
|---|---|
| Docker 化 | **新建** `docker-compose.yml`（项目根目录，含 PostGIS/Redis/RabbitMQ/Prometheus/Grafana/backend/frontend） |
| 可观测性 | 新增 `util/RequestTraceContext`、`config/RequestTraceFilter`、`config/ObservabilityConfig`；改 `pom.xml`、`application.properties` |
| AOP 审计 | 新增 `annotation/OperationLog`、`aspect/OperationLogAspect`；改 `entity/AdminOperationLog`、各 `Admin*ServiceImpl` |
| 登录风控 | 新增 `service/LoginRiskControlService(Impl)`；改 `AuthServiceImpl.login`、`application.properties` |
| 通知 Outbox | **改主启动类加 `@EnableScheduling`**；新增 `entity/NotificationEventOutbox`、`repository`、`service/impl/NotificationOutboxDispatcherServiceImpl`、`service/NotificationEventPublisher`、`config/NotificationAmqpConfig`、`messaging/NotificationEventRabbitListener`、`controller/AdminNotificationOutboxController`；改 `service/impl/NotificationServiceImpl`（写 outbox + **删 `REQUIRES_NEW`**）、`pom.xml` |

---

## 修订记录

### R1（2026-06-22）全文回溯审计修正
经对两个项目源码逐条核对，修正 8 处问题：

| # | 问题类型 | 原文档错误 | 修正 |
|---|---|---|---|
| 1 | 🔴 硬错误 | 假设本项目有 `docker-compose.yml`，多处"修改"它 | 本项目无 Docker 配置；新增"升级项⓪ Docker 化前置"，改为新建（且必须用 PostGIS 镜像） |
| 2 | 🔴 硬错误 | 称本项目"无 Token 黑名单"，建议迁移智慧校园方案 | 本项目已有更优的 `revoke_before` 方案；删除迁移，升级项③ 缩减为纯登录风控 |
| 3 | 🔴 硬错误 | AOP 切点表述"针对 `controller.Admin*` 包" | 本项目 Controller 平铺无子包；切点改为注解驱动 |
| 4 | 🔴 硬错误 | Outbox 实体字段表残缺（少 6 字段） | 补全 `eventLogId/aggregateType/aggregateId/notificationId/topicName/routingKey`，并厘清"通用领域事件"定位 |
| 5 | 🔴 硬错误 | 附录把 `NotificationScheduleDispatchJob` 列入 Outbox 全套 | 它属"定时通知"，与 Outbox 独立；移出 Outbox 索引 |
| 6 | 🔴 隐蔽坑 | 未提 `@EnableScheduling` 前置 | 本项目主启动类只有 `@EnableAsync`；Outbox `@Scheduled` 必须显式开启调度 |
| 7 | 🔴 隐蔽坑 | 未指出 `sendNotification` 的 `REQUIRES_NEW` 与 Outbox 同事务要求矛盾 | 明确事务反转：删 `REQUIRES_NEW` 改 `REQUIRED`，否则破坏事件一致性 |
| 8 | 🟡 表述 | 称需"新增 `AuthController.logout`" | logout 已存在（`@DeleteMapping("/logout")`）；配合现有 revoke_before 即时失效 |

### R2（2026-06-22）第二轮独立复审（实施工程师视角）
从「内部一致性 / 可执行性 / 技术深度 / 业务正确性」四视角复审，修正 9 处：

| # | 问题类型 | 问题 | 修正 |
|---|---|---|---|
| 1 | 🔴 技术错误 | JPA 派生查询方法名 `...NextRetryAtNullOr...` 有解析歧义，会解析失败 | 改 `@Query` JPQL + `Pageable` 限量 |
| 2 | 🔴 示例错误 | `deleteShare` 参数顺序写反 | 实际为 `deleteShare(shareId, operatorUserId)` |
| 3 | 🔴 事实订正 | Outbox 业务场景误含"举报" | 实为点赞/回复/关注/POI申请/成就/路线 6 类，**举报不触发通知** |
| 4 | 🟠 补充 | 消费链路被简化为"Listener 直接写表" | 澄清源设计是 Listener→ConsumerService，给出简化方案 A / 贴源方案 B |
| 5 | 🟠 补充 | AOP 注解加在"被内部调用的 Service"上会取不到用户 | 明确注解适用边界 + operatorUserId 统一从 SecurityContext 取 |
| 6 | 🟠 补充 | `sendNotification` 的"不给自己发"守卫 | 改造示例显式保留该业务规则 |
| 7 | 🟠 补充 | Outbox 建表 DDL / 迁移脚本缺失 | 补 `notification_event_outbox` 建表 SQL + 索引 + 主键风格 |
| 8 | 🟡 表述 | 章节展开顺序(按领域)与实施顺序(M0–M4)易混淆 | 加说明，施工以 M0–M4 为准 |
| 9 | 🟡 表述 | 登录参数误称 `username` | 实为 `identifier`（邮箱/用户名均可），风控 key 同步订正 |

**R2 复审中加固的关键论据**：经核实，地点探索**所有通知统一走 `sendNotification` 单一入口、全项目无人绕过**（仅其内部一处 `new Notification()/save`）——这让 Outbox「单点改造、全局生效」的论证比初版更扎实。

### M1（2026-06-23）实施记录与 SB4 实测
- **交付物**：新增 `util/RequestTraceContext`、`config/RequestTraceFilter`、`config/ObservabilityConfig`（简化版，删除智慧校园 token-blacklist/session-governance/menu-cache 等业务标志引用）；改 `pom.xml`（+`spring-boot-starter-actuator` +`micrometer-registry-prometheus`）、`application.properties`（+`management.endpoints.web.exposure.include` +`management.endpoint.prometheus.enabled` +日志 pattern 含 `[%X{traceId:-}]` +`spring.application.name=discover-share-places`）。
- **核验**：`./mvnw compile` **exit=0**（SB4 兼容硬证据）；包路径/`com.example` 残留/配置/依赖静态核验全通过。
- **SB4 关键发现（已回写 5.3 与附录 A）**：`MeterRegistryCustomizer` 包路径从 `actuate.autoconfigure.metrics` 迁移到 `micrometer.metrics.autoconfigure`（新模块 `spring-boot-micrometer-metrics`）。
- **运行时验证待办**（需 `docker compose up` 后人工确认）：`GET /actuator/prometheus` 返回指标、响应头含 `X-Trace-Id`、日志行含 `[traceId]`、Grafana 查到 `http_server_requests_seconds` / `jvm_memory_used_bytes`。

### M2（2026-06-23）实施记录
- **交付物**：新增 `service/LoginRiskControlService` + `impl/LoginRiskControlServiceImpl`（移植，保留 Redis 优先 + ConcurrentHashMap 本地降级双保险）；改 `AuthServiceImpl.login`（链式拆命令式，插入 assertLoginAllowed/recordFailure/clearFailures；用户不存在也 recordFailure 防枚举）、`application.properties`（+4 配置项）。
- **适配点**：`assertLoginAllowed` 抛 `BusinessException(429)` 取代智慧校园的 `RuntimeException`（契合 discover 全局异常约定）。
- **核验**：`./mvnw compile` **exit=0**；`verifyLogin` 零调用方，确认只改 `login` 一处。
- **运行时验证待办**：5 次错密码→第 6 次锁定（Result{429}）、成功清零、关 Redis 降级本地。

### M3（2026-06-23）实施记录与 SB4 实测
- **交付物（M3-a 基础设施）**：新增 `annotation/OperationLog`、`aspect/OperationLogAspect`（@annotation 切点 + SpEL + SecurityContext 取 userId + traceId + REQUIRES_NEW 失败独立提交 + 审计落库容错）、`db/migration_admin_audit_enhance.sql`；改 `entity/AdminOperationLog`（+6 字段）、`AdminOperationLogService`（+12 参数重载）、`AdminOperationLogServiceImpl`（+REQUIRES_NEW 重载 + 共享 doRecord，旧 6 参数 REQUIRED 向后兼容）、`pom.xml`（+AOP）。
- **交付物（M3-b 迁移示范）**：`AdminContentServiceImpl` 的 `deleteShare`/`deleteReply` 手动 record → `@OperationLog`（summary 简化为 SpEL）。
- **核验**：`./mvnw compile` **exit=0**。
- **SB4 关键发现（第二个迁移点，已回写附录 A）**：AOP starter `spring-boot-starter-aop` 在 SB4 改名为 **`spring-boot-starter-aspectj`**（BOM 只管理后者，用旧名报 version missing）。
- **summary 语义权衡**：AdminUser/Announcement/Activity/ContentReport 的 summary 含 DB 数据（username/title/预览），SpEL 无法表达，按 6.4「无法用注解表达的场景」保留手动 record。
- **附带清理点（M5 收尾）**：`AdminContentServiceImpl.buildPreview` 迁移后成死代码、`adminOperationLogService` 字段在该类未使用。

### M4（2026-06-24）实施记录
- **交付物**：新增 `entity/NotificationEventOutbox`（JPA 14 字段，保留 aggregateType/aggregateId/routingKey，省 eventLogId/topicName）、`repository/NotificationEventOutboxRepository`（@Query JPQL + Pageable，规避 R2 的派生方法名歧义）、`config/NotificationAmqpConfig`（TopicExchange+DLX+Queue+DLQ+2 Binding，@ConditionalOnProperty）、`service/NotificationEventPublisher`+`impl`（ObjectProvider<RabbitTemplate> + mock 降级）、`service/NotificationOutboxDispatcherService`+`impl`（@Scheduled 三态状态机）、`messaging/NotificationEventRabbitListener`（消费→反序列化 payloadJson→写 Notification 表，失败抛异常触发 DLX）、`controller/AdminNotificationOutboxController`（overview/retry）、`db/migration_notification_outbox.sql`；改 `SmartCampusBackendApplication`（+@EnableScheduling）、`NotificationServiceImpl.sendNotification`（**删 REQUIRES_NEW 改 REQUIRED** + 保留"不给自己发"守卫 + 写 outbox 替代同步 save）、`pom.xml`（+spring-boot-starter-amqp）、`application.properties`（+outbox 配置 + Listener retry 策略 3 次→DLQ）。
- **核验**：`./mvnw compile` **exit=0**（M4-a/b/c 三段分别编译通过）；事务反转/守卫/@EnableScheduling/@Scheduled/DLX/@ConditionalOnProperty 静态核验全通过；8 文件无 `com.example` 残留。
- **SB4 兼容**：amqp starter 名字未变（`spring-boot-starter-amqp`），无迁移点。
- **运行时验证待办**（需 `docker compose up` rabbitmq 后确认）：① 点赞/回复/关注等 6 类通知触发后 outbox 有 PENDING；② Dispatcher 15s 扫描发布；③ Listener 消费后 Notification 表有记录；④ 失败事件 retry 3 次路由 DLQ。
- **本地开发注意**：无 RabbitMQ 时设 `RABBITMQ_ENABLED=false`（AmqpConfig/Listener 不生效、Publisher mock 降级），否则 Dispatcher 把事件标记 FAILED 堆积。
- **docker-compose 补丁待办**：backend 环境变量需补 `SPRING_RABBITMQ_HOST: rabbitmq`（容器内连 rabbitmq 服务名），否则连默认 localhost 失败。

### M5（2026-06-24）收尾清理
- **docker-compose 补丁（M4 待办落地）**：backend 环境变量补 `SPRING_RABBITMQ_HOST: rabbitmq` + `SPRING_RABBITMQ_PORT/USERNAME/PASSWORD`，容器内连 rabbitmq 服务名。
- **敏感默认值清理**：`application.properties` 的 `MAIL_USERNAME`/`MAIL_PASSWORD`/`app.email.from` 默认值（真实邮箱 `2183846277@qq.com` + QQ 授权码 `qamcgjfdglzmdiji`）改为空，真实值由环境变量或 `application-local.properties`（gitignore）注入。`src/main/resources` 全量扫描确认无授权码残留。
- **死代码清理**：`AdminContentServiceImpl` 删除 M3-b 迁移后无调用的 `buildPreview` 私有方法 + 未使用的 `adminOperationLogService` 字段与 import。其他类的 `buildPreview` 是各自独立方法，未动。
- **配置完整性**：`.env.example` 补 `RABBITMQ_ENABLED` 开关（本地无 RabbitMQ 时设 false，Publisher mock 降级）。
- **核验**：`./mvnw compile` **exit=0**；grep 确认无敏感残留、死代码已清。

---

**审计中确认无误的关键事实**（保留）：Spring Boot 4.0.3 / Java 21 / JPA；29 实体；37 Controller；`AdminOperationLog` 字段与 `record()` 6 参数签名；`sendNotification` 同步直写；`AuthService/Impl` 存在；智慧校园 OperationLogAspect / ObservabilityConfig / RequestTraceFilter / RequestTraceContext / Outbox Dispatcher 三态逻辑 / LoginRiskControlService / NotificationAmqpConfig 内容均核实无误。

---

*本文档为活文档，每个里程碑完成后请回填"实际落地差异"与"踩坑记录"至修订记录。*
