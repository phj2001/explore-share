# 地点探索 AI 原生平台（Spring AI）· 面试题库（100+ 问，含高分参考答案）

> 站在资深大厂面试官（AI 应用 / Java 后端 / 全栈方向）视角，基于本项目**真实代码**逐条设计。
> 每题给出「面试官为什么问 / 高分回答要点 / 加分项 or 追问」。
> 面试铁律：**照代码讲，讲清 What → Why → Trade-off，尤其是真实踩坑与工程取舍。**
> 关联文件已在答案中标注，追问时可直接定位。

**项目一句话定位**：已上线（map.jieai.shop）的地图化地点探索/打卡分享社区，在其上做了两轮升级——① 工程化（通知 Outbox、可观测、登录风控、AOP 审计、Docker）② AI 原生（Spring AI 2.0 对话式探索助手）。全栈 Spring Boot 4 + Java 21 + Vue 3。

> ⚠️ **命名遗留（面试务必主动说明）**：目录/包名带 `smart-campus`（包 `com.smartcampus`），是项目前身遗留，**产品实为"地点探索"，包名不改是为了避免大规模重构风险**。这是个可以主动交代的工程决策点。

**规模**：36 数据实体、39 后端 Controller、25 前端页面 + 运营后台；AI 助手已完成 4832 个 POI 向量化入库。

---

## 目录

- A. 项目定位与整体架构（Q1–Q10）
- B. Spring AI 2.0 与模型接入（Q11–Q20）
- C. 空间约束混合检索（差异化核心）（Q21–Q34）
- D. Agent 与 Tool Use（Q35–Q46）
- E. 流式 vs 非流式兼容坑（Q47–Q54）
- F. 语义缓存（Q55–Q66）
- G. 多轮记忆与个性化（Q67–Q75）
- H. 限流 / 护栏 / 安全（Q76–Q85）
- I. LLM-as-judge 评测（Q86–Q92）
- J. 通知 Outbox 可靠投递（Q93–Q106）
- K. 可观测 / 登录风控 / AOP 审计（Q107–Q117）
- L. JWT 安全 / Spring Security（Q118–Q125）
- M. PostGIS / 数据 / 部署（Q126–Q134）
- N. 深挖 / 压力测试 / 反问（Q135–Q145）

---

## A. 项目定位与整体架构

### Q1. 用一分钟介绍这个项目。

**高分回答**：这是一个**已经上线、有真实用户**的地图化地点探索社区（map.jieai.shop），用户能在地图上发现地点、打卡、分享、规划路线、社交关注。技术上是 Spring Boot 4 + Java 21 + Vue 3 全栈，用 Hibernate Spatial + PostGIS 做空间数据存储和地理检索。

在这个已上线平台之上我做了**两轮升级**：第一轮是**工程化**（把通知改造成 Outbox 可靠投递、加 traceId+Prometheus 可观测、登录风控、AOP 操作审计、Docker 化）；第二轮是**AI 原生化**——用 Spring AI 2.0 落地一个对话式 AI 探索助手，核心是**空间约束的混合检索**（地理围栏 + pgvector 语义排序）+ Tool Use。

**加分**：强调这不是玩具，是**上线运行、在真实系统上做增量升级**——考验的是在存量系统上做演进的能力，比从零搭 demo 更难。

---

### Q2. 为什么包名是 smart-campus 但产品是地点探索？（必被问）

**高分回答**（坦诚）：这是**历史遗留**。项目前身是一个校园管理系统，后来产品方向转型为面向大众的地点探索社区，但包名 `com.smartcampus`、模块名 `smart-campus-backend` 保留了。**我刻意没改包名**——因为改包名要动几百个文件的 import、重构风险高、收益低（用户看不到包名）。这是个**成本-收益的工程判断**：把精力花在真正有价值的功能升级上，而不是无收益的大规模重命名。面试时我会主动交代，避免面试官误以为是校园项目。

**加分**：体现"不为洁癖做高风险重构"的工程成熟度。

---

### Q3. 这个项目的业务闭环有多完整？

**高分回答**：覆盖完整社区闭环——**36 个数据实体、39 个 Controller、25 个前端页面**：
- **POI 域**：地点 CRUD、分类、申请审核、地图点位、边界查询、收藏、评价、图库、签到；
- **内容域**：分享（含图片、标签、点赞、回复）、推荐分享、内容举报治理；
- **路线域**：用户路线、推荐路线、路线规划、途经点；
- **社交域**：关注、粉丝、公开主页、Feed 流、排行榜、成就；
- **运营后台**：用户管理、内容审核、举报处理、系统配置、操作日志、通知 Outbox 监控、数据看板；
- **基础**：认证（JWT+邮箱验证码）、通知、公告、活动。

不是单点功能，是一个**能运营的完整产品**。

---

### Q4. 这个项目和项目一（LangGraph 遥感 Agent）的差异化在哪？为什么两个都做？

**高分回答**（简历核心卖点）：刻意做成**两套主流生态的 AI 落地**：
- **项目一**：Python / LangGraph，**离线多智能体分析**，通用文本混合检索；
- **项目二**：Java / Spring AI，**在线对话式应用**，地图域**空间约束**语义检索。

差异化体现能力广度：LLM/Agent 应用我在 **Python 和 Java 两套生态都独立落地过**，且都做到可观测、可评测、可部署的生产级完整度。RAG 上也不同——项目一是通用语义检索，项目二是"地理围栏内的语义推荐"，是地图域独有的 RAG。面试官问"只会 Python 吧"时，这就是有力回应。

---

### Q5. AI 助手是怎么嵌入到已有平台的？架构上怎么隔离？

**高分回答**（`assistant` 包 + `@ConditionalOnProperty`）：AI 能力全部收在独立的 `com.smartcampus.assistant` 包里（config/controller/service/repository/tool/dto 分层），**用 `@ConditionalOnProperty(prefix="app.assistant", name="enabled", havingValue="true")` 统一开关**——`app.assistant.enabled=false` 时整个 AI 模块**不装配**，平台完全回退到纯业务系统，零影响。

好处：AI 是**可插拔的增强模块**，不侵入核心业务。工具层还**复用现有 Service**（POIService/RouteService 等），不新建业务逻辑。这是在存量系统加 AI 的正确姿势——**隔离 + 复用 + 可一键关闭**。

---

### Q6. `app.assistant.enabled` 这个开关的意义？关掉会发生什么？

**高分回答**：它是**特性开关（feature flag）**。关掉时，所有标了 `@ConditionalOnProperty` 的 AI Bean（Controller/Service/Config/Tool）都不装配，`/api/assistant/**` 接口返回 404。意义：
1. **成本控制**——AI 调用花钱，能一键停；
2. **故障隔离**——AI 出问题（key 失效、供应商挂）不影响主平台；
3. **灰度**——可以按环境开关。

踩坑（交接文档记的）：这个开关和 key 必须写在**配置文件**（application-local.properties），不能靠 `$env`——因为 `$env` 只对单个终端有效、重启即丢，曾导致模块没加载报 404 反复横跳。这是真实踩过的坑。

---

### Q7. 你的 AI 助手落地分了几个阶段（M0-M7）？

**高分回答**（体现工程规划）：按里程碑推进：M0 ChatClient 最小装配、M1 POI 全量嵌入入库、M2 空间约束检索、M3 Agent 对话+Tool Use、M4 生产级（语义缓存+限流+护栏+指标）、M6 LLM-as-judge 评测、M7（升级方案 P1/P2）多轮记忆+个性化。每个阶段都本地验证通过（嵌入 4832 POI、检索正常、对话+工具正常、缓存秒回、限流 429、护栏 400、指标可见）。**分阶段交付**而不是一把梭，每步可验证——这是靠谱的工程节奏。

---

### Q8. 这个 AI 助手真正的技术难点是什么？

**高分回答**：三个：
1. **空间约束混合检索**——不是普通 RAG，要先地理围栏（边界框+Haversine）再语义排序，让"推荐"限定在用户可达范围内（见 C 部分）；
2. **Spring AI 2.0 流式 + 工具调用与 Qwen 的兼容性 bug**——`.stream()` + Tool Calling 抛 `NoSuchElementException`，我用非流式 call() + 分块回放规避（见 E 部分）；
3. **生产级工程化**——语义缓存的时效性绕过、越权防护（ToolContext 传 userId）、限流护栏、评测。

难点不在"调通 LLM"，在"让它在真实地图业务里可用、安全、可控、省钱"。

---

### Q9. 简历里说"已上线、有真实用户"，上线部署是怎么做的？

**高分回答**（`docker-compose.yml` + `ops/`）：Docker Compose 一键编排**七个服务**：PostGIS（含 pgvector）、Redis、RabbitMQ、Prometheus、Grafana、前端、后端，全配置环境变量化 + 健康检查依赖编排。域名 map.jieai.shop。**部署踩坑（交接文档）**：上线走 compose 的容器 `discover` 库是空的，必须把 POI 数据 + 向量灌进去，并在生产环境设 `ASSISTANT_ENABLED`、`DASHSCOPE_API_KEY`，否则"检索不到 POI"。诚实讲，AI 助手在生产的 POI 向量需要单独灌，这是部署时要记得的一步。

---

### Q10. 本地和生产是两套数据库？怎么避免搞混？（真实踩坑）

**高分回答**（交接文档明确记录）：是两套，**这是个真实踩过的坑**：
- 本地后端连**宿主机 PostgreSQL 的 `campus_map` 库**（`application-local.properties`）；
- `docker compose` 那套后端连**容器里的 `discover` 库**（初始空）。

调试一律用 campus_map。而且**向量表要手动建**——`02-pgvector.sql`（poi_embedding）、`03-assistant-cache.sql`（assistant_cache）只在容器 discover 首次初始化自动跑，宿主 campus_map 需手动执行这两段 SQL。缺表时代码 **fail-open** 当未命中，不报错但功能不生效（很隐蔽）。这个坑说明：**多环境的库结构一致性要主动保证**，而 fail-open 设计虽然保证不崩，但也可能掩盖"表没建"这种问题——是个双刃剑。

---

## B. Spring AI 2.0 与模型接入

### Q11. 为什么用 Spring AI 2.0？它和直接调 OpenAI SDK 有什么区别？

**高分回答**（`AssistantConfig`）：选 Spring AI 2.0 因为它是**唯一兼容 Spring Boot 4 的版本**（SB4 较新，生态兼容性有限，这是硬约束）。区别：Spring AI 提供 `ChatClient` 统一抽象——自动装配、`@Tool` 注解式工具绑定、`toolContext` 传参、Advisor 机制、和 Spring 生态（DI、配置、Micrometer）无缝集成。直接调 SDK 要自己处理工具调用协议、消息组装、重试。Spring AI 把这些**框架化**了，符合 Java 项目"约定优于配置"的习惯。代价是版本兼容性受限、踩到框架 bug 时（见流式坑）比较被动。

---

### Q12. ChatClient 是怎么装配的？底层连的是谁？

**高分回答**（`AssistantConfig`）：`@Bean ChatClient chatClient(ChatClient.Builder builder)` 基于 Spring AI 自动装配的 Builder 构建。底层 `OpenAiChatModel` 由 `spring-ai-starter-model-openai` 自动配置，通过 `spring.ai.openai.*` 配置**指向 DashScope 的 OpenAI 兼容端点**。所以我用的是"OpenAI 协议的客户端 + DashScope 的服务"——DashScope 提供 OpenAI 兼容 API，Spring AI 的 OpenAI starter 直接能用，不需要专门的 DashScope SDK。这是**面向协议编程**，换供应商只改配置。

---

### Q13. 为什么用 Qwen/DashScope 而不是 OpenAI？

**高分回答**：几个现实原因：国内**网络可达、合规**；DashScope 提供 **OpenAI 兼容端点**，Spring AI 的 OpenAI starter 直接对接，迁移成本低；同时提供 **LLM + Embedding（text-embedding-v4，1024 维）**，一个 key 覆盖对话和嵌入。选型主线和项目一一致——国内可用 + 兼容 OpenAI 协议 + 一个 key 全能力。

---

### Q14. Embedding 用的什么模型？维度多少？为什么维度要一致？

**高分回答**：DashScope `text-embedding-v4`，输出 **1024 维**。两张向量表 `poi_embedding` 和 `assistant_cache` 都是 `vector(1024)`，和模型输出维度严格一致。**为什么必须一致**：pgvector 的向量列维度是固定的，插入维度不匹配的向量会直接报错；而且**检索时 query 向量和库里 POI 向量必须同模型同维度**，否则在不同的向量空间里算余弦距离没有意义（`AssistantRetrievalService` 注释专门写了"必须与 POI 入库同模型，向量空间才一致"）。这是向量检索的铁律——**查询和入库必须用同一个 embedding 模型**。

---

### Q15. POI 是怎么被向量化的？用什么文本？

**高分回答**（`EmbeddingService.buildContent`）：语料 = **name + category + description**，用全角分隔符 `｜` 连接（如"星巴克｜咖啡馆｜安静适合办公"）。选这三个字段因为它们是 POI 的**主体语义**——名称、类别、简介足以表征"这是个什么地方"。嵌入用 `OpenAiEmbeddingModel.embed`，经 `PoiEmbeddingRepository.upsert` 入 `poi_embedding` 表。已完成 **4832 个 POI** 入库。

---

### Q16. 4832 个 POI 全量嵌入是怎么做的？会不会很慢/被限流？

**高分回答**（`EmbeddingService.embedAll`）：**批量嵌入**——`batchSize=9`，一次 `embeddingModel.embed(List)` 处理一批（结果顺序与输入一致），大幅减少 API 往返、规避 QPS 限流。batchSize 取 9 是**保守值**（远低于 DashScope 批量上限），在吞吐和限流风险间取平衡。**容错**：单批失败计入 fail 不影响其余批（`try/catch` 每批），返回 `EmbedStats(total, success, fail)` 统计。由管理员端点 `POST /admin/embed-all` 触发。设计要点：**批处理省调用 + 单批失败隔离**。

---

### Q17. 向量表为什么用 JdbcTemplate 而不是 JPA？

**高分回答**（`PoiEmbeddingRepository` 注释）：因为 **Hibernate/JPA 不认识 pgvector 的 `vector` 类型**。所以向量表的读写全部用 `JdbcTemplate` 原生 SQL，`float[]` 和 vector 列之间通过 `"[v1,v2,...]"` **字面量 + `::vector` cast** 转换（`toVectorLiteral`）。而普通业务实体（POI/User 等）仍用 JPA。这是**混合持久化**——常规实体用 JPA 享受 ORM 便利，特殊类型（向量、空间）用原生 SQL 精确控制。同理 POI 的空间列 geom 用 Hibernate Spatial。

---

### Q18. upsert 是怎么实现的？为什么用 ON CONFLICT？

**高分回答**（`PoiEmbeddingRepository.upsert`）：`INSERT ... ON CONFLICT (poi_id) DO UPDATE SET content=EXCLUDED.content, embedding=EXCLUDED.embedding, updated_at=now()`。用 PostgreSQL 的 `ON CONFLICT`（原生 upsert）：poi_id 已存在就更新向量、不存在就插入。为什么：POI 会被编辑（改了描述要重新嵌入），upsert 让"全量嵌入"可**重复执行且幂等**——重跑 embed-all 不会产生重复行，而是刷新。比先 select 再 insert/update 少一次往返且无并发竞态。

---

### Q19. 增量嵌入做了吗？POI 改了向量会自动更新吗？

**高分回答**（诚实）：`EmbeddingService` 有 `refresh(poiId)` 单点刷新入口，但**没有在 POIService 的写路径自动埋点**——POI 增改后需要**手动调 embed-all** 或 refresh。这是已知的待办（交接文档列了"增量嵌入未接线"）。当前是"全量重建"模式。要做增量：在 POI 创建/更新的 service 里调 `embeddingService.refresh(poi.getId())`，或用定时任务扫描变更。我没做的原因是 POI 变更频率低、全量重建成本可接受，属于优先级判断；但这确实是可以补强的点。

---

### Q20. 如果 DashScope 欠费或 key 失效，AI 助手会怎样？用户体验如何？

**高分回答**（真实踩坑，交接文档）：key 缺失会导致 500，模块没加载会 404——这曾因为把 key 写在 `$env` 里重启丢失而反复出现。**正确做法**是把 key 和开关写在配置文件。用户体验兜底：`AssistantController.chat` 的 SSE 里，`error ->` 分支会给前端发 "助手出错了，请稍后再试" 而不是裸露异常；前端 `AssistantChat.vue` 应有"助手未启用/暂时不可用"的降级提示（升级方案 P0 项）。诚实讲，最理想是加一条**部署后自动冒烟测试**（调一次固定 query，失败告警），避免 key 失效导致功能静默不可用——这是我认可但还没完全做的防护。

---

## C. 空间约束混合检索（差异化核心）

### Q21. 你的"空间约束混合检索"到底是什么？完整讲一遍。

**高分回答**（`AssistantRetrievalService.search`，本项目 AI 最核心，必考）：**三段式「先地理围栏、再语义排序」**：
1. **边界框预过滤**：`GeoUtils.calculateBounds(lat, lng, 半径km)` 算出中心点半径圆的**外接正方形**边界框，用 `POIRepository.findWithinBounds`（经纬度 BETWEEN 查询）拿到框内 POI；
2. **Haversine 精确过滤**：边界框是正方形，四角超出真实圆半径，用 `GeoUtils.haversineMeters` 算每个点到中心的**真实球面距离**，`> radiusMeters` 的剔除，同时记录距离；
3. **候选集内语义 Top-K**：把问题嵌入成向量，用 `PoiEmbeddingRepository.findSimilar(candidateIds, queryVector, topK)` **只在圆内的 POI id 集合里**做余弦近邻排序。

结果 = 圆内候选总数 + 语义 Top-K 命中（按相关度降序，带真实距离）。

**一句话**：这是"**地理围栏内的语义推荐**"——先约束到用户能去的范围，再在范围内按语义相关度排，区别于项目一的全局通用语义检索。

---

### Q22. 为什么要"先地理围栏再语义"？反过来不行吗（先语义召回再过滤距离）？

**高分回答**（关键设计题）：不行/不好。因为这是**地点推荐**场景——**距离是硬约束**。用户问"附近安静的咖啡馆"，10 公里外语义再匹配的咖啡馆也没用。
- **先语义后过滤**：要先在全库几千个 POI 里做语义 Top-K，可能 Top-K 全在圆外，过滤完啥都不剩，还得反复扩大 K，浪费且不稳定；
- **先围栏后语义**：先把候选缩到"用户可达范围"（几十个），再在这个小集合里做语义排序——**既保证距离约束，又缩小了向量检索范围**（`findSimilar` 只在 candidateIds 内算，快）。

本质：**硬约束（距离）优先过滤，软偏好（语义）在约束内排序**。这是推荐系统的通用范式——先 filtering 再 ranking。

---

### Q23. 边界框和 Haversine 为什么要两步？只用其中一个不行吗？

**高分回答**（很能体现深度）：两步各司其职、互补：
- **只用边界框**：边界框是圆的**外接正方形**，四个角超出真实半径（角上的点距中心是 √2×半径），会把圆外的点误纳入——**不精确**；
- **只用 Haversine**：要对全库每个点算 Haversine 距离，**没有索引、全表扫描**，慢；

所以：**边界框做粗筛（走经纬度 BETWEEN，可走 B-tree 索引，快速砍掉绝大部分）→ Haversine 做精筛（只对框内少量点算精确距离，剔除四角）**。这是"廉价粗筛 + 精确细筛"的两阶段过滤，和 RAG 的"召回+精排"是同一种思想。

---

### Q24. 为什么不直接用 PostGIS 的 `ST_DWithin`？（必被追问的取舍题）

**高分回答**（诚实的工程取舍，简历面试准备里专门列了）：这是个**主动的工程取舍**，不是不知道 ST_DWithin。原因：
1. **复用现有查询**：平台原本就有基于经纬度 BETWEEN 的 `findWithinBounds`（POI 地图点位查询），直接复用，不引入新的空间查询路径；
2. **避免额外依赖耦合**：向量表 `poi_embedding` 是 JdbcTemplate 独立管理的，如果用 ST_DWithin 要把空间查询和向量查询耦合，架构上更复杂；
3. **Haversine 精度足够**：对"米级距离过滤"，Haversine 和 ST_DWithin 精度相当。

**如果面试官追问更优方案**：我会大方承认——**PostGIS `geography` 类型 + `ST_DWithin` 是更专业的方案**，它有 GiST 空间索引、能一步完成"圆域内"查询、精度用椭球模型更高。我的方案是"复用存量 + 快速落地"的取舍，代价是自己实现了距离过滤。**能说清取舍的两面，比假装自己的方案最优更加分**。

---

### Q25. `calculateBounds` 是怎么算边界框的？

**高分回答**（`GeoUtils.calculateBounds`）：`latDelta = radius/111.0`（1 纬度≈111km），`lngDelta = radius/(111.0*cos(lat))`（经度间距随纬度收窄，要除以 cos 纬度）。返回 `[minLat, maxLat, minLng, maxLng]`。关键点：**经度的度-公里换算要考虑纬度**——赤道上 1 经度≈111km，但高纬度会缩短（乘 cos），不修正的话边界框会算歪。这是地理计算的常见细节。注意单位：这个函数半径单位是**千米**，而检索服务传的是米，所以 `radiusMeters/1000.0`（代码里有注释强调这个单位陷阱）。

---

### Q26. Haversine 公式算的是什么？为什么用它而不是欧氏距离？

**高分回答**（`GeoUtils.haversineMeters`）：Haversine 算**球面两点的大圆距离**（地球是球面，不能用平面欧氏距离）。地球半径取 6371000m。经纬度是球面坐标，直接算欧氏距离在大范围会有明显误差（尤其经度方向）。Haversine 考虑了地球曲率，对几百米到几十公里的距离精度很好。代码里还有 `Math.min(1.0, sqrt(a))` 防止浮点误差让 asin 参数略超 1 导致 NaN——这是数值稳定性的细节。

---

### Q27. `findSimilar` 为什么要限定在候选 id 集合里？SQL 怎么写的？

**高分回答**（`PoiEmbeddingRepository.findSimilar`，差异化核心）：
```sql
SELECT poi_id, content, 1-(embedding <=> ?::vector) AS score
FROM poi_embedding
WHERE poi_id IN (候选id列表)
ORDER BY embedding <=> ?::vector LIMIT ?
```
`WHERE poi_id IN (...)` 就是**把语义近邻限定在地理围栏筛出的候选集内**——这正是"空间约束"与"语义检索"结合的落点，也是和项目一"全局语义检索"的核心区别。`<=>` 是余弦距离，`1-距离`=相似度。候选为空直接返回空列表（防御）。IN 用动态占位符拼（id 是可信的 Long，无注入风险）。

---

### Q28. 圆内没有 POI 会怎样？返回错误吗？

**高分回答**：不是错误，是**正常空结果**。`within.isEmpty()` 时返回 `new RetrievalResult(query, 0, List.of())`（候选数 0、空命中），并记 info 日志。业务语义："这个位置附近半径内没有相关地点"是合理的正常情况（用户在荒郊野外），不该报错。Tool 层 `recommendNearbyPlaces` 拿到空结果会返回自然语言"附近没找到相关地点，建议扩大范围或换关键词"，让模型优雅告知用户。**区分"错误"和"空结果"**是 API 设计的基本功。

---

### Q29. embedding 调用失败了怎么处理？

**高分回答**（`AssistantRetrievalService` 注释）：`embeddingModel.embed(query)` 失败时**向上抛**，由 M4 护栏层/Controller 统一兜底（SSE 的 error 分支给用户友好提示）。设计选择：检索是核心步骤，embedding 失败意味着无法语义排序，与其返回半成品不如明确失败让上层处理。这和语义缓存的 fail-open（失败当未命中）不同——**缓存失败可以静默降级（不影响主流程），但主检索失败要暴露**。区分哪些依赖失败该降级、哪些该抛出，是可靠性设计的关键判断。

---

### Q30. 检索的半径和 topK 是怎么配置的？

**高分回答**：`AssistantProperties.getRetrieval()` 里配 `radiusMeters`（默认半径）和 `topK`。检索方法接受 `radiusOverride`，null 时取配置默认。工具 `recommendNearbyPlaces` 的 `radiusMeters` 参数也可让模型按需传（"再远一点"时模型可传更大半径）。配置化让半径/TopK 可调而不改代码。默认值是产品体验和成本的平衡——半径太大候选多、语义排序噪声大；topK 太大 prompt 太长。

---

### Q31. 这套检索和项目一的混合 RAG 相比，为什么没有 BM25/RRF/rerank？

**高分回答**（诚实对比）：因为**场景不同，不需要那么重**。项目一是政策文档问答，文档长、术语多，需要 BM25 补语义盲区、rerank 精排。项目二是**POI 推荐**——POI 语料短（name+category+description），主要靠**语义相似 + 地理约束**就够；而"地理围栏"本身就是最强的"精排"（把不相关的远处点全砍了）。硬加 BM25/rerank 收益小、复杂度高。所以我的取舍是：**项目二的"精排"是地理距离，不是 cross-encoder**。这体现"按场景选技术复杂度"，不是把项目一的方案照搬——**合适比复杂重要**。

---

### Q32. `RetrievalResult` 里为什么既返回候选总数又返回 Top-K？

**高分回答**（`RetrievalResult(query, candidateCount, hits)`）：`candidateCount`=圆内候选总数，`hits`=语义 Top-K。返回候选总数的用途：
1. **给模型/用户上下文**——工具输出会说"共 N 个候选，返回最相关的 K 个"，让用户知道范围内有多少地点；
2. **可观测**——日志里能看到"圆内候选 vs 返回数"，判断是"附近本来就没地方"还是"有地方但语义都不匹配"。

这是**信息完整性**设计——不只给结果，还给结果的"上下文"（从多少里选的）。

---

### Q33. 检索到的距离是怎么传到最终回答里的？为什么强调距离要照实？

**高分回答**：Haversine 算出的精确距离存在 `Candidate.distance`，`findSimilar` 返回后回查候选复用这个已算的距离（`PoiHit` 带 `distanceMeters`），不重复算。工具输出里"距你约 X 米"。**为什么强调照实**（system prompt 专门写了一大段）：LLM 有个坏毛病——会把多个地点的距离**写成同一个值或编造**。所以 prompt 里反复强调"每个地点距离严格引用工具返回值，逐个如实写，不同地点距离通常不同，严禁写成同一个值"。这是**防止 LLM 篡改工具返回的事实数据**——工具给了准确数字，模型不能自作主张改。

---

### Q34. 如果同一个位置反复搜同一个 query，每次都要重新算吗？

**高分回答**：不一定——有**语义缓存**（见 F 部分）。相同/相似 query 在同一地理桶内，缓存命中就直接回放历史回答，跳过整个检索+LLM 流程。但注意缓存是缓在**对话层**（AssistantChatService），缓的是最终回答；检索服务本身（`/retrieve` 接口）每次都算。设计上，昂贵的是"检索+LLM 生成"整条链，所以缓存放在最外层收益最大。这引出下一大块——语义缓存。

---

## D. Agent 与 Tool Use

### Q35. 你的 AI 助手有几个工具？分别做什么？

**高分回答**（`ExplorerTools`，5 个 `@Tool`）：
1. `recommendNearbyPlaces`——核心：空间约束+语义检索推荐附近地点（复用 `AssistantRetrievalService`）；
2. `searchPlaces`——按名称/类别精确查地点（复用 `POIService`）；
3. `planRoute`——两点路线规划，返回距离时长（复用 `RouteService`，高德）；
4. `listCategories`——列出所有地点类别，帮模型澄清模糊意图；
5. `getUserPreferences`——汇总当前用户收藏/签到的类别偏好，做个性化（复用 `AssistantUserPreferenceService`，userId 经 ToolContext 注入）。

设计原则：**全部复用现有 Service/Repository，不新建业务逻辑**——工具只是把平台已有能力用 `@Tool` 暴露给 ChatClient。

---

### Q36. `@Tool` 注解是怎么工作的？LLM 怎么知道调哪个工具？

**高分回答**：`@Tool(description=...)` 标注方法，`@ToolParam(description=...)` 标注参数，Spring AI 把这些**反射成 JSON Schema**（工具名、描述、参数类型/描述）随请求发给 LLM。LLM 根据用户意图和工具描述**自主决定调哪个、传什么参数**（Function Calling），Spring AI 收到 tool_call 后**反射调用对应 Java 方法**，把返回值喂回 LLM 继续生成。所以 `description` 写得好不好直接影响模型选对不选对——我的 description 都写了"什么时候用"（如"需要地点推荐时优先用本工具""问怎么走/路线时使用"）。

---

### Q37. 工具描述（description）你是怎么写的？为什么这么写？

**高分回答**：description 的核心是**告诉模型"何时该调我"**，不只是"我是干嘛的"。比如：
- `recommendNearbyPlaces`："...需要地点推荐时**优先**用本工具"——建立优先级；
- `getUserPreferences`："...**仅在用户需求宽泛、未说明具体偏好时**调用"——限定触发条件，防止过度调用。

配合 system prompt 里的路由规则（"附近推荐用 recommendNearbyPlaces、指名某地用 searchPlaces、问路线用 planRoute"）。**工具描述 + system prompt 双重引导**，让模型选工具更准。这是 Prompt Engineering 在 Tool Use 上的体现——工具描述本身就是 prompt。

---

### Q38. 工具执行失败了怎么办？会中断对话吗？

**高分回答**（`ExplorerTools` 每个工具的 catch，很重要）：**不中断**。每个工具都 `try/catch`，失败时**返回自然语言错误串**（如"推荐地点时出错了，请稍后再试"）而**不抛异常**。为什么：如果抛异常，整个 ChatClient 调用会失败、对话中断；返回错误串则**让模型能优雅兜底**——模型看到"出错了"会转告用户或换个方式。同时 catch 里 `recordToolError` 上报 Micrometer 失败计数，便于观测高德 API、POI 检索的隐性故障率。这是**工具健壮性**设计——工具的错误是"数据"不是"异常"，交给模型处理。

---

### Q39. 工具返回的文本会不会太长撑爆 prompt？

**高分回答**（`capResult` / `MAX_RESULT_CHARS`）：有防护。`MAX_LIST=10` 限制返回条数，`MAX_RESULT_CHARS=4000` 是**单次工具返回的硬上限**——超了截断加"内容过长已截断"。description 也 `truncate(desc, 60)` 截断单个地点简介。为什么：工具返回会拼进 prompt 再喂给 LLM，某条 description 异常长或结果太多会把 prompt 撑大（涨 token、可能超窗口）。这是**防御性兜底**——即使未来放宽条数，也不会因单条超长炸掉。护栏那块还提到要对工具返回做长度截断，就是这个。

---

### Q40. `getUserPreferences` 的 userId 是怎么传给工具的？为什么不让模型传？

**高分回答**（安全关键题，`ToolContext` 机制）：userId 经 **`ToolContext`** 由服务端注入，**不是模型可填的 `@ToolParam`**。链路：`AssistantChatService` 从 Controller 拿到登录用户 userId → `.toolContext(Map.of("userId", userId))` → 工具里 `extractUserId(toolContext)` 取出。

**为什么不让模型传**（核心）：如果 userId 是普通工具参数，用户可以在消息里说"我是 userId=1"诱导模型传别人的 id，**越权读取他人的收藏/签到数据**。ToolContext 只在 **Java 侧对工具方法可见，不出现在暴露给模型的 JSON Schema 里**，模型看不到也改不了。这是把"当前操作者是谁"这类**敏感上下文安全传给工具的标准做法**——身份信息绝不能经过模型可控的通道。

---

### Q41. 个性化推荐（getUserPreferences）具体怎么做？隐私怎么保护？

**高分回答**（`AssistantUserPreferenceService`）：工具复用现有的 `POIFavoriteService`/`POICheckInService`，返回用户**常收藏/签到的类别分布**（如"常收藏：咖啡馆 x5、图书馆 x3"），供模型作为推荐排序参考。隐私保护：
1. **只返回类别聚合**，不含具体时间戳/地址等敏感细节；
2. **只在需求宽泛时才调**（system prompt 规则 10 + description 限定），用户明确说"找咖啡馆"就不调，避免过度个性化打扰；
3. **userId 服务端注入**防越权（Q40）。

设计克制——个性化是"锦上添花"，不是每次都强推，也不泄露原始行为数据。

---

### Q42. 为什么个性化用工具（Tool）而不是直接把用户偏好拼进 system prompt？

**高分回答**（升级方案 P2 的设计考量）：拼进 system prompt 的问题：**每次对话都要查一次偏好、都塞进 prompt**，但并非每次提问都需要个性化（用户明确说"找咖啡馆"时不需要），白白消耗 token 和一次 DB 查询。用工具让模型**按需调用**——只在需求宽泛时才触发查询。这是**惰性/按需**的设计：把"是否需要个性化"的判断交给模型，而不是无条件预加载。体现了对 token 成本和调用效率的意识。

---

### Q43. 系统提示词（system prompt）你设计了哪些关键规则？

**高分回答**（`AssistantChatService.SYSTEM_PROMPT`，11 条）：核心几条：
- **双重身份**：既是地点探索专家，也是通用助手（地点外的日常问题也正常答，不以"我只管地点"拒绝）；
- **数据真实性**：地点必须通过工具获取真实数据，**数量内容与工具返回完全一致，绝不编造**；
- **距离照实**：严格引用工具返回的距离，不同地点不能写成同值；
- **位置使用**：用消息里给的用户位置，不臆造坐标；
- **时间**：用注入的当前时间答，不编；时效性信息（天气/新闻）坦诚无法获取；
- **多轮**：结合历史理解"这个/那个/再远一点"；
- **个性化**：仅需求宽泛时调 getUserPreferences；
- **语言**：全程简体中文，不夹英文，不输出工具原始 JSON。

设计主线：**防幻觉（不编地点/距离）+ 通用可用（不死板拒答）+ 体验（中文、简洁）**。

---

### Q44. 为什么 system prompt 里要强调"不编造地点"这么多次？

**高分回答**：因为这是**地点推荐场景最致命的失败模式**——LLM 的知识里"知道"很多地方（比如它训练数据里的连锁店），会自信地推荐一个**平台里根本没有、或不在用户附近**的地点。用户按它推荐去了扑空，直接摧毁信任。所以 prompt 反复强调"工具返回几个推荐几个、绝不补充虚构、哪怕你知道某地也不行"。这也是评测里 `groundedness`（依据性）指标要量化的——**推荐的地点是否都来自检索候选**。防幻觉在推荐系统里是产品红线。

---

### Q45. 当前时间是怎么给到模型的？为什么要注入？

**高分回答**（`buildUserContent`）：把服务器当前时间格式化成"2026-07-04 14:30（星期五）"注入 user 消息。为什么：**LLM 不知道"现在"是几点**（它的知识有截止日期，且无实时时钟），不注入的话时间类问题必然编造或拒答。注入后，"现在几点""今天星期几""这个点还营业吗"能如实回答。同样用户位置（lat/lng）也注入 user 消息。这是**给模型补充它没有的实时上下文**——时间和位置是它天生缺失的，必须外部喂。

---

### Q46. 多个工具可以在一次对话里连续调用吗（多步 Tool Use）？

**高分回答**：可以。Spring AI 的 ChatClient 支持**多轮工具调用**——模型可以先调 `listCategories` 澄清、再调 `recommendNearbyPlaces` 推荐、甚至对推荐结果调 `planRoute` 规划路线，这个编排**由 LLM 自主完成**，不需要我写编排逻辑（升级方案 P4 提到"周末半日游"这类多步组合，LLM 本身就能编排，主要靠 system prompt 引导）。这是 Agent 相比固定流程的优势——**多步工具调用的编排是模型的自主能力**。我要做的是把工具描述和 system prompt 写好，让它编排得对。

---

## E. 流式 vs 非流式兼容坑（真实踩坑，高频考点）

### Q47. 你的对话是流式的吗？前端看到的是逐字输出吗？

**高分回答**（诚实且有亮点）：前端体验是流式的（SSE 逐块输出），但**底层不是真流式**——这里有个真实的工程坑。Spring AI 2.0 的 `.stream()` + Tool Calling 对 DashScope/Qwen 存在兼容性 bug（抛 `NoSuchElementException: No value present`）。我改用**非流式 `.call()` 拿到完整回答，再分块"流式回放"给前端**（`splitForReplay` 按 30 字符切块，SSE 逐块发）。所以前端看着是流式，实际是"伪流式回放"。这是**保留工具能力 + 前端流式体验的工程取舍**，不是教科书方案。

---

### Q48. 为什么 `.stream()` + Tool Calling 会报 NoSuchElementException？

**高分回答**（能讲清底层更加分）：根因是 **Spring AI 2.0 对 DashScope 流式响应里工具调用增量的解析有 bug**。流式模式下，工具调用（tool_calls）是**分片增量返回**的（function name、arguments 一点点吐），Spring AI 在拼接/解析这些增量时，某个环节取值遇到空的 Optional 就 `NoSuchElementException: No value present`。本质是**框架对"流式 + 工具增量"这个组合的兼容性没做好**，尤其 DashScope 的兼容端点和标准 OpenAI 的流式工具协议有细微差异。纯流式对话（不带 tools）或纯非流式带 tools 都没问题，**唯独"流式 + tools"这个组合触发**。

---

### Q49. 你的"非流式 call() + 分块回放"具体怎么实现的？

**高分回答**（`AssistantChatService.stream`）：
1. 用 `chatClient.prompt().system(...).messages(history).user(...).tools(explorerTools).toolContext(...).call().content()` **非流式**拿到完整回答字符串（工具调用在这一步稳定完成）；
2. `splitForReplay(reply)` 把回答按 `CACHE_REPLAY_CHUNK=30` 字符切成块列表；
3. `Flux.fromIterable(chunks)` 返回，Controller 的 SseEmitter 逐块 `emitter.send`；
4. 整个阻塞调用放在 `Schedulers.boundedElastic()` 线程执行，`Flux.defer` 延迟到订阅时执行——**避免阻塞请求/事件循环线程**。

所以前端 EventSource 收到的是一段段增量文本，体验和真流式一致。

---

### Q50. 这样做牺牲了什么？为什么可接受？

**高分回答**（取舍分析）：牺牲的是**首字延迟（TTFB）**——真流式是模型边生成边吐字，第一个字很快到；我这个要**等模型完整生成 + 工具调用全部完成**才开始回放，首字延迟 = 完整生成时间。换来的是**工具调用的稳定性**（不崩）。

为什么可接受：
1. 地点推荐的回答通常不长（几句话 + 几个地点），完整生成也就一两秒，首字延迟增加有限；
2. 分块回放让用户**视觉上仍是流式**，不是干等一坨；
3. 工具能力是核心（没工具就没法推荐真实地点），**稳定性 > 首字延迟**。

这是典型的"两害相权取其轻"——为了核心功能的稳定，牺牲一点可感知性很低的延迟。

---

### Q51. 为什么要用 boundedElastic 线程？不用会怎样？

**高分回答**（Reactor 关键）：`.call()` 是**阻塞** IO（等 LLM 响应几秒）。如果直接在 Reactor 的**事件循环线程**或请求线程上跑这个阻塞调用，会**占住宝贵的少量事件循环线程**，并发一高线程被占满、整个服务卡死。`Schedulers.boundedElastic()` 是专门给**阻塞任务**用的弹性线程池，把阻塞调用甩到这里跑，不阻塞事件循环。`Flux.defer` 保证这段逻辑延迟到订阅时才执行。这是 Reactor 里**隔离阻塞调用**的标准做法——阻塞和非阻塞线程必须分开。

---

### Q52. 如果以后 Spring AI 修复了这个 bug，你会怎么改回真流式？

**高分回答**（升级方案 P3，有前瞻）：分两步：
1. **先验证**——升级 `spring-ai-starter-model-openai` 或 DashScope 兼容模式的新版本，测"流式 + tools"是否已修复。Spring AI 迭代快，很可能已经修了；
2. **若已修**：直接把 `.call()` 换回 `.stream()`，改动很小（`stream()` 返回 `Flux<String>`，正好对接现有 SSE）；
3. **若仍未修**：退而求其次做**两阶段**——工具调用阶段用非流式拿工具结果，最终自然语言生成单独用 `.stream()`，即"先执行 tool calls，再流式生成回答文本"，让最耗时的文本生成是真流式。

优先级：这个对体验提升有限（伪流式已经"看起来"是流式），排在多轮记忆/个性化之后。**知道怎么改回去 + 知道优先级**，说明是主动踩坑不是被动绕开。

---

### Q53. 用户中途关闭页面（SSE 断开），后台的 LLM 调用会继续跑吗？

**高分回答**（`AssistantController` 的 Disposable，很细致）：会被**取消**。Controller 里 `chatService.stream(...).subscribe(...)` 返回 `Disposable subscription`，然后注册了三个回调：`emitter.onTimeout / onError / onCompletion` 都调 `subscription.dispose()`。作用：**连接终止（超时/客户端断开/正常完成）时取消上游订阅**，否则 emitter 断开后，boundedElastic 上的 LLM 阻塞调用仍会跑完——**白烧 token 且占用线程**。正常完成时 dispose 是幂等 no-op。这是**资源及时释放**的细节——客户端走了就别再为它烧钱。

---

### Q54. SseEmitter 的超时设多少？为什么？

**高分回答**：`new SseEmitter(120_000L)` = 2 分钟。为什么：一次对话（含工具调用、可能多步）最长也就几十秒，2 分钟是宽松上限，防止异常情况下连接无限挂着占资源。超时触发 `onTimeout`→dispose 订阅 + complete。设太短会误杀正常的长回答，太长则异常连接占用久。2 分钟是经验上的安全值。护栏那层还有请求前的限流/校验，双重保护。

---

## F. 语义缓存

### Q55. 语义缓存是什么？解决什么问题？

**高分回答**（`AssistantSemanticCache`）：用 pgvector 实现**"相似问题命中"**的缓存——把问题向量按地理位置分桶存进 `assistant_cache` 表，新问题在**同桶内做余弦近邻**，相似度 ≥ 阈值就**直接复用历史回答**，跳过整个 LLM + 工具调用链。解决的问题：**降本 + 提速**。热门问题（"附近有什么好吃的"）反复被问，每次都走完整 LLM+检索链既慢又贵；语义缓存让相似问题**秒回**，显著降低 LLM 调用成本和延迟。

**加分**：它是"语义"缓存不是"精确"缓存——"附近的咖啡馆"和"附近有咖啡店吗"字面不同但语义相同，都能命中。

---

### Q56. 为什么按"地理位置分桶"而不是全局缓存？（关键设计）

**高分回答**（必考，简历面试准备专门列了）：因为**同一个问题在不同地点答案完全不同**。"附近有什么咖啡馆"这句话，在西安钟楼问和在延安问，正确答案是两组不同的地点。如果全局缓存，在钟楼问过一次，延安的用户问同样的话会命中钟楼的答案——**答非所问**。

所以缓存 key 要**感知地理位置**：`locBucket(lat, lng)` 把经纬度保留 3 位小数（≈110m）分桶，缓存按区域隔离，只在**同一地理桶 + 语义相似**双命中才复用。这是地图域缓存的必然设计——**位置是答案的一部分，必须进 key**。

---

### Q57. 地理分桶为什么是 3 位小数（约 110m）？粒度怎么定的？

**高分回答**：经纬度小数点后 3 位≈110m（1 度≈111km，0.001 度≈111m）。粒度权衡：
- **桶太大（粒度粗）**：跨度几公里都算一个桶，但几公里外的"附近"答案已经不同了，会命中不准的缓存；
- **桶太小（粒度细）**：每个用户位置都是独立桶，几乎命不中，缓存失去意义。

110m 是个平衡——它接近"附近推荐"的有效邻域尺度，在这个范围内"附近的咖啡馆"答案基本一致，又足够粗能让邻近用户共享缓存。诚实讲，这是启发式取值，理想应结合检索半径调（半径大桶可以大点）。

---

### Q58. 时效性问题（"现在几点"）为什么必须绕过缓存？怎么绕的？

**高分回答**（`isCacheable` / `TIME_SENSITIVE_PATTERN`，很出彩的设计）：因为时效性问题的答案**随时间变化**——"现在几点"缓存 24 小时（TTL 内）会重放旧时间，"今天天气"隔天就错。这类问题进缓存是**严重错误**（答错基本事实）。

怎么绕：`TIME_SENSITIVE_PATTERN` 正则匹配"几点|几号|时间|今天|明天|现在|星期|天气|新闻|股价|汇率|比分"等特征词，命中就 `isCacheable=false`——**既不查缓存也不写缓存**。原则：**"宁可漏缓存（多花一次 LLM 调用），不可错缓存（答错事实）"**。这体现了对"什么能缓、什么不能缓"的精细判断，不是无脑全缓。

---

### Q59. 缓存命中的相似度阈值怎么定？定高定低有什么影响？

**高分回答**（`similarityThreshold`）：可配。阈值权衡：
- **阈值太高**（如 0.98）：几乎只有几乎一模一样的问题才命中，命中率低，缓存价值小；
- **阈值太低**（如 0.7）：语义差挺远的也命中，可能"附近的咖啡馆"命中"附近的餐厅"的缓存——**答非所问**。

要设在"语义确实等价"的分界上（通常 0.9+）。宁可偏高（漏命中最多多花钱），不可偏低（错命中答错）。这和时效性绕过是同一个保守原则——**缓存宁缺毋滥**，因为一次错误回答的代价远大于一次缓存 miss。

---

### Q60. 缓存的 SQL 怎么查的？怎么同时满足"同桶 + 时间未过期 + 最相似"？

**高分回答**（`AssistantSemanticCache.lookup`）：
```sql
SELECT response, 1-(embedding <=> ?::vector) AS score
FROM assistant_cache
WHERE loc_bucket = ? AND created_at >= now() - (? || ' seconds')::interval
ORDER BY embedding <=> ?::vector LIMIT 1
```
`WHERE loc_bucket=?` 同地理桶，`created_at >= now()-TTL` 时间未过期，`ORDER BY embedding<=>query LIMIT 1` 取最相似的一条。拿到后再判 `score >= threshold` 才算命中。一条 SQL 同时完成"桶过滤 + TTL 过滤 + 最近邻"，简洁高效。

---

### Q61. 缓存的 fail-open 是什么意思？为什么这么设计？

**高分回答**（`AssistantSemanticCache` 注释）：**fail-open = 缓存故障时"放行"而非"阻断"**。查询异常→当作未命中（走正常 LLM 流程）；写入异常→只记日志。甚至 `assistant_cache` 表根本没建（老库没跑 03 脚本），也不会中断对话。

为什么：缓存是**性能优化**不是**功能刚需**，它挂了最多是"变慢/变贵"，绝不该导致对话失败。所以任何缓存异常都静默降级。对比：主检索的 embedding 失败是要抛的（Q29），因为那是核心功能。**区分"优化性依赖"和"功能性依赖"**——前者 fail-open，后者 fail-fast。这个判断力是可靠性设计的核心。

---

### Q62. 缓存会无限增长吗？过期数据怎么清理？

**高分回答**（`evictExpired` + `@Scheduled`）：TTL 只在 lookup 时按 created_at 过滤，**行数只增不减**，所以加了**定时物理清理**：`@Scheduled(fixedDelay=1小时)` 定时 `DELETE FROM assistant_cache WHERE created_at < now()-TTL`，删掉过期行。为什么必须清理：不删的话 `assistant_cache` 无限膨胀，**拖慢 HNSW 索引和查询**。清理也 fail-open（失败记日志下轮重试）。依赖主启动类 `@EnableScheduling`。这又是"无界增长必须有清理"的卫生原则（和项目一 task_store 清理一致）。

---

### Q63. 缓存是跨用户共享的吗？会不会泄露隐私？

**高分回答**（`AssistantSemanticCache` 注释）：**跨用户共享**，但**不泄露隐私**。因为缓存的回答是**基于位置+问题的通用推荐**，M3 阶段没有把用户个性化信息注入回答（个性化在 M7 才加，且是工具按需调）。所以"钟楼附近的咖啡馆"这个回答对任何在钟楼的用户都一样，跨用户共享合理且无隐私问题。

**注意**：这也意味着**个性化对话不该走这个共享缓存**——M7 的多轮/个性化请求会**跳过语义缓存**（见 Q64），正是为了避免把 A 用户的个性化答案缓存给 B。设计上把"通用可共享"和"个性化不可共享"分开了。

---

### Q64. 加了多轮对话后，缓存逻辑为什么要改？

**高分回答**（`AssistantChatService.stream` 的 `cacheable` 判断，M7 关键）：因为**多轮场景下同一句话在不同上下文含义不同**。"再远一点的"这句话，脱离上文毫无意义，缓存它会灾难。所以规则改成：**`cacheable = history.isEmpty() && isCacheable(message)`**——**只有无历史的首轮请求才走缓存**，有历史的多轮请求一律跳过。

原因：语义缓存是按**单条 query** 做语义匹配的，多轮里"这个""远一点"这种指代词，单看这一句缓存会命中错误的历史答案（语义漂移）。所以"有上文就不缓存"。这是多轮记忆和语义缓存**冲突时的正确取舍**——保正确性，牺牲多轮场景的缓存收益。

---

### Q65. 缓存命中时，多轮记忆还会记录这一轮吗？

**高分回答**（`AssistantChatService.stream` 缓存命中分支）：会。缓存命中回放前，仍 `conversationMemory.append(conversationId, "user", message)` 和 `append(..., "assistant", cached)`——**把这一轮记进历史**。为什么：即使这次是缓存命中（首轮无历史才会命中），也要把它存进记忆，这样**下一轮追问**才有上下文。缓存只是省了"生成"这步，不能省"记录对话"这步。这是缓存和记忆两个机制的正确协作——命中缓存 ≠ 跳过记忆。

---

### Q66. 你改了 prompt 或逻辑后，怎么保证缓存不返回旧答案？（真实踩坑）

**高分回答**（交接文档明确记录的坑）：**必须手动清缓存**。改了提示词/工具逻辑后，语义缓存会**回放旧答案**——你以为改动没生效，其实是命中了旧缓存。解决：`psql -c "TRUNCATE assistant_cache;"` 清空。这是个真实踩过的坑——**缓存的存在会掩盖代码改动**，调试时容易误判"改了没用"。所以我在评测时也建议关缓存（`ASSISTANT_CACHE_ENABLED=false`），保证每次独立生成。这提醒：**有缓存的系统，改逻辑后要记得让缓存失效**，否则会得出错误的调试结论。

---

## G. 多轮记忆与个性化

### Q67. 多轮对话记忆是怎么存的？为什么用 Redis 而不是数据库表？

**高分回答**（`AssistantConversationMemory`）：用 **Redis List** 按 conversationId 存最近若干轮的 user/assistant 消息（JSON 编码 `Turn(role, content)`）。选 Redis 不建表的原因：**会话记忆是短期、可丢失的**——TTL 内有效即可，不需要跨设备持久化查询，也没有复杂关联查询需求。复用项目既有 Redis（和限流、风控同套设施），零新增数据库表。这是**按数据特性选存储**——短期易失数据用 Redis，业务实体才用 PostgreSQL。

---

### Q68. 历史记忆怎么防止无限增长和 token 膨胀？

**高分回答**（`AssistantConversationMemory.append`）：三重控制：
1. **只保留最近 N 轮**：`redisTemplate.opsForList().trim(key, -maxEntries, -1)`，maxEntries = `maxTurns * 2`（一问一答=2 条），每次追加后 trim，单会话不无限膨胀；
2. **TTL 过期**：`expire(key, ttlMinutes)`，会话闲置超时自动清（闲聊记录没必要长期留）；
3. **读取也限量**：`loadHistory` 用 `range(key, -maxEntries, -1)` 只取最近 N 轮。

为什么限轮数：历史要拼进 prompt 发给 LLM，全量历史会撑爆 token。`maxTurns`（建议 6）是"够理解追问"和"token 可控"的平衡。

---

### Q69. conversationId 是谁生成的？不传会怎样？

**高分回答**：**前端生成**——打开对话面板时生成一个 UUID，后续同一会话复用。不传（null/空）时，`loadHistory` 返回空列表，`stream` 里 `history.isEmpty()`，行为**退化为无历史的单轮对话**（和升级前一致）。设计上多轮是**可选增强**——传了 conversationId 就有记忆，不传就是单轮，向后兼容。fail-open 也在这体现：Redis 挂了、历史读失败，最坏退化成单轮，不让整个请求失败。

---

### Q70. 历史消息是怎么拼进 LLM 请求的？

**高分回答**（`AssistantChatService.stream`）：`chatClient.prompt().system(SYSTEM_PROMPT).messages(history).user(当前消息).tools(...).call()`——`.messages(history)` 把加载的历史 `List<Message>`（UserMessage/AssistantMessage 交替）塞在 system 之后、当前 user 消息之前。这样 LLM 看到"系统设定 + 历史对话 + 当前问题"，能理解"再远一点""这个"等指代。`parseLine` 把 Redis 里的 JSON 还原成 Spring AI 的 `UserMessage`/`AssistantMessage` 类型。

---

### Q71. 多轮记忆和项目一的 rewrite（指代消解）是一回事吗？

**高分回答**（跨项目对比，加分）：目标相同（理解追问），**手段不同**：
- **项目一**：用一个独立的 `rewrite` 节点，**先用 LLM 把"再远一点"改写成完整独立问题**，再送后续处理——是"**改写 query**"；
- **项目二**：**把历史消息原样塞进 prompt**（`.messages(history)`），让主 LLM 结合上下文自己理解——是"**给上下文**"。

两种都行。改写的好处是下游（意图分类/检索）拿到的是完整 query，缓存/日志更清晰；给上下文的好处是简单、不额外调 LLM。项目二选给上下文是因为对话式场景 LLM 直接理解上下文最自然，且省一次改写调用。能对比两个项目的不同设计说明我理解了本质而非套模板。

---

### Q72. 多轮记忆的 fail-open 具体怎么体现？

**高分回答**：`loadHistory` 和 `append` 都 try/catch，异常只 `log.warn` 不抛。`loadHistory` 失败返回空列表（等同无历史），`append` 失败静默忽略。所以 Redis 抖动/宕机时，**最坏情况是这次对话退化成单轮**（没记住上文），而不是整个请求 500。和语义缓存、限流一致——**所有"增强性"的 Redis 依赖都 fail-open**，因为它们挂了不该阻断核心对话。这是贯穿项目的可靠性原则。

---

### Q73. 如果用户在多轮里换了位置（走到别处继续问），历史里的旧位置会不会误导？

**高分回答**（好问题，展示边界思考）：历史消息里存的是文本内容（role+content），当前请求的位置是**每轮单独注入 user 消息**的（`buildUserContent` 每次用当前 lat/lng）。所以当前这轮用的是**当前位置**，历史只提供"聊过什么"的语义上下文。但确实有微妙情况：历史回答里提到的地点是基于旧位置的，如果用户追问"第一个怎么走"，模型可能用旧地点+新位置算路线。诚实讲这是多轮+位置变化的边界 case，当前没专门处理，靠 system prompt 引导"用消息里给的当前位置"。要更严谨可以在位置显著变化时提示或清历史。

---

### Q74. 个性化和多轮记忆是 M7 一起做的，它们冲突吗？

**高分回答**：不冲突，是**两个正交增强**——多轮记忆解决"理解追问"（Redis 存历史），个性化解决"贴合用户偏好"（工具查收藏/签到）。但它们和**语义缓存**都有冲突：有历史/个性化的请求都应跳过共享缓存（避免语义漂移和跨用户串答案）。所以我的处理是"**有历史的多轮请求跳过缓存**"（Q64）。个性化因为 userId 经 ToolContext 且只在需求宽泛时调，本身不进缓存 key。三个机制的协作边界我是理清了的：缓存管首轮通用问题、记忆管多轮上下文、个性化管偏好排序。

---

### Q75. 个性化数据（收藏/签到）本来就有，为什么之前 AI 没用？

**高分回答**（诚实，升级方案背景）：因为**M3 阶段 AI 助手从没读取过这些数据**——`POIFavorite`/`POICheckIn`/`POIReview` 一直存在于业务库，但助手的 Controller 拿到 userId 后从未透传给工具层。升级方案 P2 才补上：`AssistantController` 拿 userId → ChatService toolContext 透传 → `getUserPreferences` 工具查询。这是**在存量数据上挖增量价值**的典型——数据早就有，只是没接线。体现了"先看手头有什么数据再设计功能"的 data-centric 意识，而不是重新造数据。

---

## H. 限流 / 护栏 / 安全

### Q76. 为什么 AI 接口一定要限流？怎么实现的？

**高分回答**（`AssistantRateLimiter`）：因为 **AI 调用直接花钱**（每次 LLM/embedding 都计费），公开上线后若不限流，会被恶意或异常刷爆成本。实现：基于 **Redis 的"每用户每分钟"固定窗口计数**——key = `assistant:rl:{userId}:{epochMinute}`，`increment` 计数，首次计数时 `expire(70秒)`，超过 `perUserPerMin` 上限返回 false（Controller 抛 429）。按分钟分桶（epochMinute）实现窗口滚动。复用项目既有 Redis（和登录风控同套）。

**加分**：`/chat` 和 `/retrieve` 共用限流——因为检索也产生 embedding 计费调用，不能只限对话不限检索。

---

### Q77. 固定窗口限流有什么缺陷？你为什么还用它？

**高分回答**（体现对算法的理解）：固定窗口的缺陷是**窗口边界的突刺**——比如上限 10/分钟，用户在 00:59 打 10 次、01:00 又打 10 次，跨边界的 2 秒内实际打了 20 次。滑动窗口/令牌桶能平滑这个。我用固定窗口的原因：**实现极简（一个 Redis incr + expire）、够用**——对"防刷爆成本"这个目标，固定窗口的边界突刺无关紧要（本来上限就是防滥用的粗粒度阈值，多几次不影响成本量级）。这是**匹配需求选最简方案**——不为了"更高级"引入复杂度。项目一的 API 限流用的是滑动窗口（deque），两个项目按场景选了不同粒度。

---

### Q78. 限流的 fail-open 合理吗？被绕过怎么办？

**高分回答**（`AssistantRateLimiter` catch）：Redis 异常时 `return true`（放行）。合理性权衡：Redis 挂了是小概率，此时**放行保证正常用户能用**，代价是限流暂时失效（可能被刷）。选择 fail-open 是因为"因缓存故障阻断所有正常对话"的体验损失，大于"Redis 短暂宕机期间被刷"的成本风险。但这是有风险的选择——如果安全性要求极高，应该 fail-close（Redis 挂了就拒绝）。诚实讲这是**可用性优先**的取舍，前提是 Redis 足够稳定。护栏那层（注入检测）和鉴权是不 fail-open 的，多层防御下限流 fail-open 可接受。

---

### Q79. Prompt 注入护栏是怎么做的？能防住吗？

**高分回答**（`AssistantGuard`，要辩证答）：`check(message)` 做两件事：**长度限制**（超 `maxMessageLength` 抛 400）+ **注入启发式检测**——正则匹配"忽略以上指令""ignore previous instructions""system prompt""开发者模式/越狱/jailbreak""you are now""忘记你的身份"等常见注入模式，命中抛 400。

**能不能防住**（关键，别吹）：**这是轻量启发式，不是绝对安全**。正则能挡掉直白的注入，但挡不住变体、编码绕过、多语言变形。我的定位很清楚——它是**纵深防御的一层基础防线**，配合：① system prompt 的强约束、② 工具**只返回平台真实数据**（模型就算被越狱也调不出平台外的地点）、③ ToolContext 防越权。**单层护栏必然能绕，但多层组合提高了攻击成本**。诚实承认单层局限比吹"我能防注入"更专业。

---

### Q80. 护栏检测到违规为什么在"开流之前"就拒绝？

**高分回答**（`AssistantController.chat`）：护栏 `guard.check` 和限流 `rateLimiter.allow` 都在**请求线程、创建 SseEmitter 之前**执行。为什么：
1. **省资源**——违规/超限的请求根本不该开 SSE 流、不该起 LLM 调用，早拒早省；
2. **SecurityContext 可用**——请求线程里能拿到登录态（userId），护栏/限流需要 userId；如果拖到异步流里，SecurityContext 可能已经不在当前线程了。

所以顺序是：鉴权→限流→护栏→（都过了才）开流。**把校验放在最前面、失败快速返回**，是接口设计的基本功。

---

### Q81. 工具返回的内容需要护栏吗？（输出侧安全）

**高分回答**（诚实 + 前瞻）：输入侧有护栏（AssistantGuard），输出侧目前主要靠 **`capResult` 长度截断**（防 prompt 膨胀）。**输出内容安全（过滤违规/已封禁 POI）目前没做**——因为 POI 实体没有"状态/封禁"字段（交接文档列为待办）。这是已知缺口：如果某个 POI 被举报封禁，AI 仍可能推荐它。要补需要：给 POI 加状态字段，检索时过滤掉封禁的。我会诚实说这是输出治理的待完善点。好在工具**只返回平台内真实 POI**，不会凭空生成违规内容，风险相对可控。

---

### Q82. admin 接口（embed-all/evaluate）怎么做权限控制？

**高分回答**（`AssistantController`）：用 Spring Security 的方法级注解 **`@PreAuthorize("hasAnyRole('SUPER_ADMIN','ADMIN')")`**。`/admin/embed-all`（全量嵌入）和 `/admin/evaluate`（评测）都标了——因为这俩是重操作（嵌入几千 POI 耗成本、评测跑一堆 LLM 调用），必须限管理员。普通对话/检索接口只要登录态。权限分层：`/api/assistant/**` 需登录，admin 子路径额外要 ADMIN 角色。角色来自 JWT 里的 role claim，经 JwtAuthenticationFilter 注入 SecurityContext。

---

### Q83. 一个普通登录用户能不能绕过护栏直连 LLM？

**高分回答**（交接文档提到的历史问题）：现在不能。但**曾经有过风险**——M0 阶段有个 `GET /dev/ping` 验证接口，**任何登录用户能绕过护栏/限流直连 LLM**，存在成本滥用风险。M3 正式 `/chat` 上线时**移除了这个接口**（Controller 注释专门写了）。这说明：**开发期的调试接口必须在上线前清理**，否则就是后门。现在所有 AI 入口（chat/retrieve）都过限流+护栏+鉴权，没有旁路。这是个真实的安全意识案例——调试便利接口是常见的安全隐患。

---

### Q84. 你怎么防止用户通过对话套取系统 prompt 或越权？

**高分回答**（综合防御）：多层：
1. **套 system prompt**：护栏正则挡"system prompt""ignore instructions"等；即使套出来，system prompt 里没有敏感信息（就是行为约束），泄露危害小；
2. **越权读他人数据**：userId 经 **ToolContext** 服务端注入，模型无法伪造（Q40）——这是最关键的越权防护；
3. **调用平台外能力**：工具白名单固定 5 个，模型只能调这些，且都只返回平台数据；
4. **成本滥用**：限流 + 护栏长度限制。

核心思想：**不信任模型的任何输出，敏感操作（身份、权限）绝不经过模型可控的通道**。模型是"不可信的中间层"，安全边界建在模型两侧（输入护栏 + 工具侧身份注入）。

---

### Q85. 邮箱验证码、密码这些怎么防刷防泄露？

**高分回答**（`AuthService` + 登录风控）：注册/登录有**邮箱验证码**（`EmailService` 发码）；**登录风控**（`LoginRiskControlService`）——失败 N 次锁定（见 K 部分）；密码用 Spring Security 的 encoder 加盐哈希存储（不明文）；JWT 有过期 + 吊销机制（见 L 部分）。验证码本身应有发送频率限制和有效期。这些是认证安全的标配。诚实讲，验证码的防刷（同一邮箱/IP 频率限制）如果没做全，是可以补强的点，但登录风控 + JWT 吊销覆盖了主要的账户安全场景。

---

## I. LLM-as-judge 评测

### Q86. 你怎么评测 AI 助手的质量？

**高分回答**（`AssistantEvaluationService`，M6，呼应项目一方法论）：用**固定用例集 + LLM-as-judge** 量化推荐质量，产出两个指标：
- **relevance（相关性）**：回答是否切题、给出合适地点；
- **groundedness（依据性）**：回答推荐的地点是否**都来自检索候选**（防编造、隐含"在地理约束内"）。

12 个固定评测用例（覆盖推荐/类别/路线/模糊意图等典型表述），在给定 (lat,lng) 跑全部用例，每例：检索候选 → 生成回答 → LLM judge 打分 → 求平均。管理员端点 `POST /admin/evaluate?lat&lng` 触发。

---

### Q87. groundedness 这个指标为什么重要？怎么判？

**高分回答**（地点推荐的核心质量）：groundedness 直接量化**"AI 有没有编造平台外地点"**——这是地点推荐最致命的失败。判法（`judge` 的 JUDGE_SYSTEM）：给 judge LLM "用户问题 + 系统检索到的候选地点列表 + 助手回答"，让它判断"助手回答里提到的地点是否都在候选列表里"，列表外的疑似编造要扣分（如实说无结果则高分）。

为什么这样判：把"有没有编造"变成"回答地点 ⊆ 候选地点"的**可核对判断**——candidateNames 是 ground truth，judge 只需比对回答有没有超出这个集合。这比笼统问"答得好不好"客观得多。

---

### Q88. LLM 给 LLM 打分，你怎么保证 judge 靠谱？

**高分回答**（和项目一一致的思路）：几个措施：
1. **judge 有明确 ground truth**——groundedness 有"候选列表"当参照，不是纯主观；
2. **结构化输出**：强制 judge 只输出 `{"relevance":..., "groundedness":..., "analysis":...}` JSON，`clamp01` 把分数夹到 [0,1]，`stripFences` 去掉 markdown 代码块标记，解析失败返回 [0,0] 不崩；
3. **建议评测时关缓存**（`ASSISTANT_CACHE_ENABLED=false`）——否则命中缓存导致每例不是独立生成，评测失真。

诚实讲，LLM-as-judge 有固有局限（judge 本身可能误判），它给的是**趋势性的质量信号**，不是精确真值。价值是可复现、可回归——改了 prompt/工具后重跑看指标动没动。

---

### Q89. 简历里说这个评测分数"暂未写具体数值"，为什么？

**高分回答**（极其诚实，加分）：因为**我还没在真实环境完整跑出稳定的 relevance/groundedness 数值**。评测端点（`/admin/evaluate`）代码写好了、逻辑验证过能跑，但要跑出可写进简历的基线分，需要在有 POI 数据的位置（lat/lng）完整跑一轮。我**坚持不编造分数**——项目一的 RAG 指标（90.5%/91.0%）是真跑出来的所以写了，项目二这个没跑稳就不写具体数字，只说"自建了 LLM-as-judge 评测集"。**宁可少写一个数字，也不编一个**。这个态度本身就是可信度的证明。

---

### Q90. 评测用例为什么是这 12 条？怎么设计的？

**高分回答**（`EVAL_QUERIES`）：覆盖**典型用户表述模式**——推荐类（"周末带娃""朋友聚餐""情侣约会"）、类别类（"咖啡馆""图书馆""公园"）、模糊意图（"随便推荐个地方""附近好玩的"）、体验类（"安静看书的角落""拍照打卡"）。设计意图：**覆盖真实用户会怎么问**，尤其包含模糊表述（考验模型会不会调 listCategories/getUserPreferences 澄清）。诚实讲，12 条是小基线集，只能反映方向性质量，不是统计严谨的评测。要更严谨得扩到几十上百条、多位置。

---

### Q91. 评测和项目一的评测方法论有什么呼应？

**高分回答**（体现方法论一致性）：两个项目都用了**固定用例集 + LLM-as-judge + 客观化打分**的思路：
- 项目一：RAGAS 三指标（Recall/Faithfulness/Relevance），把主观分拆成"关键点/论断被覆盖比例"的客观口径；
- 项目二：relevance/groundedness，groundedness 用"候选列表"做参照客观化。

呼应点：**都不满足于让 LLM 给一个笼统总分，而是想办法引入可核对的客观参照**。这说明我对"AI 系统怎么评测"有一套自己的方法论，不是两个项目各写各的。JD 里"搭建评估闭环（autorater/LLM-as-judge）"要的就是这个。

---

### Q92. 评测发现质量不好，你会怎么改进？

**高分回答**（闭环思维）：评测是为了**驱动迭代**。假设 groundedness 低（模型爱编地点）：① 加强 system prompt 的"不编造"约束（已做多层）；② 检查是不是候选太少（半径/topK 调优）导致模型"没得推只好编"；③ 输出后处理——校验回答里的地点名是否在候选里，不在的过滤。假设 relevance 低：① 检查检索质量（语义排序准不准）；② 优化工具选择引导。关键：**评测→归因→改一处→重新评测**的闭环，而不是评完就完。评测建议纳入 CI（升级方案 P4），每次改动自动跑防止改坏。

---

## J. 通知 Outbox 可靠投递

### Q93. Outbox 模式是什么？解决什么问题？

**高分回答**（`NotificationServiceImpl` + Dispatcher，工程化核心，必考）：Outbox（发件箱）解决**"业务操作和消息发送的原子性"**问题。场景：用户点赞→要写业务数据 + 发通知。如果"写业务"和"发 MQ 消息"是两个独立操作，会出现不一致——业务提交了但 MQ 发失败（用户没收到通知），或 MQ 发了但业务回滚（发了不该发的通知）。

Outbox 做法：**把"发消息"变成"往 outbox 表写一条事件记录"，和业务写在同一个数据库事务里**——业务成功则事件必存、业务回滚则事件回滚，天然原子。然后一个**独立的调度器**扫描 outbox 表，把 PENDING 事件投递到 MQ。这样"业务和事件"强一致，"投递"异步可靠。

---

### Q94. 为什么 `sendNotification` 的事务传播是 REQUIRED 而不是 REQUIRES_NEW？（关键取舍）

**高分回答**（简历面试准备专门列的，Outbox 精髓）：**必须 REQUIRED（和业务同事务）**，这是 Outbox 的核心。`NotificationServiceImpl.sendNotification` 标 `@Transactional`（默认 REQUIRED），注释写明"与业务同事务——业务回滚则事件回滚"。

对比：如果用 **REQUIRES_NEW**（新开独立事务），outbox 事件会**独立提交**，就出现"**业务失败但事件已发**"的不一致——比如点赞业务后续步骤回滚了，但通知事件已经独立提交并投递，用户收到"XX点赞了你"但实际点赞没成功。所以**必须 REQUIRED**，让 outbox 事件和业务共享同一个事务边界，一起成功一起回滚。这是 Outbox 能保证一致性的根本前提——**事件写入必须搭业务事务的车**。

---

### Q95. 调度器（Dispatcher）是怎么投递事件的？

**高分回答**（`NotificationOutboxDispatcherServiceImpl`）：`@Scheduled(fixedDelay=15s)` 定时扫描：
1. `dispatchPendingEvents`——查 PENDING 状态、`nextRetryAt` 到期的事件（分页 batchSize），逐条 `dispatchSingleEvent`；
2. 每条：调 `publisher.publish` 发到 MQ → 成功标 SUCCESS、记 publishedAt；失败标 FAILED、attemptCount+1、`nextRetryAt=now+retryDelay`、记 lastError（截断 500）；
3. 还会**自动重试**未超上限且到期的 FAILED 事件（`findRetryableFailed`）。

三态状态机：**PENDING(0) → SUCCESS(1) / FAILED(2)**。

---

### Q96. 批量投递方法为什么不加 @Transactional？（很细的坑）

**高分回答**（Dispatcher 注释"修复#5"，体现对 Spring 事务的深刻理解）：因为**类内自调用不触发事务代理**——`scheduledDispatch` 调 `dispatchPendingEvents` 是同类方法自调用，走的是原始对象不是 Spring 代理，加 `@Transactional` 也**无效**（Spring 事务基于 AOP 代理，自调用绕过代理）。加了反而误导（让人以为有事务保护）。

而且**批量方法本就不该包在一个大事务里**——Outbox 期望的是"**逐条独立提交、一条失败不影响其它**"。所以每条通过 `repository.save(event)` 独立提交（无环绕事务时 Spring Data 每次 save 自带事务）。这正是 Outbox 投递的正确语义。**知道"自调用不走代理"这个坑，是 Spring 事务的进阶考点。**

---

### Q97. 失败重试怎么防止无限重试风暴？

**高分回答**（`dispatchSingleEvent` + maxAttempts）：**最大重试次数 + 死信**。每次失败 attemptCount+1，`if attempts >= maxAttempts(默认5)` 就**转入死信**（error 日志"已达上限转死信待人工处理"），不再自动重试。`findRetryableFailed` 查的是"**未超上限且到重试时间**"的，超上限的自然不再被捞。死信由**管理接口**（`AdminNotificationOutboxController`）人工处理/手动重试（`retryEvent`）。

为什么要上限：无限重试一个必然失败的事件（比如 payload 损坏）会**持续消耗资源形成风暴**。上限 + 死信 + 人工兜底是可靠投递的标准设计——**自动重试有限次，剩下交给人**。

---

### Q98. MQ（RabbitMQ）在这里的角色？消费端怎么保证幂等？

**高分回答**（`NotificationEventPublisherImpl` + `NotificationEventRabbitListener`）：
- **发布端**：`publish` 把 payloadJson 发到 exchange，**消息头带 `outboxId`** 供消费端幂等；
- **消费端**：`@RabbitListener` 消费，委托 `NotificationDeliveryService.deliver(payloadJson, outboxId)` 反序列化并写 Notification 表。**用 outboxId 做幂等去重**——同一 outboxId 的事件即使重复投递/消费，也只落库一次；
- **消费失败**：抛异常触发 retry，超限后由队列 **DLX（死信交换机）** 路由到死信队列，避免无限重试。

幂等的意义：MQ 的投递语义通常是 at-least-once（可能重复），outboxId 去重保证**重复消费不产生重复通知**。

---

### Q99. 如果没有 RabbitMQ（本地开发），通知还能工作吗？

**高分回答**（`NotificationEventPublisherImpl` "修复#1"，降级设计）：能。`rabbitTemplate==null`（`app.notification.outbox.rabbitmq.enabled=false` 或 MQ 不可用）时，publish **不再静默丢弃**，而是**直接调 `NotificationDeliveryService` 同步落库**。所以本地无 MQ 时通知照样发（同步直发），Dispatcher 标的 SUCCESS 名副其实。同样携带 outboxId 幂等。

为什么重要：**降级要真降级，不能假装成功**——之前的 bug 是无 MQ 时静默丢弃但仍标 SUCCESS（假成功）。修复后无 MQ 走同步兜底，保证功能可用。这是"降级路径也要保证正确性"的体现。

---

### Q100. Outbox 事件和真正的 Notification 是同一张表吗？

**高分回答**：不是，是两张：
- `notification_event_outbox`（Outbox 事件表）：存"待投递的事件"，含 payloadJson、状态、重试信息——是**投递中转站**；
- `notification`（通知表）：存"用户实际收到的通知"——是**最终业务数据**。

流程：业务写 outbox 事件（同事务）→ 调度器投递到 MQ → 消费端 `deliver` 反序列化 payloadJson **写 Notification 表**。分开的原因：outbox 是**技术性的可靠投递机制**，notification 是**业务实体**，职责不同。outbox 事件投递成功后就是历史记录（可定期清），notification 是用户要查看的。

---

### Q101. 覆盖了哪些通知场景？

**高分回答**：6 类通知场景——点赞、回复、关注等社交互动（简历写的"覆盖点赞/回复/关注等 6 类"）。业务代码（POIShareService/UserFollowService 等）在相应操作后调 `sendNotification(recipientId, actorId, type, ...)`，统一走 Outbox。设计价值：**业务方零改动地接入可靠投递**——业务只管调 `sendNotification`，Outbox 的一致性/重试/幂等对业务透明。这是好的抽象——把可靠性下沉到通知基础设施，业务不感知。

---

### Q102. 通知 Outbox 有配套测试吗？测了什么？

**高分回答**（`NotificationOutboxDispatcherServiceImplTest` 等，简历写的"21 个单元测试"）：测试覆盖**三态状态机和重试逻辑**——PENDING→SUCCESS（投递成功）、PENDING→FAILED（投递失败置 FAILED + nextRetryAt）、FAILED 重试、超 maxAttempts 转死信、重试计数、手动 retryEvent 等。为什么重点测这块：Outbox 的**状态流转和重试是核心可靠性逻辑，也最容易写错边界**（计数、时间、状态判断）。用单测锁住这些逻辑，改动后能回归。这体现了"对关键可靠性逻辑做充分单测"的意识。

---

### Q103. Outbox 事件表会无限增长吗？

**高分回答**（诚实）：会——投递成功的事件默认留在表里。缓解上应有**定期清理已 SUCCESS 且超一定时间的事件**（类似语义缓存的定时清理）。诚实讲，当前重点在投递可靠性和重试，清理策略是可以补的运维项。SUCCESS 的历史事件保留一段时间有价值（排查、审计），但要设过期。这和 task_store、语义缓存是同一个"无界增长要清理"的原则——我在语义缓存和登录风控本地态做了定时清理，Outbox 的历史清理是可以对齐补上的点。

---

### Q104. 为什么用 Outbox 而不是直接在业务事务里发 MQ？

**高分回答**（追问 Q93 的本质）：因为**MQ 发送不能加入数据库事务**——数据库事务只能保证数据库操作的原子性，MQ 是外部系统。"业务写库 + 发 MQ"如果直接写在一起：库事务提交了但发 MQ 失败→通知丢；或发了 MQ 但库事务回滚→假通知。**两个异构系统无法用一个事务保证原子**。Outbox 的巧妙在于：**把"发消息"转化成"写库"**（写 outbox 表），这样它就能进数据库事务了，再用异步调度器把"库里的事件"搬到 MQ。本质是**用本地事务的确定性，换取跨系统的最终一致性**。这是分布式系统一致性的经典模式。

---

### Q105. RequestTraceFilter 生成的 traceId 怎么和 Outbox/异步串起来？

**高分回答**（可观测性衔接）：`RequestTraceFilter` 在请求入口生成 traceId 放进 `RequestTraceContext`（ThreadLocal）和日志 MDC。同步链路里日志、AOP 审计都带这个 traceId。但**异步链路（Outbox 调度器、MQ 消费）是独立线程**，ThreadLocal 不自动传递——诚实讲，跨异步的 traceId 传递需要把 traceId 存进 outbox payload 或消息头才能串联。当前 Outbox 事件本身有 id 可追踪。要做到"从用户点赞到通知落库"的全链路 traceId 贯通，需要在 payload 里带上 traceId。这是可观测性可以深化的点——同步链路已贯通，异步链路的 trace 传播是进阶项。

---

### Q106. Outbox 相比"事务消息"（如 RocketMQ 事务消息）有什么优劣？

**高分回答**（展示视野）：
- **Outbox 优势**：不依赖特定 MQ 的事务消息能力，**任何 MQ 都能用**（RabbitMQ 没有原生事务消息）；实现直观、可控、易调试（事件就在库里看得见）；
- **Outbox 劣势**：需要**额外的调度器轮询**（有延迟，我设 15s）+ 一张 outbox 表；轮询有一定 DB 压力；
- **事务消息（RocketMQ）**：MQ 原生支持，无需 outbox 表和轮询，延迟更低；但**绑定特定 MQ**、实现是黑盒。

我选 Outbox 是因为用的 RabbitMQ 没有事务消息，且 Outbox 的可见性/可控性适合这个项目。能说清两种方案的取舍，说明我理解的是"一致性问题的多种解法"而不是背一个模式。

---

## K. 可观测 / 登录风控 / AOP 审计

### Q107. 你的可观测性体系包含什么？

**高分回答**（升级项①）：三层：
- **traceId 全链路**：`RequestTraceFilter` 每个请求生成 traceId，进日志 MDC，一次请求的所有日志能用 traceId 串起来；
- **指标（Metrics）**：Micrometer 采集，`ObservabilityConfig` 配置，接 **Prometheus**（`ops/prometheus.yml`）+ **Grafana** 看板。AI 侧有 `assistant.chat.requests`（请求数）、`assistant.chat.cache`（缓存命中率）、`assistant.chat.latency`（延迟）、`assistant.tool.errors`（工具失败率）；
- **审计**：AOP 操作日志（见 Q112）。

价值：上线系统必须能**看到发生了什么**——延迟多少、缓存命中率、哪个工具在失败、谁做了什么操作。

---

### Q108. AI 助手上报了哪些 Micrometer 指标？为什么选这些？

**高分回答**（`AssistantChatService` + `ExplorerTools`）：
- `assistant.chat.requests`（counter）——对话请求量；
- `assistant.chat.cache{result=hit/miss/bypass}`（counter）——缓存命中/未命中/绕过（时效性）分布，能算**缓存命中率**（直接关系成本）；
- `assistant.chat.latency`（timer）——端到端延迟（P50/P99）；
- `assistant.tool.errors{tool=xxx}`（counter）——分工具的失败计数，发现高德路线 API、POI 检索的**隐性故障率**。

选这些因为它们对应 AI 服务的**核心健康指标**：量、成本（缓存率）、体验（延迟）、依赖健康（工具失败）。这几个指标能回答"AI 助手现在健不健康、贵不贵、快不快"。

---

### Q109. 缓存命中的 bypass 和 miss 为什么要分开统计？

**高分回答**（`meterRegistry.counter("assistant.chat.cache", "result", cacheable ? "miss" : "bypass")`，很细）：
- **miss**：可缓存的问题但没命中（缓存里没有相似的）；
- **bypass**：**主动绕过**缓存（时效性问题或多轮请求）。

分开的意义：**命中率的分母不同**。真正的缓存命中率应该是 `hit / (hit + miss)`——bypass 的请求本来就不该进缓存，不能算进"未命中"拉低命中率指标。如果把 bypass 混进 miss，会误以为缓存效果差。这体现了**指标定义的严谨**——统计口径要能反映真实效果，区分"没命中"和"根本没想命中"。

---

### Q110. 登录风控是怎么实现的？

**高分回答**（`LoginRiskControlServiceImpl`，升级项③）：**Redis 优先 + 本地内存降级**的失败计数锁定：
- `recordFailure`：登录失败时 Redis `increment` 计数（key `auth:login:fail:count:{user}`），达 `maxFailures`（默认 5）就设锁定 key（`auth:login:fail:lock:{user}`，锁 `lockDuration` 默认 900s）；
- `assertLoginAllowed`：登录前检查是否锁定，锁了抛 `BusinessException(429)`"请 X 秒后重试"；
- `clearFailures`：登录成功清计数。

**双保险**：Redis 不可用时降级到 `ConcurrentHashMap` 本地内存计数（`ObjectProvider.getIfAvailable` 拿不到 Redis 就 null → 走本地）。防暴力破解的标准设计——**多次失败锁定 + 缓存故障不阻断登录**。

---

### Q111. 登录风控的本地内存降级，会不会内存泄漏？（真实修复）

**高分回答**（`purgeExpiredLocalState` "修复#7"，体现细致）：会，所以加了定时清理。问题：`cleanupLocalState` 只清"当次访问到的用户名"，如果 Redis 宕机降级期间遭遇**随机用户名攻击**（每次换一个不存在的用户名试），这些用户名的计数条目**再也不会被访问到**，会长期滞留导致内存缓慢增长。

修复：`@Scheduled(fixedDelay=10min) purgeExpiredLocalState` **周期性全量扫描**本地 Map，清掉所有过期条目。只作用于本地内存（Redis 正常时这些 Map 本就空）。这是个很真实的边界——**降级路径的资源清理容易被忽略**，但恰恰是被攻击时才走降级，更要防内存问题。能想到"随机用户名攻击导致降级态内存增长"说明考虑了攻击场景。

---

### Q112. AOP 操作审计切面怎么工作的？

**高分回答**（`OperationLogAspect`，升级项②）：**注解驱动**的审计——`@Around("@annotation(operationLog)")` 拦截所有标了 `@OperationLog` 的方法，自动落库到 `AdminOperationLog`。一次调用采集：
- **操作人**：从 `SecurityContextHolder` 取 Long userId；
- **业务参数**：**SpEL 表达式**从方法入参提取 targetId/summary（`@OperationLog(targetIdSpel="#id", summarySpel="...")`）；
- **请求元数据**：HTTP method/URI/IP（含 X-Forwarded-For）；
- **traceId**：复用 `RequestTraceContext`，审计和日志/指标串联；
- **结果**：成功/失败 + 耗时 + 异常信息。

价值：管理员的每个敏感操作（改用户角色、审核内容）都有据可查。

---

### Q113. 为什么审计切点用 `@annotation` 而不是按包名/类名匹配？

**高分回答**（`OperationLogAspect` 注释）：因为**本项目 Controller 平铺无子包**（都在 `controller` 包下，没有按业务分子包），无法用 `execution(* com.smartcampus.controller.admin..*)` 这种包路径精准匹配"哪些是要审计的管理操作"。用 `@annotation(operationLog)` 是**注解驱动、零命名耦合**——想审计哪个方法就标 `@OperationLog`，和包结构/命名无关，精准且灵活。这是 AOP 切点设计的取舍——**注解驱动 > 命名约定驱动**，尤其当代码结构不规整时。

---

### Q114. 审计日志落库失败会影响主业务吗？为什么用 REQUIRES_NEW？

**高分回答**（`OperationLogAspect` + record 的 REQUIRES_NEW）：**不影响主业务，且失败记录不丢**。两个设计：
1. **容错**：审计落库 `try/catch`，失败只 `log.warn` **不阻断主业务**——审计是旁路，不能因为记日志失败让业务操作失败；
2. **REQUIRES_NEW**：`AdminOperationLogService.record` 用独立事务（REQUIRES_NEW）——这样**即使主业务事务回滚，审计记录仍独立提交**。为什么重要：主业务失败（比如审核操作报错回滚）时，**恰恰要记下"谁在什么时候尝试了这个失败操作"**（异常分支 finally 里落库后 re-throw 原异常）。如果审计和主业务同事务，主业务回滚会把"失败尝试"的审计也回滚掉，就丢了关键的失败记录。

**对比 Outbox 的 REQUIRED**：审计要 REQUIRES_NEW（独立，失败也要记），Outbox 要 REQUIRED（同事务，业务回滚事件也回滚）——**同样是事务传播，因目的不同选相反策略**，这个对比很能体现理解深度。

---

### Q115. 审计里 SpEL 求值失败会怎样？

**高分回答**（`evalSpel`）：`try/catch` 返回 null，**不阻断审计**。SpEL 表达式可能写错或参数缺失，如果求值失败就抛异常会导致整个审计失败甚至影响主流程（虽然主流程有 catch）。所以 SpEL 求值失败只是"这条审计的 targetId/summary 取不到"，降级用 action 名当 summary，审计照记。防御性——**审计的辅助信息提取失败，不该拖垮审计本身**。体现了"每一层都要容错"的一致风格。

---

### Q116. traceId 是怎么在一次请求内传递的？

**高分回答**（`RequestTraceFilter` + `RequestTraceContext`）：`RequestTraceFilter`（Filter，请求入口）为每个请求生成/透传 traceId，存进 `RequestTraceContext`（基于 ThreadLocal）和日志框架的 MDC。同一请求线程内，所有日志自动带 traceId（logback pattern 里引用 MDC），AOP 审计也从 `RequestTraceContext.getCurrentTraceId()` 取。所以一次请求的所有日志/审计能用同一个 traceId 检索。局限：ThreadLocal 不跨异步线程（Q105），跨线程要手动传。这是分布式追踪的基础——**给每个请求一个唯一标识贯穿全链路**。

---

### Q117. Grafana 看板你会重点看哪些图？

**高分回答**（运维视角）：AI 侧：**缓存命中率**（直接看省了多少钱）、**端到端延迟 P99**（体验）、**工具失败率**（依赖健康，尤其高德 API）、**请求量趋势**（容量）；平台侧：**HTTP 请求 QPS/错误率/延迟**、**JVM 内存/GC**、**数据库连接池**、**Outbox 积压量**（PENDING/FAILED 数量，积压说明投递有问题）、**登录失败率**（安全）。核心是能一眼看出"系统健不健康、AI 贵不贵、有没有依赖在悄悄失败"。指标的价值是**在用户投诉前发现问题**。

---

## L. JWT 安全 / Spring Security

### Q118. JWT 认证流程讲一下。

**高分回答**（`JwtTokenProvider` + `JwtAuthenticationFilter`）：
- **签发**（登录成功）：`generateToken(userId, username, role)`——subject=userId，claim 带 username/role，设 issuedAt/expiration，HMAC 签名；
- **校验**（每个请求）：`JwtAuthenticationFilter`（OncePerRequestFilter）从 `Authorization: Bearer xxx` 提 token → `validateToken` 验签验过期 → 取 userId → 黑名单检查 → 用户状态检查 → `setAuthentication` 把 userId 作 principal、role 作 authority 放进 SecurityContext。

后续 Controller 用 `Authentication` 拿 userId、`@PreAuthorize` 判角色。token 24h 过期需重新登录（正常行为）。

---

### Q119. JWT 是无状态的，怎么实现"登出"或"改密后让旧 token 失效"？

**高分回答**（`JwtAuthenticationFilter` 的 revoke_before 方案，很好的考点）：JWT 无状态、签发后到期前一直有效，天然不支持主动吊销。我用 **`revoke_before` 黑名单**：
- **登出/改密时**：Redis 设 `jwt:revoke_before:{userId} = 当前时间戳(ms)`；
- **每次校验时**：如果 token 的 `issuedAt < revoke_before`，说明这个 token 是"吊销时刻之前签发的"，视为已吊销。

这样一次操作就能让该用户**所有旧 token 失效**（改密后旧 token 全废），而新登录签发的 token（issuedAt 更新）不受影响。用 Redis 存 revoke_before（带 TTL = token 有效期，过期自动清）。这是给无状态 JWT 加"有状态吊销能力"的经典方案——**用一个时间戳分界"吊销前/后签发的 token"**。

---

### Q120. revoke_before 为什么要减 1000ms？（真实修复的边界 bug）

**高分回答**（`JwtAuthenticationFilter` 注释详解，极细致的坑，超加分）：因为**JWT 的 iat 精度只到"秒"，而 revoke_before 是完整毫秒**。

具体 bug：`getIssuedAt().getTime()` 拿到的 iat 已被**截断到整秒**（比如 10:00:00.000），而登出时设的 revoke_before 是完整毫秒（比如 10:00:00.567）。如果用户"登出后同一秒内立刻重新登录"，新 token 的秒级 iat（10:00:00.000）**恰好 ≤ 登出毫秒值（10:00:00.567）**，会被误判为"吊销前签发"→ **新 token 被误杀，用户重登后仍 401**。

修复：判断条件用 `issuedAt < revoke_before - 1000`，加 **1 秒宽限**消除这个秒/毫秒精度差导致的误杀。这是个**极其隐蔽的时间精度边界 bug**——能定位到"iat 秒级截断 vs revoke_before 毫秒"这个根因并修，是很强的 debug 能力体现。

---

### Q121. JwtAuthenticationFilter 里为什么要缓存用户信息？

**高分回答**（`JwtAuthenticationFilter` 的 user:info 缓存）：**每个请求都要拿用户的 status/role 做鉴权**（判断是否被禁用、什么角色）。如果每个请求都查数据库，DB 压力大。所以缓存：`user:info:{userId} = "status:role"`，Redis 存 5 分钟 TTL。命中就用缓存，未命中查库再写缓存。权衡：5 分钟 TTL 意味着**用户被禁用/改角色后最多 5 分钟才生效**——用短 TTL 换 DB 减压。对"禁用要立即生效"的场景可以配合 revoke_before（改角色时也 revoke）。这是**鉴权路径的性能优化**——高频读的鉴权数据要缓存，但要接受一点一致性延迟。

---

### Q122. 用户被禁用了，怎么处理？

**高分回答**（`JwtAuthenticationFilter`）：校验时取到 status，`if UserStatus.fromCode(status) == DISABLED` → `writeUnauthorizedResponse(401, "账号已被禁用")`，清 SecurityContext，直接返回不放行。所以禁用用户即使持有有效 token 也访问不了（最多 5 分钟缓存延迟）。要立即生效可在禁用时同时清 `user:info` 缓存 + 设 revoke_before。状态校验放在鉴权 filter 里是对的——**每个请求都验状态**，而不是只在登录时验。

---

### Q123. JWT 的 secret 怎么管理？签名算法是什么？

**高分回答**（`JwtTokenProvider.getSigningKey`）：HMAC-SHA（`Keys.hmacShaKeyFor`），secret 从 `JwtConfig` 读（配置化，来自配置文件/环境变量，不硬编码）。HMAC 是对称签名——同一个 secret 签发和验证。secret 必须够长（HMAC-SHA256 要 ≥256 位）且保密（泄露=能伪造任意 token）。诚实讲，对称签名的局限是"验证方也能签发"，如果有多服务且不想共享签发能力，应该用 RS256（非对称，私钥签公钥验）。单体应用用 HMAC 够了。secret 走配置注入是基本安全。

---

### Q124. Spring Security 的整体配置怎么做的？

**高分回答**（`SecurityConfig`）：无状态（STATELESS，不用 session）；`JwtAuthenticationFilter` 加在 `UsernamePasswordAuthenticationFilter` 之前；配置公开路径（登录/注册/公开查询）和需鉴权路径；`@EnableMethodSecurity` 开启 `@PreAuthorize` 方法级权限；自定义 `CustomAuthenticationEntryPoint`（未认证 401）和 `CustomAccessDeniedHandler`（无权限 403）返回统一的 Result JSON。角色体系 `UserRole`（SUPER_ADMIN/ADMIN/USER）。这是典型的 **JWT + 无状态 + 方法级权限** 的 Spring Security 配置。

---

### Q125. principal 为什么直接是 Long userId 而不是 UserDetails 对象？

**高分回答**（`setAuthentication`）：`UsernamePasswordAuthenticationToken(userId, null, authorities)` 直接把 **Long userId 作 principal**。好处：后续任何地方（Controller、AOP 审计、AI 的 toolContext）拿当前用户就是 `authentication.getPrincipal()` 强转 Long，**简单直接、无需再查 UserDetails**。代价：principal 上只有 id 没有完整用户对象，需要用户详情时得查库。权衡：绝大多数场景只需要 userId（做归属判断、审计、透传），完整用户对象按需查。这是**轻量 principal**设计——principal 只放最必要的身份标识（id + role authority），符合无状态高频鉴权的性能需求。AI 的 ToolContext 传 userId 也正是靠这个（`instanceof Long`）。

---

## M. PostGIS / 数据 / 部署

### Q126. Hibernate Spatial + PostGIS 怎么存储和查询地理数据？

**高分回答**：POI 的位置用 **PostGIS 的 geometry 类型**（`POINT`，EPSG:4326/WGS-84），通过 **Hibernate Spatial** 映射到实体的空间字段（JTS 的 `Point`）。查询：范围查询用经纬度 BETWEEN（`findWithinBounds`，走普通索引），复杂空间关系用 PostGIS 函数（`ST_Within/ST_Intersects/ST_DWithin`）。这样既能用 JPA 管理 POI 实体，又能用 PostGIS 的空间能力。POI 同时有普通经纬度列（lat/lng，BigDecimal）和空间列，检索时用经纬度做边界框粗筛。

---

### Q127. 一个 PostgreSQL 同时装 PostGIS 和 pgvector，会不会冲突？

**高分回答**：不冲突，这正是选型的巧妙——**一个库同时装 `postgis` 和 `vector` 两个扩展**（`01-extensions.sql` 建扩展），POI 的空间列用 PostGIS，POI 的向量存 `poi_embedding` 表用 pgvector。好处：**空间数据和向量数据在同一个库**，空间约束混合检索时，"地理围栏"（经纬度/PostGIS）和"语义排序"（pgvector）都在一个库里完成，无需跨库 join 或跨中间件。这是"一个 PG 打天下"的选型——省中间件、数据不出库。两个扩展各管各的类型，井水不犯河水。

---

### Q128. 4832 个 POI 的向量检索性能怎么样？需要索引吗？

**高分回答**：4832 是**小规模**，即使 pgvector 全表扫（暴力 KNN）也很快（毫秒级）。而且我的检索是**先地理围栏缩小到候选集**（`WHERE poi_id IN (...)`），`findSimilar` 只在几十个候选里算余弦，更快。所以当前规模**不强依赖向量索引**（HNSW）。诚实讲，POI 涨到几十万时才需要建 HNSW 索引，且那时候要重新考虑"IN 候选集 + 向量索引"的配合（IN 过滤可能让索引失效，需要评估）。当前是"小数据 + 地理预过滤"，性能不是瓶颈。语义缓存表倒是提到要防膨胀影响 HNSW。

---

### Q129. POIRepository.findWithinBounds 为什么能走索引？

**高分回答**：因为它是**经纬度 BETWEEN 查询**（`latitude BETWEEN ? AND ? AND longitude BETWEEN ? AND ?`），经纬度列上有 **B-tree 索引**（`POIDatabaseIndexInitializer` 初始化索引），BETWEEN 范围查询能用 B-tree。这也是我选"边界框（BETWEEN）+ Haversine"而非纯 ST_DWithin 的一个实际考量——**复用已有的经纬度索引**，而 ST_DWithin 要 GiST 空间索引（另一套）。边界框粗筛走 B-tree 快速砍候选，是这个方案性能可行的基础。

---

### Q130. Docker Compose 编排了哪七个服务？依赖怎么保证？

**高分回答**（`docker-compose.yml`）：PostGIS（含 pgvector）、Redis、RabbitMQ、Prometheus、Grafana、后端、前端。依赖保证：**健康检查 + depends_on**——后端 `depends_on` 数据库/Redis/MQ 的 healthy 状态，等依赖真正就绪才启动（不是启动就连），避免"数据库还没起来后端就连失败"。全配置**环境变量化**（`.env`），DB 初始化脚本（`db/init/01-03`）在 PG 首次启动自动跑。目标：**一条命令分钟级拉起完整环境**。这是现代应用交付的标准——环境即代码，可复现。

---

### Q131. db/init 里的三个 SQL 脚本分别做什么？

**高分回答**：`01-extensions.sql`（建 postgis + vector 扩展）、`02-pgvector.sql`（建 poi_embedding 向量表）、`03-assistant-cache.sql`（建 assistant_cache 语义缓存表）。**踩坑**（Q10）：这三个只在**容器 discover 库首次初始化时自动跑**，宿主机 campus_map 库需**手动执行 02/03**（vector 扩展已装）。这就是"本地和容器两套库结构要手动对齐"的根源。诚实讲，理想应该用 **Flyway/Liquibase 迁移**统一管理 schema（项目里 `db/migration` 有 Flyway 风格的迁移脚本），保证所有环境结构一致，避免手动建表遗漏导致 fail-open 静默失效。

---

### Q132. 数据库迁移是怎么管理的？

**高分回答**（`src/main/resources/db/migration/`）：有 `V1__baseline.sql` 基线 + 一系列 `migration_*.sql`（成就、邮箱、审计增强、通知 outbox、通知、POI 申请、标签、关注、路线等）。按功能演进逐步迁移。诚实讲，`db/init`（Docker 初始化）和 `db/migration`（迁移脚本）两套并存，理想应统一到 Flyway 自动执行。这反映了项目是**在存量系统上增量演进**的真实状态——每加一个功能加一个迁移脚本，历史包袱和规范并存。面试时可以坦诚这是可以收敛统一的地方。

---

### Q133. 前端是怎么组织的？和后端怎么对接？

**高分回答**（`smart-campus-frontend`）：Vue3 + Vite + Element Plus + Pinia。25 个页面分**公开端**（Home/地图/用户主页/路线/设置）和**运营后台**（Admin/ 下 18 个管理页）。`api/` 下每个业务模块一个 API 文件（37 个），`utils/request.js` 统一封装 axios（JWT 注入、错误处理、`requestCache` 请求缓存）。地图用高德 JS SDK（`utils/amap.js`）。AI 助手是 `components/assistant/AssistantChat.vue`，通过 `api/assistant.js` 连 SSE。Pinia 管理 user/map/poi 状态。前后端分离，JWT 鉴权，SSE 流式对接 AI。

---

### Q134. AI 助手前端是怎么消费 SSE 流的？

**高分回答**（`components/assistant/AssistantChat.vue` + `api/assistant.js`）：用 `fetch` 的流式读取或 EventSource 消费 `/api/assistant/chat` 的 SSE，每收到一个 data 块就追加到当前回答气泡，实现逐字（逐块）显示。因为后端是"非流式 call + 分块回放"，前端收到的是一段段 30 字符的增量，拼起来就是完整回答。注意 `/chat` 是 SSE 需要带 JWT——踩坑：SSE/EventSource 不能设自定义 header 时要用别的方式传 token（项目一用 `?api_key=`，项目二 chat 用 POST + fetch stream 可带 header）。前端还要处理 error 事件（显示"助手出错了"）和降级提示（助手未启用）。

---

## N. 深挖 / 压力测试 / 反问

### Q135. 你这个 AI 助手和市面上的"套壳 ChatGPT"有什么区别？

**高分回答**：套壳是"把用户问题直接转发给 LLM"，我这个是**深度绑定业务数据和空间约束的 Agent**：
1. **接真实平台数据**——推荐的是平台里 4832 个真实 POI，不是 LLM 编的；
2. **空间约束**——推荐限定在用户地理可达范围（围栏+语义），套壳做不到；
3. **Tool Use**——能调路线规划、类别查询、个性化偏好等平台能力；
4. **生产工程**——语义缓存降本、限流护栏、越权防护、可观测、评测。

套壳的价值是"能聊天"，我的价值是"**能在真实地图业务里给出可信、可达、安全的推荐**"。区别就是"有没有和业务深度耦合 + 有没有生产级工程"。

---

### Q136. 如果用户量涨 100 倍，这个 AI 助手的瓶颈在哪？

**高分回答**（有分析）：
1. **LLM 调用成本和 QPS**——最大瓶颈。缓解已有语义缓存（降调用量）、限流（防刷）；100 倍要靠更高缓存命中率 + 可能的模型降级（简单查询用小模型）+ DashScope 配额扩容；
2. **embedding 调用**——每次检索都 embed，量大要缓存 query 向量或本地化 embedding 模型；
3. **向量检索**——4832 POI 不是瓶颈，但 POI 涨了要建 HNSW 索引；
4. **数据库连接**——检索+缓存都打 PG，要连接池调优、读写分离。

诚实定位：这是**上线的作品级系统**，扛得住中小流量，但**没为超大规模专门调优**。100 倍要系统性做：缓存分层、embedding 缓存、模型分级、DB 扩展。能说清瓶颈和方向就够了。

---

### Q137. 你项目里最"不完美"、最想改的地方是什么？（诚实反思）

**高分回答**（一口气说真实缺陷）：
1. **增量嵌入没接线**——POI 改了要手动 embed-all，应在写路径埋点；
2. **输出侧护栏缺失**——POI 无状态字段，AI 可能推荐已封禁地点；
3. **评测分数没跑出真实值**——LLM-as-judge 端点写好了但没跑稳，简历里都不敢写数字；
4. **db/init 和 db/migration 两套 schema 管理**——应统一到 Flyway；
5. **伪流式**——受 Spring AI bug 所限，等框架修复应切真流式；
6. **Outbox 历史事件无清理**——会增长；
7. **前端"点推荐结果联动地图"未做**——推荐了但不能点击定位。

能列出七个真实缺陷并知道怎么改，是我真的在乎这个系统的证明，而不是背项目。

---

### Q138. Spring AI 那个流式 bug，你花了多久定位？怎么排查的？

**高分回答**（展示 debug 能力）：这类框架兼容 bug 最难的是**判断是自己代码错还是框架错**。排查路径：① 报错 `NoSuchElementException: No value present` 在 Spring AI 内部栈，不在我代码——怀疑框架；② 做**控制变量**：纯流式无 tools（正常）、纯非流式带 tools（正常）、流式+tools（崩）——**定位到是"流式+tools"这个特定组合**；③ 结合 DashScope 兼容端点和标准 OpenAI 流式工具协议的差异，判断是框架对 DashScope 流式工具增量的解析问题；④ 决策：不去改框架源码（成本高、升级会冲突），而是**绕过**——非流式 call + 分块回放，保留工具能力和前端体验。关键是**控制变量精准定位 + 务实绕过而非硬刚**。

---

### Q139. 如果让你给这个 AI 助手加一个最有价值的新功能，你会加什么？

**高分回答**（产品+技术判断）：**"推荐结果联动地图 + 多步行程规划"**。现在推荐是纯文字，用户看到"星巴克距你 200 米"但不能点击在地图上定位。我会：① 前端把推荐的 POI 结构化返回（不只文字），点击在地图高亮定位、一键路线规划；② 后端支持"周末半日游"这类多步编排——LLM 自主组合 `recommendNearbyPlaces` + `planRoute` 生成一条包含多个点的行程（这个 LLM 本身能编排，主要靠 system prompt 引导 + 前端渲染行程）。价值：从"问答"升级到"**可执行的行程助手**"，真正把 AI 和地图业务闭环。这也是升级方案 P4 提到的方向。

---

### Q140. 两个项目你都用了 RAG，如果只保留一个 RAG 设计讲给我听，你讲哪个？为什么？

**高分回答**（引导到差异化亮点）：我会讲**项目二的空间约束混合检索**。因为项目一的混合 RAG（向量+BM25+RRF+rerank+HyDE）是**业界成熟的通用范式**，讲出来体现"我会标准方案"；而项目二的"地理围栏+语义排序"是**针对地图域的定制设计**，体现"我能根据场景改造 RAG 而不是套模板"。尤其"先硬约束（距离）过滤、再软偏好（语义）排序""为什么不用 ST_DWithin 而用边界框+Haversine 的工程取舍"——这些是**我自己想的取舍**，最能体现工程判断力。通用方案证明基础，定制方案证明能力上限。

---

### Q141. 面试官质疑"你这就是把现成的东西拼起来"，怎么回应？

**高分回答**（价值主张）：拼是手段，**难的是拼得对、拼得可靠**。具体技术含量：
1. **Outbox 的事务传播取舍**（REQUIRED vs REQUIRES_NEW）——用错就数据不一致，这是分布式一致性的真功夫；
2. **空间约束检索**——不是拼 pgvector，是设计了"围栏+语义"两阶段过滤，还想清了为什么不用 ST_DWithin；
3. **Spring AI 流式 bug 的定位和绕过**——真实踩坑，控制变量定位 + 务实解决；
4. **JWT revoke_before 的毫秒精度 bug**——极隐蔽的边界，能修说明理解透彻；
5. **全链路 fail-open/fail-close 的判断**——哪些依赖挂了该降级、哪些该抛，每一处都是判断。

"拼"能做出 demo，但**在已上线系统上做出一致、安全、可观测、可评测的增量**才是工程能力。而且这是**真实上线、有用户**的系统，不是玩具。

---

### Q142. 这个项目是在已有系统上改的，你怎么保证不改坏原有功能？

**高分回答**（存量演进的关键）：几个策略：
1. **隔离**——AI 能力全在独立 `assistant` 包，`@ConditionalOnProperty` 一键开关，关掉就完全回退，不侵入核心；
2. **复用不改**——工具层复用现有 Service（POIService/RouteService），只读不改它们的逻辑；
3. **加不删**——Outbox 改造通知时，业务方调用 `sendNotification` 的签名不变（零改动接入）；
4. **测试兜底**——关键改造（Outbox 状态机）配 21 个单测，改动后回归；
5. **降级保平台**——AI/缓存/MQ 挂了都 fail-open，不拖垮主平台。

核心原则：**新功能可插拔、对存量只读复用、关键逻辑有测试、故障不外溢**。在跑着用户的系统上改，"不改坏"比"改得炫"更重要。

---

### Q143. 你怎么控制这个 AI 助手的成本？

**高分回答**（成本意识，JD 要）：多层：
1. **语义缓存**——相似问题命中直接回放，跳过 LLM+工具（最大降本，可看命中率指标）；
2. **限流**——每用户每分钟上限，防刷爆；
3. **护栏长度限制**——防超长输入涨 token；
4. **工具结果截断**（capResult 4000 字）——防 prompt 膨胀；
5. **个性化按需调**——不无脑预加载偏好，省 token 和 DB；
6. **批量嵌入**——embed-all 批处理省调用；
7. **可观测**——缓存命中率、请求量指标让成本可见可控。

我能说清"钱花在哪、怎么省的"——一次对话的成本 = embedding + LLM 生成 + 可能的工具调用，缓存命中则近乎零。**成本可观测 + 多层节流**是上线 AI 的必修课。

---

### Q144. 你这个项目独立做的，遇到最大的困难是什么？怎么解决的？

**高分回答**（真实，参考 2026.03-06）：最大困难是**在一个已上线、有存量数据和用户的系统上安全地加 AI**，不是从零写。三个具体难点：
1. **Spring AI 2.0 生态不成熟**——SB4 兼容版本少、流式+工具有 bug，很多要自己趟（控制变量定位 + 绕过）；
2. **空间约束检索的设计**——普通 RAG 教程没有"地理围栏内语义检索"，要自己想两阶段过滤、想清 ST_DWithin 的取舍；
3. **多环境数据库一致性**——本地 campus_map / 容器 discover 两套库、向量表要手动建、fail-open 掩盖建表遗漏——这些坑是靠踩出来+记录（交接文档）解决的。

解决方式的共性：**遇到框架/设计的空白地带，靠控制变量定位、务实取舍、并把坑记录下来避免重踩**。

---

### Q145. 最后一问：这个项目你最自豪的一个技术决策是什么？

**高分回答**（收尾，展示判断力）：**空间约束混合检索的"先围栏后语义"设计，以及为它做的一系列务实取舍**。自豪的不是用了 pgvector，而是**我想清了这个场景的本质**——地点推荐里"距离是硬约束、语义是软偏好"，所以应该先用廉价的边界框把候选缩到用户可达范围，再用 Haversine 精确剔除，最后在小候选集里做语义排序。而且我为它做了**清醒的取舍**：明明知道 PostGIS 有 `ST_DWithin` 更"专业"，但我选了"边界框+Haversine"来复用存量经纬度索引、避免耦合——并且我能说清这个取舍的两面（复用存量 vs 专业方案）。

这个决策体现的是我理解的工程判断力：**不是用最炫的技术，而是理解场景本质后，在'专业'和'务实'之间做对取舍，并且对取舍的代价心里有数**。这也呼应我在两个项目里一致的理念——能区分"能跑"和"合适"。

---

> **备考建议**：本文 145 题覆盖架构/Spring AI/空间检索/Tool Use/流式坑/缓存/记忆/安全/评测/Outbox/可观测/JWT/部署/反问十四大块。面试前重点吃透：① Q21-24 空间约束检索（尤其 Q24 ST_DWithin 取舍）、② Q47-52 流式兼容坑、③ Q55-64 语义缓存（尤其 Q56 地理分桶、Q58 时效性绕过）、④ Q93-96 Outbox 事务传播、⑤ Q114 审计 REQUIRES_NEW vs Outbox REQUIRED 对比、⑥ Q119-120 JWT revoke_before、⑦ Q40 ToolContext 防越权、⑧ N 部分所有反问题。**每条都能翻到源码，务必照代码讲、讲清真实踩坑与工程取舍。评测真实分数一旦跑出来，第一时间补进 Q86/Q89。**

